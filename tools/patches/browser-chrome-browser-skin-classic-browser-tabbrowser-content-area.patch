diff --git a/browser/chrome/browser/skin/classic/browser/tabbrowser/content-area.css b/browser/chrome/browser/skin/classic/browser/tabbrowser/content-area.css
index 4a4ee9c..e64e5b0 100644
--- ./browser/chrome/browser/skin/classic/browser/tabbrowser/content-area.css
+++ ./browser/chrome/browser/skin/classic/browser/tabbrowser/content-area.css
@@ -281,8 +281,8 @@
     --panel-min-width: 140px;
     min-width: var(--panel-min-width);
     max-width: calc(100% - var(--panel-min-width));
-    flex: 1;
-    width: 49.4%;
+    flex: 1 1 0;
+    width: auto;
   }
 
   &[splitview] {
@@ -341,9 +341,10 @@
       }
     }
 
+    /* Column ordering for N-way split */
+    /* First panel - no start margin */
     .split-view-panel[column="0"] {
-      order: 0;
-      margin-inline-end: 0;
+      order: var(--panel-order, 0);
 
       @media not (-moz-platform: macos) {
         z-index: 1;
@@ -376,9 +377,20 @@
       }
     }
 
+    /* Last panel - no end margin (detected via :last-child in flex) */
+    .split-view-panel:last-child {
+      margin-inline-end: 0;
+    }
+
+    /* All panels use order from attribute or CSS variable */
+    .split-view-panel {
+      order: var(--panel-order);
+    }
+
+    /* Splitters position between panels */
     .split-view-splitter {
-      order: 1;
       flex: none;
+      order: var(--splitter-order);
       -moz-user-focus: normal;
       border-color: transparent;
 
@@ -387,21 +399,21 @@
       }
     }
 
-    .split-view-panel[column="1"] {
-      order: 2;
-      margin-inline-start: 0;
+    /* Layout mode support */
+    &[split-view-layout="vertical"] {
+      flex-direction: column;
+    }
 
-      @media -moz-pref("browser.nova.enabled") {
-        /* Outer (inline-end) edge faces the window: the panel carries the
-         * full 2px gap, plus 2px for the selected panel's focus outline. */
-        :root[uidensity="compact"] & {
-          margin-inline-end: 2px;
-        }
+    &[split-view-layout="grid-2x2"] {
+      display: grid;
+      grid-template-columns: 1fr 1fr;
+      grid-template-rows: 1fr 1fr;
+    }
 
-        :root[uidensity="compact"] &.deck-selected {
-          margin-inline-end: 4px;
-        }
-      }
+    &[split-view-layout="grid-3x2"] {
+      display: grid;
+      grid-template-columns: 1fr 1fr 1fr;
+      grid-template-rows: 1fr 1fr;
     }
 
     /* Panels with a custom width shouldn't grow. */
