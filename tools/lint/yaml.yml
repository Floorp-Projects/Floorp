yamllint:
    description: YAML linter
    include:
        - .cron.yml
        - python/mozlint/
        - security/nss/.taskcluster.yml
        - taskcluster
    extensions: ['yml', 'yaml']
    type: external
    payload: yamllint_:lint
