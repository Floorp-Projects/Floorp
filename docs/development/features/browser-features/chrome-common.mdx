---
title: "Common Chrome Feature Catalog"
sidebar_label: "Common Chrome"
floorp_commit: "145eca89ab58c45ed05f3baf611eee4563a8351d"
generated: true
---
# Common Chrome Feature Catalog

Common chrome features are discovered from `browser-features/chrome/common/mod.ts` and are intended for loader-managed browser chrome behavior that can participate in hot reload.

## Feature Entries

| Feature | Source | Entrypoints | Description |
|---|---|---|---|
| addons | `browser-features/chrome/common/addons/index.ts` | `default class Addons`, `init` | Chrome Web Store Install Confirmation UI Customization This module modifies the addon installation confirmation popup to show Chrome extension specific messages when installing from Chrome Web Store. |
| browser-share-mode | `browser-features/chrome/common/browser-share-mode/index.ts` | `default class BrowserShareMode`, `init` | Owns the browser share mode feature through the BrowserShareMode chrome component and wires `browser-share-mode`. |
| browser-tab-color | `browser-features/chrome/common/browser-tab-color/index.ts` | `default class BrowserTabColor`, `init` | Owns the browser tab color feature through the BrowserTabColor chrome component and wires `tabcolor-manager`. |
| chrome-css | `browser-features/chrome/common/chrome-css/index.ts` | `default class ChromeCSS`, `init` | Owns the chrome css feature through the ChromeCSS chrome component and wires `service`. |
| command-palette | `browser-features/chrome/common/command-palette/index.ts` | `default class CommandPalette`, `init` | Owns the command palette feature through the CommandPalette chrome component and wires `service`, `components/CommandPalette`, `style.css?inline`. |
| context-menu | `browser-features/chrome/common/context-menu/index.ts` | `default class ContextMenu`, `init` | Owns the context menu feature through the ContextMenu chrome component. |
| designs | `browser-features/chrome/common/designs/index.ts` | `default class Designs`, `init` | Owns the designs feature through the Designs chrome component and wires `browser-design-element`. |
| external-browser | `browser-features/chrome/common/external-browser/index.ts` | `default class ExternalBrowser`, `init` | Owns the external browser feature through the ExternalBrowser chrome component and wires `tab-context-menu`, `link-context-menu`. |
| flex-order | `browser-features/chrome/common/flex-order/index.ts` | `default class FlexOrder`, `init` | Owns the flex order feature through the FlexOrder chrome component and wires `flex-order`. |
| hub-panel-menu | `browser-features/chrome/common/hub-panel-menu/index.ts` | `default class HubPanelMenuComponent`, `init` | Owns the hub panel menu feature through the HubPanelMenuComponent chrome component and wires `HubPanelMenu`. |
| keyboard-shortcut | `browser-features/chrome/common/keyboard-shortcut/index.ts` | `default class KeyboardShortcut`, `init` | Owns the keyboard shortcut feature through the KeyboardShortcut chrome component and wires `service`, `config`. |
| modal-parent | `browser-features/chrome/common/modal-parent/index.ts` | `default class ModalParent`, `init` | Owns the modal parent feature through the ModalParent chrome component and wires `modalManager`, `modalElement`, `data/data`. |
| mouse-gesture | `browser-features/chrome/common/mouse-gesture/index.ts` | `default class MouseGesture`, `init` | Owns the mouse gesture feature through the MouseGesture chrome component and wires `service`, `config`, `components/GestureDisplay`. |
| panel-sidebar | `browser-features/chrome/common/panel-sidebar/index.ts` | `default class PanelSidebar`, `init` | Owns the panel sidebar feature through the PanelSidebar chrome component and wires `components/panel-sidebar`, `components/sidebar`, `components/sidebar-contextMenu`. |
| private-container | `browser-features/chrome/common/private-container/index.ts` | `default class PrivateContainer`, `init` | Owns the private container feature through the PrivateContainer chrome component and wires `browser-private-container`, `context-menu`. |
| profile-manager | `browser-features/chrome/common/profile-manager/index.ts` | `default class ProfileManager`, `init` | Owns the profile manager feature through the ProfileManager chrome component and wires `service`. |
| pwa | `browser-features/chrome/common/pwa/index.ts` | `default class Pwa`, `init` | Owns the PWA feature through the Pwa chrome component and wires `SsbPageAction`, `SsbPanelView`, `config`. |
| qr-code-generator | `browser-features/chrome/common/qr-code-generator/index.ts` | `default class QRCodeGenerator`, `init` | Owns the qr code generator feature through the QRCodeGenerator chrome component and wires `qr-code-button`, `qr-code-panel`, `style.css?inline`. |
| reboot-panel-menu | `browser-features/chrome/common/reboot-panel-menu/index.ts` | `default class RebootPanelMenuComponent`, `init` | Owns the reboot panel menu feature through the RebootPanelMenuComponent chrome component and wires `RebootPanelMenu`. |
| reverse-sidebar-position | `browser-features/chrome/common/reverse-sidebar-position/index.ts` | `init` | Provides initialization for the reverse sidebar position feature. |
| split-view | `browser-features/chrome/common/split-view/index.ts` | `default class SplitView`, `init` | Owns the split view feature through the SplitView chrome component and wires `split-view-manager`. |
| statusbar | `browser-features/chrome/common/statusbar/index.ts` | `default class StatusBar`, `init` | Owns the statusbar feature through the StatusBar chrome component and wires `context-menu`, `statusbar`, `statusbar-manager`. |
| tab | `browser-features/chrome/common/tab/index.ts` | `default class Tab`, `init` | Owns the tab feature through the Tab chrome component and wires `scroll`, `openPosition`, `sizeSpecification`. |
| tab-inline-edit | `browser-features/chrome/common/tab-inline-edit/index.ts` | `default class TabInlineEdit`, `init` | Owns the tab inline edit feature through the TabInlineEdit chrome component and wires `controller`. |
| tab-refresh | `browser-features/chrome/common/tab-refresh/index.ts` | `default class TabRefresh`, `init` | Owns the tab refresh feature through the TabRefresh chrome component and wires `controller`. |
| tab-sleep-exclusion | `browser-features/chrome/common/tab-sleep-exclusion/index.ts` | `default class TabSleepExclusion`, `init` | Owns the tab sleep exclusion feature through the TabSleepExclusion chrome component. |
| tab-stacks | `browser-features/chrome/common/tab-stacks/index.ts` | `default class TabStacks`, `init` | Owns the tab stacks feature through the TabStacks chrome component and wires `stack-bar`. |
| tab-strip-wheel-guard | `browser-features/chrome/common/tab-strip-wheel-guard/index.ts` | `default class TabStripWheelGuard`, `init` | Owns the tab strip wheel guard feature through the TabStripWheelGuard chrome component and wires `classifier`. |
| tabbar | `browser-features/chrome/common/tabbar/index.ts` | `default class TabBar`, `init` | Owns the tabbar feature through the TabBar chrome component and wires `multirow-tabbar/multirow-tabbar`, `tabbbar-style/tabbar-style`. |
| ui-custom | `browser-features/chrome/common/ui-custom/index.ts` | `default class UICustomization`, `init`, `initBeforeSessionStoreInit` | Owns the ui custom feature through the UICustomization chrome component and wires `styles/style-manager`, `layout/dom-manipulator`. |
| undo-closed-tab | `browser-features/chrome/common/undo-closed-tab/index.ts` | `default class UndoClosedTab`, `init` | Owns the undo closed tab feature through the UndoClosedTab chrome component and wires `styleElem`. |
| window-drag | `browser-features/chrome/common/window-drag/index.ts` | `default class WindowDrag`, `init` | Owns the window drag feature through the WindowDrag chrome component and wires `drag-session`. |
| workspaces | `browser-features/chrome/common/workspaces/index.ts` | `default class Workspaces`, `init` | Owns the workspaces feature through the Workspaces chrome component and wires `workspacesTabManager`, `utils/workspace-icons`, `workspacesService`. |
| zen-mode | `browser-features/chrome/common/zen-mode/index.ts` | `default class ZenMode`, `init` | Owns the zen mode feature through the ZenMode chrome component and wires `zen-mode`, `icon.css?inline`. |
