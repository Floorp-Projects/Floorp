name: Check TaskList

on:
  pull_request:
    types: [opened, edited, reopened]

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - name: Check TaskList
        uses: sumally/github-check-tasklist-action@v1.0.1
