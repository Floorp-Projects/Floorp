# wasmparser version

Current version is: 0.14.0

# Upgrade process

1. Pull latest release from npm and extract WasmDis.js and WasmParser.js, e.g.

```
curl https://registry.npmjs.org/wasmparser/-/wasmparser-0.14.0.tgz | tar -zx --strip-components 2 package/dist/{WasmDis,WasmParser}.js
```

2. Remove reference to source maps (last line)

