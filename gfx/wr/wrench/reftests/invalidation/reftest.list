# Test that a red -> green -> red rect correctly invalidates each frame
# Skipped on debug Android because of panic,
# GL error 502 at blit_framebuffer
skip_on(android,debug) dirty([(50,50):256x256]) one-rounded-rect.yaml dirty([(50,50):256x256]) one-rounded-rect-green.yaml dirty([(50,50):256x256]) one-rounded-rect.yaml == one-rounded-rect.yaml
