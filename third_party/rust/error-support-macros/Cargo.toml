[package]
name = "error-support-macros"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"
publish = false

[lib]
proc-macro = true

[dependencies]
syn = { version = "1.0", features = ["derive", "parsing", "full"]}
quote = "1.0"
proc-macro2 = "1.0"
