import os

Import('env')

penv = env.Clone()

src = [
    "client.c",
    "config.c",
    "encrypt.c",
    "mparray.c",
    "poly.c",
    "rand.c",
    "prg.c",
    "server.c",
    "serial.c",
    "share.c",
]

libs = [
  "msgpackc"
]

# Enable mp_print()
penv.Append(CFLAGS = ['-DMP_IOFUNC'])

penv.Append(LIBS = libs)
penv.StaticLibrary("mprio", src)

