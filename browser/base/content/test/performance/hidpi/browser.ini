[DEFAULT]
prefs =
  browser.startup.recordImages=true
  layout.css.devPixelsPerPx='2'

[../browser_startup_images.js]
skip-if = !debug || (os == 'win' && (os_version == '6.1'))  || (fission && !webrender) # fission && !webrender: The startup warning confuses the test. Also, this is an unsupported combo / hidpi results in the toolbar overflowing on Win 7
