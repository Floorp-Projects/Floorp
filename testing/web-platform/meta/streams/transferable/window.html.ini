[window.html]
  expected:
    if not fission and (os == "linux") and not swgl and (processor == "x86_64") and debug and webrender: ["ERROR", "OK"]
    if not fission and (os == "linux") and not swgl and (processor == "x86_64") and not debug and not webrender: ["ERROR", "OK"]
    if not fission and (os == "win") and debug and not webrender and (processor == "x86"): ["ERROR", "OK"]
    if not fission and (os == "win") and debug and webrender: ["ERROR", "OK"]
    if not fission and (os == "mac") and debug: ["ERROR", "OK"]
    if fission and (os == "linux"): ["ERROR", "OK"]
    [OK, ERROR]
  [transfer to and from an iframe should work]
    expected: FAIL

  [the same ReadableStream posted multiple times should arrive together]
    expected: FAIL

  [port.postMessage should be able to transfer a ReadableStream]
    expected: FAIL

  [window.postMessage should be able to transfer a ReadableStream]
    expected: FAIL

