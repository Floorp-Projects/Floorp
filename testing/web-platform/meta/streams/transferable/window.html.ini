[window.html]
  expected:
    if (os == "linux") and (processor == "x86_64") and not swgl and not debug and not webrender and not fission: [ERROR, OK]
    if (os == "linux") and (processor == "x86_64") and not swgl and not debug and webrender and fission: [ERROR, OK]
    if (os == "linux") and (processor == "x86_64") and not swgl and debug and not webrender: [ERROR, OK]
    if (os == "linux") and (processor == "x86_64") and not swgl and debug and webrender: [ERROR, OK]
    if (os == "android") and debug and not webrender: [ERROR, OK]
    if (os == "mac") and debug: [ERROR, OK]
    [OK, ERROR]
  [transfer to and from an iframe should work]
    expected: FAIL

  [the same ReadableStream posted multiple times should arrive together]
    expected: FAIL

  [port.postMessage should be able to transfer a ReadableStream]
    expected: FAIL

  [window.postMessage should be able to transfer a ReadableStream]
    expected: FAIL
