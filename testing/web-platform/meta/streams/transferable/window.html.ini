[window.html]
  expected:
    if (os == "linux") and not swgl and not fission and (processor == "x86_64") and debug and webrender: [ERROR, OK]
    if (os == "linux") and not swgl and fission and webrender and not debug: [ERROR, OK]
    if (os == "android") and not swgl and not debug and webrender: [ERROR, OK]
    if (os == "mac") and debug: [ERROR, OK]
    if (os == "android") and swgl: [ERROR, OK]
    if (os == "win") and swgl: [ERROR, OK]
    [OK, ERROR]
  [transfer to and from an iframe should work]
    expected: FAIL

  [the same ReadableStream posted multiple times should arrive together]
    expected: FAIL

  [port.postMessage should be able to transfer a ReadableStream]
    expected: FAIL

  [window.postMessage should be able to transfer a ReadableStream]
    expected: FAIL
