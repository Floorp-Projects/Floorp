[window.html]
  expected:
    if (processor == "x86_64") and (os == "android") and debug and webrender and not swgl: [ERROR, OK]
    if (processor == "x86_64") and (os == "linux") and not webrender and fission: [ERROR, OK]
    if (processor == "x86_64") and (os == "win") and swgl: [ERROR, OK]
    if (processor == "x86_64") and (os == "mac") and debug: [ERROR, OK]
    if processor == "x86": [ERROR, OK]
    [OK, ERROR]
  [transfer to and from an iframe should work]
    expected: FAIL

  [the same ReadableStream posted multiple times should arrive together]
    expected: FAIL

  [port.postMessage should be able to transfer a ReadableStream]
    expected: FAIL

  [window.postMessage should be able to transfer a ReadableStream]
    expected: FAIL
