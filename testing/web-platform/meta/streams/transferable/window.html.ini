[window.html]
  expected:
    if not fission and not debug and (os == "linux") and not webrender and (processor == "x86_64"): [ERROR, OK]
    if not fission and debug and not webrender and (os == "win"): [ERROR, OK]
    if fission and (os == "win") and debug: [ERROR, OK]
    if fission and (os == "win") and not debug: [ERROR, OK]
    [OK, ERROR]
  [transfer to and from an iframe should work]
    expected: FAIL

  [the same ReadableStream posted multiple times should arrive together]
    expected: FAIL

  [port.postMessage should be able to transfer a ReadableStream]
    expected: FAIL

  [window.postMessage should be able to transfer a ReadableStream]
    expected: FAIL
