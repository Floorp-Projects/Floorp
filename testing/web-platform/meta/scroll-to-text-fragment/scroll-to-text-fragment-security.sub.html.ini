[scroll-to-text-fragment-security.sub.html]
  expected:
    if not debug and (os == "linux") and webrender: ["TIMEOUT", "OK"]
    if debug and (os == "mac"): ["TIMEOUT", "OK"]
    if debug and (os == "linux"): ["TIMEOUT", "OK"]
    if debug and (os == "win"): ["TIMEOUT", "OK"]
    if not debug and (os == "mac"): ["TIMEOUT", "OK"]
    [OK, TIMEOUT]
  [Test that a text fragment directive requires a user activation (user_activation=true).]
    expected: [FAIL, TIMEOUT, NOTRUN]

  [Test that a text fragment directive is not activated when there is a window opener (noopener=true).]
    expected:
      if (os == "win") and not debug and (processor == "x86"): ["FAIL", "NOTRUN", "TIMEOUT"]
      if (os == "win") and debug and (processor == "x86_64"): ["TIMEOUT", "FAIL", "NOTRUN"]
      if (os == "linux") and debug and not webrender: ["TIMEOUT", "FAIL", "NOTRUN"]
      if (os == "linux") and debug and webrender: ["NOTRUN", "FAIL", "TIMEOUT"]
      if (os == "mac") and debug: ["TIMEOUT", "NOTRUN", "FAIL"]
      [FAIL, TIMEOUT, NOTRUN]

  [Test that a text fragment directive requires a user activation (user_activation=false).]
    expected:
      if (os == "linux") and debug and webrender: ["NOTRUN", "FAIL", "TIMEOUT"]
      if (os == "win") and (processor == "x86") and not debug: ["FAIL", "NOTRUN", "TIMEOUT"]
      [FAIL, TIMEOUT, NOTRUN]

  [Test that a text fragment directive is not activated within an iframe.]
    expected:
      if not debug and (os == "linux") and webrender: ["TIMEOUT", "NOTRUN", "FAIL"]
      if not debug and (os == "linux") and not webrender: ["FAIL", "NOTRUN", "TIMEOUT"]
      if not debug and (os == "win") and (processor == "x86_64"): ["FAIL", "TIMEOUT", "NOTRUN"]
      if not debug and (os == "win") and (processor == "x86"): ["FAIL", "NOTRUN", "TIMEOUT"]
      if not debug and (os == "android"): ["FAIL", "TIMEOUT", "NOTRUN"]
      if debug and (os == "android"): ["FAIL", "TIMEOUT", "NOTRUN"]
      [NOTRUN, FAIL, TIMEOUT]

  [Test that a text fragment directive is not activated when there is a window opener (noopener=false).]
    expected:
      if debug and (os == "linux") and not webrender: ["NOTRUN", "FAIL", "TIMEOUT"]
      if debug and (os == "linux") and webrender: ["NOTRUN", "TIMEOUT", "FAIL"]
      if debug and (os == "win") and (processor == "x86_64"): ["NOTRUN", "FAIL", "TIMEOUT"]
      if debug and (os == "win") and (processor == "x86"): ["TIMEOUT", "FAIL", "NOTRUN"]
      if not debug and (os == "win") and (processor == "x86"): ["FAIL", "NOTRUN", "TIMEOUT"]
      if debug and (os == "mac"): ["NOTRUN", "FAIL", "TIMEOUT"]
      if not debug and (os == "mac"): ["NOTRUN", "FAIL", "TIMEOUT"]
      [FAIL, TIMEOUT, NOTRUN]

