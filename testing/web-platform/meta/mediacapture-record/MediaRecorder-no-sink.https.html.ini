[MediaRecorder-no-sink.https.html]
  expected:
    if os == "android": TIMEOUT
  [PeerConnection MediaRecorder records {"audio":false,"video":true} from PeerConnection without sinks]
    expected:
      if os == "android": TIMEOUT

  [PeerConnection MediaRecorder records {"audio":true,"video":true} from PeerConnection without sinks]
    expected:
      if os == "android": NOTRUN
