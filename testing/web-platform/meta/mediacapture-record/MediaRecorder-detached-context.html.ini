[MediaRecorder-detached-context.html]
  expected:
    if (os == "linux") and debug and webrender and not fission: ["OK", "ERROR"]
    if (os == "linux") and not debug and webrender and not fission: ["OK", "CRASH"]
    if (os == "linux") and debug and not webrender: ["OK", "ERROR", "CRASH"]
  [MediaRecorder creation with detached context]
    expected: FAIL

