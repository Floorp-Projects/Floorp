[echo-large-bidirectional-streams.https.html]
  expected:
    if (os == "linux") and not debug and fission and (processor == "x86_64") and tsan: [TIMEOUT, OK]
    if (os == "linux") and not debug and fission and (processor == "x86_64") and not tsan: [OK, TIMEOUT]
    if (os == "linux") and not debug and fission and (processor == "x86"): [OK, TIMEOUT]
    if (os == "mac") and debug: [OK, TIMEOUT]
  [Ensure large bidirectional streams does not cause race condition]
    expected:
      if (os == "linux") and not debug and fission and (processor == "x86_64") and tsan: [TIMEOUT, PASS]
      if (os == "linux") and not debug and fission and (processor == "x86_64") and not tsan: [PASS, TIMEOUT]
      if (os == "linux") and not debug and fission and (processor == "x86"): [PASS, TIMEOUT]
      if (os == "mac") and debug: [PASS, TIMEOUT]
