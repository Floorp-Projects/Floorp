[SpeechSynthesis-pause-resume.tentative.html]
  expected:
    if debug and os == "linux" and bits == 32: OK
    TIMEOUT
  [speechSynthesis.pause() and resume() state and events]
    expected:
      if debug and os == "linux" and bits == 32: FAIL
      TIMEOUT

