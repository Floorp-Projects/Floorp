[spelling-error-005-crash.html]
  expected:
    if (os == "linux") and debug and fission and not swgl: [TIMEOUT, PASS]
    if (os == "linux") and debug and fission and swgl: [TIMEOUT, PASS]
    if (os == "linux") and debug and not fission and swgl: [TIMEOUT, PASS]
    if (os == "linux") and not debug and not swgl and fission: [TIMEOUT, PASS]
    if (os == "linux") and not debug and swgl: [TIMEOUT, PASS]
    [PASS, TIMEOUT]
