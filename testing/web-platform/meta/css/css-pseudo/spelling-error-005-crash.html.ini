[spelling-error-005-crash.html]
  expected:
    if (os == "linux") and not debug and not fission and swgl: [TIMEOUT, PASS]
    if (os == "linux") and not debug and not fission and not swgl: [TIMEOUT, PASS]
    if (os == "linux") and debug: [TIMEOUT, PASS]
    [PASS, TIMEOUT]
