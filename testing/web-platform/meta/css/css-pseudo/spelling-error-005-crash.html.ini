[spelling-error-005-crash.html]
  expected:
    if (os == "linux") and not debug and not swgl and not fission: [TIMEOUT, PASS]
    if (os == "linux") and not debug and not swgl and fission: [TIMEOUT, PASS]
    if (os == "linux") and debug: [TIMEOUT, PASS]
    [PASS, TIMEOUT]
