[user-action-pseudo-classes-in-has.html]
  expected:
    if os == "mac": ERROR
  [Invalidation with :focus, :focus-within, :focus-visible in :has()]
    expected:
      if os == "mac": TIMEOUT
      FAIL
