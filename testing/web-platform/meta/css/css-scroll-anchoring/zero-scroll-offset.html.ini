[zero-scroll-offset.html]
  [Scroll anchoring suppressed when scroll offset is zero.]
    expected:
      if not fission and (os == "linux") and not debug and not webrender: ["PASS", "FAIL"]
      if not fission and (os == "linux") and debug and webrender: ["PASS", "FAIL"]
      if not fission and (os == "linux") and debug and not webrender: PASS
      if not fission and (os == "win") and webrender and not debug: FAIL
      if not fission and (os == "android") and debug: ["PASS", "FAIL"]
      if fission and not debug: FAIL
      [FAIL, PASS]

