[css-text-line-break-ja-po-strict.html]
  [2035  REVERSED PRIME may NOT appear at line start if ja and strict]
    expected:
      if not webrender and (os == "win") and (processor == "x86") and debug: ["PASS", "FAIL"]
      if not webrender and (os == "win") and (processor == "x86") and not debug: ["PASS", "FAIL"]
      if not webrender and (os == "linux") and not debug: ["PASS", "FAIL"]

  [2030  PER MILLE SIGN may NOT appear at line start if ja and strict]
    expected:
      if (os == "linux") and not webrender and not debug: ["PASS", "FAIL"]

  [2032  PRIME may NOT appear at line start if ja and strict]
    expected:
      if (os == "linux") and not webrender and not debug: ["PASS", "FAIL"]

  [2103  DEGREE CELSIUS may NOT appear at line start if ja and strict]
    expected:
      if (os == "linux") and not webrender and not debug: ["PASS", "FAIL"]

  [FE6A  SMALL PERCENT SIGN may NOT appear at line start if ja and strict]
    expected:
      if (os == "linux") and not webrender and not debug: ["PASS", "FAIL"]

  [2109  DEGREE FAHRENHEIT may NOT appear at line start if ja and strict]
    expected:
      if (os == "linux") and not webrender and not debug: ["PASS", "FAIL"]

  [2033  DOUBLE PRIME may NOT appear at line start if ja and strict]
    expected:
      if (os == "linux") and not webrender and not debug: ["PASS", "FAIL"]

