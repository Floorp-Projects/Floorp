[background-color-animation-with-table2.html]
  expected:
    if (os == "win") and not swgl and (processor == "x86_64") and not debug and not fission: [TIMEOUT, PASS]
    if (os == "win") and not swgl and (processor == "x86") and not debug: [TIMEOUT, PASS]
    if (os == "win") and not swgl and (processor == "x86") and debug: [TIMEOUT, PASS]
    if (os == "android") and debug and not swgl: [TIMEOUT, PASS]
    if (os == "linux") and not swgl and debug: [TIMEOUT, PASS]
    if os == "mac": [TIMEOUT, PASS]
    [PASS, TIMEOUT]
