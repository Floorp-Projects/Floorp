[background-color-transparent-animation-in-body.html]
  expected:
    if (os == "linux") and debug and webrender and not swgl: ["PASS", "TIMEOUT"]
    if (os == "linux") and debug and webrender and swgl: ["PASS", "TIMEOUT"]
    if (os == "win") and debug and webrender and swgl: ["PASS", "TIMEOUT"]
    if (os == "win") and debug and webrender and not swgl: PASS
    if (os == "linux") and debug and not webrender: PASS
    if (os == "android") and not debug and webrender: ["PASS", "TIMEOUT"]
    if (os == "android") and not debug and not webrender: PASS
    if (os == "win") and not debug and not webrender: TIMEOUT
    if (os == "android") and debug: PASS
    [TIMEOUT, PASS]
