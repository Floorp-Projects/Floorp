[background-color-animation-with-table3.html]
  expected:
    if (os == "win") and (processor == "x86_64") and not swgl and webrender and not debug and fission: [TIMEOUT, PASS]
    if (os == "win") and (processor == "x86_64") and not swgl and not webrender and debug: [TIMEOUT, PASS]
    if (os == "win") and (processor == "x86") and debug: [TIMEOUT, PASS]
    if (os == "win") and (processor == "x86") and not debug: [TIMEOUT, PASS]
    if (os == "mac") and not webrender: [TIMEOUT, PASS]
    if (os == "mac") and webrender: [TIMEOUT, PASS]
    [PASS, TIMEOUT]
