[background-color-animation-with-table3.html]
  expected:
    if (os == "win") and (processor == "x86_64") and webrender and debug and not fission and not swgl: [TIMEOUT, PASS]
    if (os == "android") and not swgl and not debug and webrender: [TIMEOUT, PASS]
    if (os == "android") and not swgl and not debug and not webrender: [TIMEOUT, PASS]
    if (os == "win") and (processor == "x86_64") and not webrender and debug: [TIMEOUT, PASS]
    if (os == "win") and (processor == "x86_64") and not webrender and not debug: [TIMEOUT, PASS]
    if (os == "android") and swgl: [TIMEOUT, PASS]
    if (os == "win") and (processor == "x86"): [TIMEOUT, PASS]
    if os == "mac": [TIMEOUT, PASS]
    [PASS, TIMEOUT]
