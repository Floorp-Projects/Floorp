[background-color-animation-with-table3.html]
  expected:
    if (os == "linux") and webrender and debug and not swgl: [TIMEOUT, PASS]
    if (os == "android") and not swgl and not debug and webrender: [TIMEOUT, PASS]
    if (os == "win") and webrender and (processor == "x86") and debug: [TIMEOUT, PASS]
    if (os == "win") and not webrender and debug: [TIMEOUT, PASS]
    if (os == "win") and not webrender and not debug: [TIMEOUT, PASS]
    if (os == "mac") and webrender: [TIMEOUT, PASS]
    if (os == "mac") and not webrender: [TIMEOUT, PASS]
    [PASS, TIMEOUT]
