[background-color-animation-with-table3.html]
  expected:
    if (os == "linux") and not swgl and webrender and not fission and debug: [TIMEOUT, PASS]
    if (os == "win") and not fission and webrender and (processor == "x86"): [TIMEOUT, PASS]
    if (os == "win") and not fission and not webrender: [TIMEOUT, PASS]
    if os == "mac": [TIMEOUT, PASS]
    [PASS, TIMEOUT]
