[background-color-animation-non-zero-size-element-change-to-zero.html]
  expected:
    if (os == "linux") and swgl and not fission and debug: [TIMEOUT, PASS]
    if (os == "android") and debug and swgl: [TIMEOUT, PASS]
    if (os == "win") and debug and (processor == "x86"): [TIMEOUT, PASS]
    if (os == "android") and not debug: [TIMEOUT, PASS]
    if os == "mac": [TIMEOUT, PASS]
    [PASS, TIMEOUT]
