[one-element-transition-with-delay.html]
  expected:
    if debug and (os == "linux") and webrender and not fission and not swgl: ["PASS", "FAIL"]
    if not debug and webrender and (os == "android"): ["PASS", "FAIL"]
    if not debug and not webrender and (os == "win"): ["PASS", "FAIL"]
    if not debug and not webrender and (os == "android"): ["PASS", "FAIL"]
    if not debug and not webrender and (os == "linux"): ["PASS", "FAIL"]
