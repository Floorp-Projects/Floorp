[background-color-animation-with-images.html]
  expected:
    if (os == "win") and not debug and not webrender and (processor == "x86_64"): TIMEOUT
    if (os == "linux") and not swgl and debug and webrender: ["PASS", "TIMEOUT"]
    if (os == "linux") and not swgl and not debug and not webrender: ["PASS", "TIMEOUT"]
    if (os == "android") and webrender: TIMEOUT
    if (os == "linux") and swgl: TIMEOUT
    [TIMEOUT, PASS]
