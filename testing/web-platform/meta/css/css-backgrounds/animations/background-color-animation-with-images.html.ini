[background-color-animation-with-images.html]
  expected:
    if (os == "win") and debug and not swgl and not webrender and (processor == "x86"): ["PASS", "TIMEOUT"]
    if (os == "win") and not debug and not fission and not webrender and (processor == "x86"): ["PASS", "TIMEOUT"]
    if (os == "linux") and not fission and not debug and webrender: ["PASS", "TIMEOUT"]
    if (os == "linux") and not fission and not debug and not webrender: ["PASS", "FAIL", "TIMEOUT"]
    if (os == "win") and debug and not swgl and webrender: ["PASS", "TIMEOUT"]
    if (os == "win") and not debug and not fission and webrender: ["PASS", "TIMEOUT"]
    if (os == "linux") and not fission and debug: ["PASS", "TIMEOUT"]
    if (os == "linux") and fission and debug: ["PASS", "FAIL", "TIMEOUT"]
    if (os == "android") and not webrender and debug: ["PASS", "FAIL", "TIMEOUT"]
    [TIMEOUT, PASS]
