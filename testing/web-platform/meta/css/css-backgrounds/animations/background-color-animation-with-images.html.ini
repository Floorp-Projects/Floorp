[background-color-animation-with-images.html]
  expected:
    if (os == "linux") and not fission and not debug and not webrender: ["PASS", "FAIL", "TIMEOUT"]
    if (os == "linux") and not fission and not debug and webrender: ["PASS", "TIMEOUT"]
    if (os == "win") and webrender and not debug and not fission: ["PASS", "TIMEOUT"]
    if (os == "win") and webrender and not debug and fission: ["PASS", "TIMEOUT"]
    if (os == "win") and webrender and debug and fission: ["PASS", "TIMEOUT"]
    if (os == "android") and not webrender and debug: ["PASS", "TIMEOUT"]
    if (os == "android") and not webrender and not debug: ["PASS", "TIMEOUT"]
    if (os == "linux") and fission and debug: ["PASS", "FAIL", "TIMEOUT"]
    if (os == "linux") and not fission and debug: ["PASS", "TIMEOUT"]
    if (os == "win") and not webrender and (processor == "x86"): ["PASS", "TIMEOUT"]
    [TIMEOUT, PASS]
