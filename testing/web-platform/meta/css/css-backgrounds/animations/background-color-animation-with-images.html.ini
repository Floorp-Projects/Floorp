[background-color-animation-with-images.html]
  expected:
    if (os == "win") and (processor == "x86_64") and not swgl and webrender and not debug and not fission: ["PASS", "TIMEOUT"]
    if (os == "win") and (processor == "x86_64") and not swgl and webrender and debug: ["PASS", "TIMEOUT"]
    if (os == "linux") and not fission and not debug and webrender: ["PASS", "TIMEOUT"]
    if (os == "linux") and not fission and not debug and not webrender: ["PASS", "FAIL", "TIMEOUT"]
    if (os == "android") and debug and webrender and swgl: ["PASS", "TIMEOUT"]
    if (os == "win") and (processor == "x86_64") and swgl: ["PASS", "TIMEOUT"]
    if (os == "win") and (processor == "x86") and debug: ["PASS", "TIMEOUT"]
    if (os == "linux") and fission and debug: ["TIMEOUT", "PASS", "FAIL"]
    if (os == "linux") and not fission and debug: ["PASS", "TIMEOUT"]
    if (os == "android") and not debug and not webrender: ["PASS", "TIMEOUT"]
    if (os == "android") and debug and not webrender: ["PASS", "FAIL", "TIMEOUT"]
    [TIMEOUT, PASS]
