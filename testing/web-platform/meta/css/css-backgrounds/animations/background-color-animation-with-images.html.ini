[background-color-animation-with-images.html]
  expected:
    if (os == "win") and not debug and not fission and (processor == "x86_64") and webrender: ["PASS", "TIMEOUT"]
    if (os == "win") and not debug and not fission and (processor == "x86"): ["PASS", "TIMEOUT"]
    if (os == "win") and debug and webrender and swgl: ["PASS", "TIMEOUT"]
    if (os == "win") and debug and webrender and not swgl: ["PASS", "TIMEOUT"]
    if (os == "linux") and fission and not debug and not webrender: ["PASS", "TIMEOUT"]
    if (os == "linux") and not fission and not debug and webrender: ["PASS", "TIMEOUT"]
    if (os == "linux") and not fission and not debug and not webrender: ["PASS", "FAIL", "TIMEOUT"]
    if (os == "win") and not debug and fission: ["PASS", "TIMEOUT"]
    if (os == "android") and debug and not webrender: ["PASS", "FAIL", "TIMEOUT"]
    if (os == "linux") and fission and debug: ["TIMEOUT", "PASS", "FAIL"]
    if (os == "linux") and not fission and debug: ["PASS", "TIMEOUT"]
    [TIMEOUT, PASS]
