[CSSTransition-effect.tentative.html]
  expected:
    if (os == "android") and fission: [OK, TIMEOUT]
  [After setting a transition's effect to null, a new transition can be started]
    expected:
      if (os == "android") and debug and not fission and swgl: FAIL
      if (os == "android") and debug and fission: FAIL
      if (os == "android") and not debug: FAIL
      if os == "win": FAIL
      [FAIL, PASS]

  [After setting a transition's effect to null, it should be possible to interrupt that transition]
    expected:
      if fission and (os == "win") and (processor == "x86"): [PASS, FAIL]
      if not fission and debug and (os == "android"): [PASS, FAIL]
      if fission and (os == "linux"): [PASS, FAIL]
      if fission and (os == "android"): [PASS, FAIL]
