[CSSTransition-effect.tentative.html]
  [After setting a transition's effect to null, a new transition can be started]
    expected:
      if (os == "linux") and fission and webrender and debug: [PASS, FAIL]
      if (os == "linux") and fission and not webrender: [PASS, FAIL]
      if os == "android": FAIL
      if os == "win": FAIL
      [FAIL, PASS]

  [After setting a transition's effect to null, it should be possible to interrupt that transition]
    expected:
      if not fission and (os == "android") and debug and webrender and not swgl: [PASS, FAIL]
      if not fission and (os == "linux") and not webrender and not debug: [PASS, FAIL]
      if not fission and (os == "win") and (processor == "x86"): [PASS, FAIL]
