[CSSTransition-effect.tentative.html]
  [After setting a transition's effect to null, a new transition can be started]
    expected:
      if (os == "linux") and webrender and not swgl and debug and not fission: ["FAIL", "PASS"]
      if (os == "linux") and webrender and not swgl and debug and fission: ["PASS", "FAIL"]
      if (os == "linux") and webrender and not swgl and not debug and not fission: ["PASS", "FAIL"]
      if (os == "linux") and webrender and not swgl and not debug and fission: ["FAIL", "PASS"]
      if (os == "linux") and webrender and swgl: ["FAIL", "PASS"]
      if (os == "linux") and not webrender: ["FAIL", "PASS"]
      if os == "mac": ["FAIL", "PASS"]
      FAIL

