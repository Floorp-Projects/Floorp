[getclientrects-000.html]
  [horizontal-tb rtl]
    expected:
      if os == "mac" and not debug: ["FAIL", "PASS"]

  [vertical-lr rtl]
    expected:
      if os == "mac" and not debug: ["FAIL", "PASS"]

  [vertical-rl rtl]
    expected:
      if os == "mac" and not debug: ["FAIL", "PASS"]
