[getclientrects-001.html]
  [horizontal-tb rtl]
    expected:
      if (os == "mac") and not debug: ["PASS", "FAIL"]

  [vertical-lr rtl]
    expected:
      if (os == "mac") and not debug: ["PASS", "FAIL"]

  [vertical-rl rtl]
    expected:
      if (os == "mac") and not debug: ["PASS", "FAIL"]

