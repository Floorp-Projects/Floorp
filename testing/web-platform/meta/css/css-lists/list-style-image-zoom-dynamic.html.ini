[list-style-image-zoom-dynamic.html]
  expected:
    if not debug and (os == "win") and not webrender: ["PASS", "FAIL"]
    if not debug and (os == "mac") and not webrender: ["PASS", "FAIL"]
    [FAIL, PASS]
