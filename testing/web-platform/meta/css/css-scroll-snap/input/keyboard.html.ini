[keyboard.html]
  expected:
    if (os == "win") and not debug and (processor == "x86"): [TIMEOUT, OK, PASS]
    if (os == "win") and debug: [TIMEOUT, OK, PASS]
    if (os == "linux") and not debug: [TIMEOUT, OK, PASS]
    if (os == "mac") and not debug: [TIMEOUT, OK, PASS]
    [TIMEOUT, PASS]
  [If there is no valid snap offset on the arrow key's direction other than the current offset, and the scroll-snap-type is proximity, go to the original intended offset]
    expected:
      if (os == "linux") and not debug and fission and (processor == "x86_64"): [NOTRUN, TIMEOUT, FAIL]
      if (os == "android") and not swgl and not debug: [NOTRUN, TIMEOUT, FAIL]
      if (os == "android") and not swgl and debug: [TIMEOUT, FAIL, NOTRUN]
      [NOTRUN, FAIL, TIMEOUT]

  [Snaps to top-right after pressing ArrowRight]
    expected:
      if (os == "linux") and not debug and fission and (processor == "x86"): [PASS, FAIL]
      if (os == "linux") and not debug and fission and (processor == "x86_64"): [PASS, FAIL]
      if (os == "linux") and not debug and not fission: [PASS, FAIL]
      if os == "mac": [PASS, FAIL, NOTRUN]

  [Snaps to bottom-left after pressing ArrowDown]
    expected:
      if (os == "linux") and not swgl and not debug and fission and (processor == "x86"): [PASS, FAIL]
      if (os == "linux") and not swgl and not debug and fission and (processor == "x86_64"): [PASS, FAIL]
      if (os == "linux") and not swgl and not debug and not fission: [PASS, FAIL]
      if (os == "linux") and not swgl and debug: [PASS, FAIL]
      if (os == "mac") and not debug: [PASS, FAIL]

  [If the original intended offset is valid as making a snap area cover the snapport, but there's a defined snap offset in between, use the defined snap offset.]
    expected:
      if (os == "android") and not swgl and debug: [PASS, TIMEOUT]
      [TIMEOUT, PASS]

  [If there is no valid snap offset on the arrow key's direction other than the current offset, and the scroll-snap-type is mandatory, stay at the current offset.]
    expected:
      if (os == "linux") and not debug and fission and (processor == "x86_64"): [NOTRUN, TIMEOUT, PASS]
      if (os == "android") and debug and not swgl: [PASS, NOTRUN]
      if (os == "android") and not debug: [NOTRUN, TIMEOUT, PASS]
      [NOTRUN, PASS]

  [Snaps to top-left after pressing ArrowUp]
    expected:
      if (os == "linux") and debug and fission and swgl: PASS
      if (os == "linux") and debug and not fission: PASS
      if (os == "android") and debug and swgl: PASS
      if os == "win": PASS
      if os == "mac": PASS
      [PASS, FAIL]
