[svg-mutation-object-size-changed.html]
  expected:
    if not debug and (os == "win") and not webrender and (processor == "x86_64"): ["FAIL", "PASS"]
    if debug and (os == "win") and not webrender and (processor == "x86_64"): ["PASS", "FAIL"]
    if not debug and (os == "android") and webrender: ["PASS", "FAIL"]
    if not debug and (os == "win") and webrender: ["PASS", "FAIL"]
    if not debug and (os == "linux") and not webrender: ["PASS", "FAIL"]
    if debug and (os == "linux") and not webrender: ["PASS", "FAIL"]
    if debug and (os == "mac") and webrender: ["PASS", "FAIL"]
