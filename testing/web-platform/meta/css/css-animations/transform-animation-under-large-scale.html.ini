[transform-animation-under-large-scale.html]
  expected:
    if (os == "win") and (processor == "x86_64") and debug and webrender and not swgl: ["PASS", "TIMEOUT"]
    if (os == "mac") and debug and webrender: ["PASS", "TIMEOUT"]
    if (os == "android") and not debug and not webrender: ["PASS", "TIMEOUT"]
    if (os == "win") and (processor == "x86_64") and not debug: ["PASS", "TIMEOUT"]
    if (os == "android") and debug: ["PASS", "TIMEOUT"]
    [TIMEOUT, PASS]
