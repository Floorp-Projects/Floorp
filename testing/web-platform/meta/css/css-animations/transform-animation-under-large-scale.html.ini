[transform-animation-under-large-scale.html]
  expected:
    if debug and (os == "linux") and not fission and webrender and not swgl: ["PASS", "TIMEOUT"]
    if debug and (os == "win") and webrender and not swgl: ["PASS", "TIMEOUT"]
    if debug and (os == "win") and webrender and swgl: ["PASS", "TIMEOUT"]
    if debug and (os == "android") and webrender: ["PASS", "TIMEOUT"]
    if debug and (os == "linux") and fission: ["PASS", "TIMEOUT"]
    [TIMEOUT, PASS]
