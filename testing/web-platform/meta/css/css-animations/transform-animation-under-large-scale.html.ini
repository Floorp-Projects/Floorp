[transform-animation-under-large-scale.html]
  expected:
    if webrender and (os == "android") and debug: ["PASS", "TIMEOUT"]
    if webrender and (os == "win") and not debug: ["PASS", "TIMEOUT"]
    if not webrender and (os == "android") and not debug: ["PASS", "TIMEOUT"]
    if not webrender and (os == "linux"): TIMEOUT
    if not webrender and (os == "win"): TIMEOUT
    if not webrender and (os == "mac"): TIMEOUT
    [TIMEOUT, PASS]
