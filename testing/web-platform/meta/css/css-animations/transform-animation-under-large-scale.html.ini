[transform-animation-under-large-scale.html]
  expected:
    if (os == "win") and not fission and (processor == "x86_64") and debug and swgl: ["PASS", "TIMEOUT"]
    if (os == "linux") and not swgl and debug and webrender and fission: ["PASS", "TIMEOUT"]
    if (os == "linux") and not swgl and debug and webrender and not fission: ["PASS", "TIMEOUT"]
    if (os == "win") and not fission and (processor == "x86_64") and not debug: ["PASS", "TIMEOUT"]
    if (os == "android") and debug and not webrender: ["PASS", "TIMEOUT"]
    if (os == "android") and not debug and webrender: ["PASS", "TIMEOUT"]
    [TIMEOUT, PASS]
