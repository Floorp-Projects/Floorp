[transform-animation-under-large-scale.html]
  expected:
    if debug and (os == "win") and (processor == "x86_64") and webrender and not swgl: ["PASS", "TIMEOUT"]
    if debug and (os == "linux") and webrender and not fission and swgl: ["PASS", "TIMEOUT"]
    if debug and (os == "linux") and webrender and fission: ["PASS", "TIMEOUT"]
    if not debug and (os == "win") and webrender and not fission: ["PASS", "TIMEOUT"]
    if debug and (os == "android") and not webrender: ["PASS", "TIMEOUT"]
    if not debug and (os == "android") and not webrender: ["PASS", "TIMEOUT"]
    [TIMEOUT, PASS]
