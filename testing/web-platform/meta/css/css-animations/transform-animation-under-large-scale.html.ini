[transform-animation-under-large-scale.html]
  expected:
    if webrender and debug and (os == "linux") and not fission: ["PASS", "TIMEOUT"]
    if webrender and debug and (os == "linux") and fission: ["PASS", "TIMEOUT"]
    if webrender and not debug and (os == "android"): ["PASS", "TIMEOUT"]
    if webrender and debug and (os == "win"): ["PASS", "TIMEOUT"]
    if not webrender and (os == "linux") and debug: TIMEOUT
    if not webrender and (os == "android") and debug: ["PASS", "TIMEOUT"]
    if not webrender and (os == "win"): TIMEOUT
    if not webrender and (os == "mac"): TIMEOUT
    [TIMEOUT, PASS]
