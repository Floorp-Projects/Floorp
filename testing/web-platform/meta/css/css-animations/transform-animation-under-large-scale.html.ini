[transform-animation-under-large-scale.html]
  expected:
    if not swgl and (os == "linux") and debug and webrender and not fission: ["PASS", "TIMEOUT"]
    if not swgl and (os == "linux") and debug and webrender and fission: ["PASS", "TIMEOUT"]
    if not swgl and (os == "win") and webrender and not fission: ["PASS", "TIMEOUT"]
    if not swgl and (os == "android") and not webrender and debug: ["PASS", "TIMEOUT"]
    if not swgl and (os == "android") and webrender: ["PASS", "TIMEOUT"]
    [TIMEOUT, PASS]
