[transform-animation-under-large-scale.html]
  expected:
    if (os == "linux") and webrender and debug and fission: ["PASS", "TIMEOUT"]
    if (os == "mac") and webrender and debug: ["PASS", "TIMEOUT"]
    if (os == "mac") and webrender and not debug: ["PASS", "TIMEOUT"]
    if (os == "win") and webrender and not debug: ["PASS", "TIMEOUT"]
    if (os == "linux") and webrender and not debug: ["PASS", "TIMEOUT"]
    if (os == "android") and debug: ["PASS", "TIMEOUT"]
    [TIMEOUT, PASS]
