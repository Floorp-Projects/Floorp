[transform-animation-under-large-scale.html]
  expected:
    if (os == "linux") and not swgl and webrender and not debug: ["PASS", "TIMEOUT"]
    if (os == "win") and webrender and debug and not swgl: ["PASS", "TIMEOUT"]
    if (os == "android") and debug: ["PASS", "TIMEOUT"]
    [TIMEOUT, PASS]
