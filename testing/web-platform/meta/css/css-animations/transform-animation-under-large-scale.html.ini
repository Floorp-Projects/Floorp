[transform-animation-under-large-scale.html]
  expected:
    if not webrender and (os == "win") and debug and (processor == "x86_64"): ["PASS", "TIMEOUT"]
    if webrender and (os == "linux") and debug: ["PASS", "TIMEOUT"]
    if webrender and (os == "mac") and debug: ["PASS", "TIMEOUT"]
    if webrender and (os == "mac") and not debug: ["PASS", "TIMEOUT"]
    if webrender and (os == "win") and debug: ["PASS", "TIMEOUT"]
    if webrender and (os == "android") and not debug: ["PASS", "TIMEOUT"]
    if webrender and (os == "android") and debug: ["PASS", "TIMEOUT"]
    [TIMEOUT, PASS]
