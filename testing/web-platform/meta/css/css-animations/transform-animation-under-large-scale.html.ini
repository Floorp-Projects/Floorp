[transform-animation-under-large-scale.html]
  expected:
    if (os == "win") and webrender and not fission and debug and not swgl: ["PASS", "TIMEOUT"]
    if (os == "win") and webrender and not fission and debug and swgl: ["PASS", "TIMEOUT"]
    if (os == "linux") and webrender and debug and not fission and not swgl: ["PASS", "TIMEOUT"]
    if (os == "win") and webrender and not fission and not debug: ["PASS", "TIMEOUT"]
    if (os == "linux") and webrender and not debug and not fission: ["PASS", "TIMEOUT"]
    if (os == "android") and debug and webrender: ["PASS", "TIMEOUT"]
    if (os == "android") and not debug and not webrender: ["PASS", "TIMEOUT"]
    [TIMEOUT, PASS]
