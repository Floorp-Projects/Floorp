[observe.html]
  expected:
    if not fission and webrender and (os == "linux") and not debug: ["TIMEOUT", "OK"]
    if not fission and webrender and (os == "linux") and debug: ["OK", "TIMEOUT"]
    if fission and (os == "linux") and debug: ["OK", "TIMEOUT"]
    if fission and (os == "linux") and not debug: ["TIMEOUT", "OK"]
    if fission and (os == "win"): ["OK", "TIMEOUT"]
  [guard]
    expected:
      if not fission and webrender and (os == "linux") and not debug: ["NOTRUN", "PASS"]
      if not fission and webrender and (os == "linux") and debug: ["PASS", "NOTRUN"]
      if fission and (os == "linux") and not debug: ["NOTRUN", "PASS"]
      if fission and (os == "linux") and debug: ["PASS", "NOTRUN"]
      if fission and (os == "win"): ["PASS", "NOTRUN"]

  [test6: iframe notifications]
    expected:
      if not fission and webrender and (os == "linux") and not debug: ["FAIL", "PASS"]
      if not fission and webrender and (os == "linux") and debug: ["PASS", "FAIL"]
      if fission and (os == "linux") and debug: ["PASS", "FAIL"]
      if fission and (os == "linux") and not debug: ["FAIL", "PASS"]
      if fission and (os == "win"): ["PASS", "FAIL"]

