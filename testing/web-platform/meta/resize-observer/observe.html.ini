[observe.html]
  expected:
    if webrender and (os == "linux") and not debug and fission: [TIMEOUT, OK]
    if webrender and (os == "linux") and not debug and not fission: [TIMEOUT, OK]
    if webrender and (os == "win") and fission and not debug: [TIMEOUT, OK]
    if webrender and (os == "linux") and debug: [TIMEOUT, OK]
    TIMEOUT
  [guard]
    expected:
      if webrender and (os == "linux") and not debug and fission: [NOTRUN, PASS]
      if webrender and (os == "linux") and not debug and not fission: [NOTRUN, PASS]
      if webrender and (os == "win") and fission and not debug: [NOTRUN, PASS]
      if webrender and (os == "linux") and debug: [NOTRUN, PASS]
      NOTRUN

  [test6: iframe notifications]
    expected:
      if not fission and webrender and (os == "linux") and not debug: [FAIL, PASS]
      if not fission and webrender and (os == "linux") and debug: [PASS, FAIL]
      if fission and (os == "linux") and debug: [PASS, FAIL]
      if fission and (os == "linux") and not debug: [FAIL, PASS]
      if fission and (os == "win"): [PASS, FAIL]

  [test18: an observation is fired when device-pixel-content-box is being observed]
    expected: FAIL

