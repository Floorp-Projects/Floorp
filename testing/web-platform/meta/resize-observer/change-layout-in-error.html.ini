[change-layout-in-error.html]
  [Changing layout in window error handler should not result in lifecyle loop when resize observer loop limit is reached.]
    expected:
      if not debug and (processor == "x86_64") and (os == "win") and webrender and fission: ["PASS", "FAIL"]
      if not debug and (processor == "x86_64") and (os == "linux") and not fission and not webrender: ["PASS", "FAIL"]
      if not debug and (processor == "x86_64") and (os == "win") and not webrender: ["PASS", "FAIL"]
      if not debug and (processor == "x86_64") and (os == "linux") and fission: ["PASS", "FAIL"]
      if not debug and (processor == "x86_64") and (os == "android"): ["PASS", "FAIL"]
      if not debug and (processor == "x86_64") and (os == "mac"): ["PASS", "FAIL"]
      [FAIL, PASS]

