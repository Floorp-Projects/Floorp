[compression-large-flush-output.any.serviceworker.html]
  expected:
    if debug and (os == "linux") and not fission and not swgl: [ERROR, CRASH]
    if debug and (os == "linux") and not fission and swgl: [ERROR, CRASH]
    if debug and (os == "win") and (processor == "x86_64") and not swgl: [ERROR, CRASH]
    if debug and (os == "win") and (processor == "x86_64") and swgl: [CRASH, ERROR]
    if debug and (os == "android") and not swgl: [CRASH, ERROR]
    if debug and (os == "android") and swgl: [ERROR, CRASH]
    if debug and (os == "linux") and fission: ERROR
    if debug and (os == "mac"): ERROR
    if debug and (os == "win") and (processor == "x86"): [ERROR, CRASH]

[compression-large-flush-output.any.html]
  expected:
    if not debug: OK
    CRASH

[compression-large-flush-output.any.sharedworker.html]
  expected:
    if not debug: OK
    CRASH

[compression-large-flush-output.any.worker.html]
  expected:
    if not debug: OK
    CRASH
