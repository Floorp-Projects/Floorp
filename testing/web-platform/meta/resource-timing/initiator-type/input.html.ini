[input.html]
  [The initiator type for <input type=image> must be 'input']
    expected:
      if os == "linux": [PASS, FAIL]
      if os == "win": [PASS, FAIL]
      if os == "android": PASS
      FAIL
