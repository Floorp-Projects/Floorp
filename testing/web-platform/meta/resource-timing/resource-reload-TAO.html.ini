[resource-reload-TAO.html]
  expected:
    if not fission and (os == "android") and debug: [OK, ERROR]
    if not fission and (os == "linux") and swgl: [ERROR, OK]
    if not fission and (os == "linux") and not swgl: [OK, ERROR]
