[object-not-found-adds-entry.html]
  [Verify that a 200 null-typed object emits an entry.]
    expected:
      if (os == "linux") and not debug and not fission: [PASS, FAIL]
      if (os == "linux") and not debug and fission: [PASS, FAIL]
      if (os == "linux") and debug: [PASS, FAIL]

  [Verify that a 404 null-typed object emits an entry.]
    expected:
      if (os == "linux") and domstreams and not swgl and not fission and debug: [FAIL, PASS]
      if (os == "linux") and domstreams and not swgl and not fission and not debug: [PASS, FAIL]
      if (os == "linux") and domstreams and not swgl and fission and debug: [PASS, FAIL]
      if (os == "linux") and domstreams and not swgl and fission and not debug: [PASS, FAIL]
      if (os == "linux") and domstreams and swgl: [PASS, FAIL]
      if (os == "linux") and not domstreams: [FAIL, PASS]

  [Verify that a 404 img-typed object emits an entry.]
    expected:
      if (os == "linux") and not fission and not debug: [PASS, FAIL]
      if (os == "linux") and not fission and debug: [PASS, FAIL]
      if (os == "linux") and fission and not debug: [PASS, FAIL]
      if (os == "linux") and fission and debug: [PASS, FAIL]
