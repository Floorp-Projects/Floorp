[resource-reload-TAO.sub.html]
  expected:
    if (os == "win") and not debug and webrender and not fission: ["TIMEOUT", "OK"]
    if (os == "win") and not debug and webrender and fission: ["TIMEOUT", "OK"]
    if (os == "win") and not debug and not webrender: ["TIMEOUT", "OK"]
    if (os == "mac") and not debug: ["TIMEOUT", "OK"]
    TIMEOUT
  [Test that TAO headers are reused on reloads.]
    expected:
      if (os == "win") and not debug and webrender and fission: ["TIMEOUT", "PASS"]
      if (os == "win") and not debug and webrender and not fission: ["TIMEOUT", "PASS"]
      if (os == "win") and not debug and not webrender: ["TIMEOUT", "PASS"]
      if (os == "mac") and not debug: ["TIMEOUT", "PASS"]
      TIMEOUT

