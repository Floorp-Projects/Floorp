[resource-reload-TAO.sub.html]
  expected:
    if (os == "linux") and fission and not debug and webrender: ["TIMEOUT", "OK"]
    if (os == "win") and not debug: ["TIMEOUT", "OK"]
    if os == "mac": ["TIMEOUT", "OK"]
    TIMEOUT
  [Test that TAO headers are reused on reloads.]
    expected:
      if (os == "linux") and fission and not debug and webrender: ["TIMEOUT", "PASS"]
      if (os == "win") and not debug: ["TIMEOUT", "PASS"]
      if os == "mac": ["TIMEOUT", "PASS"]
      TIMEOUT

