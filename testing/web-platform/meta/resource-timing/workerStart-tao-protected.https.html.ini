[workerStart-tao-protected.https.html]
  expected:
    if not debug and (os == "win") and not webrender and (processor == "x86_64"): OK
    if debug and not fission and (os == "android") and webrender: OK
    if debug and not fission and (os == "android") and not webrender: ["TIMEOUT", "OK"]
    if debug and not fission and (os == "mac"): OK
    if debug and not fission and (os == "win"): OK
    if debug and not fission and (os == "linux"): OK
    [OK, TIMEOUT]
  [Add TAO iframe to remote origin. Make sure workerStart is not zero]
    expected:
      if debug and not fission and (os == "android") and webrender: FAIL
      if debug and not fission and (os == "android") and not webrender: ["NOTRUN", "FAIL"]
      if not debug and not webrender and (os == "win") and (processor == "x86_64"): FAIL
      if not debug and webrender and (os == "linux") and not fission: ["FAIL", "TIMEOUT", "NOTRUN"]
      if not debug and webrender and (os == "win") and not fission: ["FAIL", "TIMEOUT", "NOTRUN"]
      if debug and not fission and (os == "mac"): FAIL
      if debug and not fission and (os == "linux"): FAIL
      if debug and not fission and (os == "win"): FAIL
      if not debug and webrender and (os == "android"): ["FAIL", "TIMEOUT", "NOTRUN"]
      [FAIL, TIMEOUT]

  [Add TAO-less iframe to remote origin. Make sure workerStart is zero]
    expected:
      if webrender and not debug and (os == "win") and not fission: ["PASS", "TIMEOUT"]
      if webrender and not debug and (os == "linux") and not fission: ["PASS", "TIMEOUT"]
      if webrender and not debug and (os == "android"): ["PASS", "TIMEOUT"]
      if not webrender and (os == "android") and debug: ["TIMEOUT", "PASS"]

