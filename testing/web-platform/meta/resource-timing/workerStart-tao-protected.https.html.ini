[workerStart-tao-protected.https.html]
  expected:
    if (os == "win") and not fission and (processor == "x86_64") and not debug and not webrender: OK
    if (os == "linux") and webrender and debug and not fission: OK
    if (os == "android") and debug and webrender: OK
    if (os == "linux") and not webrender and debug: OK
    if (os == "mac") and debug: OK
    if (os == "win") and fission: ["TIMEOUT", "OK"]
    [OK, TIMEOUT]
  [Add TAO iframe to remote origin. Make sure workerStart is not zero]
    expected:
      if (os == "win") and not debug and webrender and fission: ["TIMEOUT", "FAIL"]
      if (os == "win") and not debug and webrender and not fission: ["FAIL", "TIMEOUT", "NOTRUN"]
      if (os == "win") and not debug and not webrender and (processor == "x86_64"): FAIL
      if (os == "linux") and webrender and not fission and not debug: ["FAIL", "NOTRUN", "TIMEOUT"]
      if (os == "linux") and webrender and not fission and debug: FAIL
      if (os == "android") and debug and webrender: FAIL
      if (os == "android") and debug and not webrender: ["FAIL", "NOTRUN"]
      if (os == "android") and not debug and webrender: ["FAIL", "TIMEOUT", "NOTRUN"]
      if (os == "win") and debug and (processor == "x86"): ["FAIL", "NOTRUN", "TIMEOUT"]
      if (os == "linux") and not webrender and debug: FAIL
      if (os == "mac") and debug: FAIL
      [FAIL, TIMEOUT]

  [Add TAO-less iframe to remote origin. Make sure workerStart is zero]
    expected:
      if (os == "win") and not webrender and debug and (processor == "x86"): ["PASS", "TIMEOUT"]
      if (os == "linux") and webrender and not debug and not fission: ["PASS", "TIMEOUT"]
      if (os == "android") and debug and not webrender: ["PASS", "TIMEOUT"]
      if (os == "android") and not debug and webrender: ["PASS", "TIMEOUT"]
      if (os == "win") and webrender and not fission: ["PASS", "TIMEOUT"]

