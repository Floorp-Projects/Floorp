[idb-partitioned-basic.tentative.sub.html]
  expected:
    if swgl and (os == "win") and not fission: [OK, TIMEOUT]
  [Simple test for partitioned IndexedDB]
    expected:
      if fission and debug and (os == "linux") and not swgl and domstreams: [PASS, FAIL]
      if fission and debug and (os == "win") and (processor == "x86_64") and not swgl: [PASS, FAIL]
      if fission and not debug and (processor == "x86_64") and (os == "win"): [FAIL, PASS]
      if fission and not debug and (processor == "x86_64") and (os == "mac"): [FAIL, PASS]
      if fission and not debug and (processor == "x86_64") and (os == "linux"): [FAIL, PASS]
      if fission and debug and (os == "mac"): [PASS, FAIL]
      if not fission and (os == "win"): [FAIL, TIMEOUT]
      FAIL
