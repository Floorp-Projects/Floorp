[element-request-fullscreen-cross-origin.sub.html]
  [Element#requestFullscreen() works properly with a tree of cross-origin iframes]
    expected:
      if not swgl and (os == "linux") and not fission and debug: [FAIL, PASS]
      if not swgl and (os == "linux") and not fission and not debug: [FAIL, PASS]
      if not swgl and (os == "win") and not debug: [FAIL, PASS]
      [PASS, FAIL]
