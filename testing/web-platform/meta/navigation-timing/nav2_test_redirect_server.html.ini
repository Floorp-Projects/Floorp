[nav2_test_redirect_server.html]
  disabled:
    if os == "android": bug 1550895 (frequently fails on geckoview)
  [Navigation Timing 2 WPT]
    expected:
      if not fission and (os == "mac") and not debug: [PASS, FAIL]
      if not fission and (os == "win") and not debug: [PASS, FAIL]
      if fission and not debug and webrender: [PASS, FAIL]
