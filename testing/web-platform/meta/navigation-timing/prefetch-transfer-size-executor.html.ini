[prefetch-transfer-size-executor.html]
  expected:
    if (os == "linux") and not fission and not swgl: [TIMEOUT, OK]
    if (os == "linux") and not fission and swgl: [OK, TIMEOUT]
    if os == "android": TIMEOUT
  [Navigation timing transfer size for a prefetched navigation should be 0.]
    expected:
      if (os == "linux") and not fission and not swgl: [TIMEOUT, PASS]
      if (os == "linux") and not fission and swgl: [PASS, TIMEOUT]
      if os == "android": TIMEOUT
