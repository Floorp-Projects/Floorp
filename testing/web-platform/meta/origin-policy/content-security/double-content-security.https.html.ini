[double-content-security.https.html]
  expected:
    if (os == "linux") and not debug and webrender and fission: OK
    if (os == "linux") and not debug and webrender and not fission: TIMEOUT
    if (os == "linux") and not debug and not webrender: OK
    if (os == "linux") and debug and webrender: OK
    if (os == "mac") and debug: ["TIMEOUT", "OK"]
    [OK, TIMEOUT]
  [img loading must be disallowed]
    expected: FAIL

