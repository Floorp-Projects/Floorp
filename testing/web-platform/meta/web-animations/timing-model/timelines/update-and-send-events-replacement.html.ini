[update-and-send-events-replacement.html]
  expected:
    if (os == "linux") and webrender and debug and fission: ["OK", "TIMEOUT"]
    if (os == "linux") and not webrender and not debug and (processor == "x86"): ["OK", "TIMEOUT"]
    if (os == "linux") and not webrender and not debug and (processor == "x86_64"): ["OK", "TIMEOUT"]
    if (os == "win") and not debug and not webrender and (processor == "x86_64"): ["OK", "TIMEOUT"]
    if (os == "linux") and not webrender and debug: OK
    if (os == "win") and debug and not webrender: ["OK", "TIMEOUT"]
    [TIMEOUT, OK]
  [Performs removal in deeply nested iframes]
    expected:
      if (os == "linux") and webrender and debug and fission: ["FAIL", "TIMEOUT"]
      if (os == "linux") and not webrender and not debug and (processor == "x86"): ["FAIL", "TIMEOUT"]
      if (os == "linux") and not webrender and not debug and (processor == "x86_64"): ["FAIL", "TIMEOUT"]
      if (os == "win") and not debug and webrender and fission: ["TIMEOUT", "PASS", "FAIL"]
      if (os == "win") and not debug and not webrender and (processor == "x86_64"): ["FAIL", "TIMEOUT"]
      if (os == "linux") and not webrender and debug: FAIL
      if (os == "win") and debug and not webrender: ["FAIL", "TIMEOUT"]
      if (os == "android") and debug: ["TIMEOUT", "PASS", "FAIL"]
      [TIMEOUT, FAIL]

  [Removes an animation after updating another animation's timeline]
    expected:
      if not nightly_build: FAIL

  [Removes an animation after updating its timeline]
    expected:
      if not nightly_build: FAIL

  [Does NOT remove an animation after making a redundant change to another animation's timeline]
    expected:
      if not nightly_build: FAIL

  [Does NOT remove an animation after making a redundant change to its timeline]
    expected:
      if not nightly_build: FAIL

  [Updates ALL timelines before checking for replacement]
    expected:
      if not nightly_build: FAIL

