[update-and-send-events-replacement.html]
  expected:
    if not debug and webrender and fission and (os == "linux"): ["TIMEOUT", "OK"]
    if not debug and webrender and fission and (os == "win"): TIMEOUT
    if not debug and webrender and not fission and (os == "linux"): ["TIMEOUT", "OK"]
    if not debug and not webrender and (os == "win") and (processor == "x86_64"): ["TIMEOUT", "OK"]
    if not debug and not webrender and (os == "android"): ["TIMEOUT", "OK"]
    if debug and (os == "linux") and not webrender: OK
    if debug and (os == "win") and webrender: ["TIMEOUT", "OK"]
    if debug and (os == "mac"): ["TIMEOUT", "OK"]
    [OK, TIMEOUT]
  [Performs removal in deeply nested iframes]
    expected:
      if not debug and not webrender and (os == "win") and (processor == "x86_64"): ["TIMEOUT", "FAIL"]
      if not debug and webrender and not fission and (os == "linux"): ["TIMEOUT", "FAIL"]
      if not debug and webrender and fission and (os == "linux"): ["TIMEOUT", "FAIL"]
      if not debug and webrender and fission and (os == "win"): TIMEOUT
      if not debug and not webrender and (os == "android"): ["TIMEOUT", "FAIL"]
      if debug and (os == "win") and webrender: ["TIMEOUT", "FAIL"]
      if debug and (os == "linux") and not webrender: FAIL
      if debug and (os == "mac"): ["TIMEOUT", "FAIL"]
      if debug and (os == "android"): ["FAIL", "PASS", "TIMEOUT"]
      [FAIL, TIMEOUT]

  [Removes an animation after updating another animation's timeline]
    expected:
      if not nightly_build: FAIL

  [Removes an animation after updating its timeline]
    expected:
      if not nightly_build: FAIL

  [Does NOT remove an animation after making a redundant change to another animation's timeline]
    expected:
      if not nightly_build: FAIL

  [Does NOT remove an animation after making a redundant change to its timeline]
    expected:
      if not nightly_build: FAIL

  [Updates ALL timelines before checking for replacement]
    expected:
      if not nightly_build: FAIL

