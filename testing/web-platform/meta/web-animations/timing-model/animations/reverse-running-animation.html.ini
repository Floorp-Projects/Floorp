[reverse-running-animation.html]
  expected:
    if debug and fission and (processor == "x86_64") and (os == "linux") and not webrender: ["PASS", "FAIL"]
    if debug and fission and (processor == "x86_64") and (os == "win") and webrender: ["PASS", "FAIL"]
    if debug and not fission and (os == "win") and webrender: ["PASS", "FAIL"]
    if debug and not fission and (os == "linux") and not webrender: ["PASS", "FAIL"]
