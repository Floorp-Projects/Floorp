[reverse-running-animation.html]
  expected:
    if debug and (os == "win") and webrender and fission: ["FAIL", "PASS"]
    if debug and (os == "win") and webrender and not fission: ["PASS", "FAIL"]
    if debug and (os == "linux") and not webrender: ["PASS", "FAIL"]
    if debug and (os == "mac"): ["PASS", "FAIL"]
