[binaryType-wrong-value.any.worker.html]
  expected:
    if (os == "win") and (processor == "x86_64") and not swgl: [OK, ERROR, TIMEOUT]

[binaryType-wrong-value.any.worker.html?wss]
  expected:
    if (os == "win") and debug and (processor == "x86_64") and swgl: [OK, ERROR, TIMEOUT]
    if (os == "win") and debug and (processor == "x86"): [OK, ERROR, TIMEOUT]

[binaryType-wrong-value.any.html?wss]
  expected:
    if (os == "win") and debug and not swgl: [OK, ERROR, TIMEOUT]

[binaryType-wrong-value.any.worker.html?wpt_flags=h2]
  expected:
    if fission and (os == "win") and (processor == "x86_64") and not swgl and debug: [TIMEOUT, OK, ERROR]
    if fission and (os == "win") and (processor == "x86_64") and not swgl and not debug: [OK, TIMEOUT, ERROR]
    if fission and (os == "linux") and debug and swgl: [TIMEOUT, OK, ERROR]
    if fission and (os == "linux") and not debug and (processor == "x86_64"): [OK, TIMEOUT, ERROR]
    if fission and (os == "mac") and debug: [TIMEOUT, OK, ERROR]
    if not fission and not debug and (os == "android"): [OK, TIMEOUT, ERROR]
    if not fission and not debug and (os == "linux"): [OK, TIMEOUT, ERROR]
    [OK, ERROR, TIMEOUT]

[binaryType-wrong-value.any.html?wpt_flags=h2]
  expected:
    if (os == "linux") and not debug and fission and (processor == "x86_64"): [OK, TIMEOUT, ERROR]
    if (os == "linux") and debug and not fission and not swgl: [OK, ERROR, TIMEOUT]
    if (os == "linux") and debug and fission and swgl: [OK, ERROR, TIMEOUT]
    if (os == "win") and not swgl and debug and (processor == "x86_64"): [OK, ERROR, TIMEOUT]
    if (os == "win") and not swgl and not debug and (processor == "x86"): [OK, ERROR, TIMEOUT]
    if (os == "android") and not debug: [OK, TIMEOUT, ERROR]
    if (os == "mac") and debug: [OK, ERROR, TIMEOUT]
    [TIMEOUT, OK, ERROR]

[binaryType-wrong-value.any.html]
  expected:
    if (processor == "x86") and debug: [OK, ERROR, TIMEOUT]
