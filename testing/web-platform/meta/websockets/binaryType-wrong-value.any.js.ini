[binaryType-wrong-value.any.worker.html]

[binaryType-wrong-value.any.worker.html?wss]

[binaryType-wrong-value.any.html?wss]

[binaryType-wrong-value.any.worker.html?wpt_flags=h2]
  expected:
    if (os == "linux") and debug and webrender and not swgl: TIMEOUT
    if (os == "linux") and debug and webrender and swgl: ["TIMEOUT", "ERROR"]
    if (os == "linux") and not debug and not webrender and (processor == "x86_64"): ["OK", "TIMEOUT"]
    if (os == "linux") and not debug and webrender and not fission: ["TIMEOUT", "CRASH"]
    if (os == "linux") and not debug and webrender and fission: ["TIMEOUT", "OK"]
    if (os == "linux") and debug and not webrender: ["TIMEOUT", "ERROR", "CRASH"]
    if (os == "win") and (processor == "x86") and not debug: ["TIMEOUT", "OK"]
  [Create WebSocket - set binaryType to something other than blob or arraybuffer - SYNTAX_ERR is returned - Connection should be opened]
    expected:
      if (os == "linux") and not webrender and (processor == "x86_64"): ["PASS", "NOTRUN"]
      if (os == "linux") and webrender: ["NOTRUN", "PASS"]

  [Create WebSocket - set binaryType to something other than blob or arraybuffer - SYNTAX_ERR is returned - Connection should be closed]
    expected:
      if (os == "linux") and not webrender and (processor == "x86_64"): ["PASS", "FAIL"]
      if (os == "linux") and webrender: ["PASS", "FAIL", "NOTRUN"]


[binaryType-wrong-value.any.html?wpt_flags=h2]
  expected:
    if (os == "linux") and debug and webrender and swgl: ["TIMEOUT", "CRASH"]
    if (os == "linux") and debug and webrender and not swgl: TIMEOUT
    if (os == "linux") and not debug and webrender and not fission: TIMEOUT
    if (os == "linux") and not debug and webrender and fission: ["TIMEOUT", "OK"]
    if (os == "linux") and not debug and not webrender and (processor == "x86_64"): ["OK", "TIMEOUT"]
    if (os == "win") and (processor == "x86") and not debug: ["TIMEOUT", "OK"]
    if (os == "linux") and debug and not webrender: ["TIMEOUT", "OK", "CRASH"]
  [Create WebSocket - set binaryType to something other than blob or arraybuffer - SYNTAX_ERR is returned - Connection should be opened]
    expected:
      if swgl and (os == "linux"): NOTRUN

  [Create WebSocket - set binaryType to something other than blob or arraybuffer - SYNTAX_ERR is returned - Connection should be closed]
    expected:
      if swgl and (os == "linux"): FAIL


[binaryType-wrong-value.any.html]
