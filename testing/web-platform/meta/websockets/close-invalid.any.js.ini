[close-invalid.any.worker.html?wpt_flags=h2]
  expected:
    if (os == "linux") and not webrender and not debug and (processor == "x86_64") and fission: ["OK", "ERROR", "TIMEOUT"]
    if (os == "linux") and not webrender and not debug and (processor == "x86_64") and not fission: ["OK", "TIMEOUT", "ERROR"]
    if (os == "linux") and webrender and not debug and fission: ["OK", "TIMEOUT"]
    if (os == "linux") and webrender and not debug and not fission: ["OK", "ERROR", "TIMEOUT"]
    if (os == "linux") and not webrender and not debug and (processor == "x86"): ["OK", "ERROR", "TIMEOUT"]
    if (os == "win") and (processor == "x86") and not debug: ["OK", "TIMEOUT"]
    if (os == "linux") and webrender and debug: TIMEOUT
    if (os == "linux") and not webrender and debug: TIMEOUT
    if (os == "android"): ["OK", "TIMEOUT"]

[close-invalid.any.html]

[close-invalid.any.worker.html]

[close-invalid.any.html?wpt_flags=h2]
  expected:
    if (os == "linux") and not webrender and not debug and (processor == "x86_64") and fission: ["TIMEOUT", "OK", "ERROR", "CRASH"]
    if (os == "linux") and not webrender and not debug and (processor == "x86_64") and not fission: ["OK", "TIMEOUT", "ERROR", "CRASH"]
    if (os == "linux") and webrender and not debug and fission: ["TIMEOUT", "OK"]
    if (os == "linux") and webrender and not debug and not fission: ["OK", "TIMEOUT", "ERROR"]
    if (os == "linux") and not webrender and not debug and (processor == "x86"): ["OK", "TIMEOUT"]
    if (os == "linux") and webrender and debug: TIMEOUT
    if (os == "linux") and not webrender and debug: TIMEOUT
    if (os == "win") and (processor == "x86") and not debug: ["OK", "TIMEOUT"]
    if (os == "android"): ["OK", "TIMEOUT"]

[close-invalid.any.html?wss]

[close-invalid.any.worker.html?wss]
