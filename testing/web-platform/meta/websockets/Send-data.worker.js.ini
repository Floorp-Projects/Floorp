[Send-data.worker.html]

[Send-data.worker.html?wpt_flags=h2]
  expected:
    if (os == "linux") and not debug and not webrender: ["OK", "TIMEOUT"]
    if (os == "linux") and not debug and webrender: TIMEOUT
    if (os == "linux") and debug and webrender: TIMEOUT
    if (os == "linux") and debug and not webrender: ["TIMEOUT", "ERROR"]

[Send-data.worker.html?wss]
