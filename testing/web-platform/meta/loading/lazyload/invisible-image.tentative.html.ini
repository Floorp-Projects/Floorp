[invisible-image.tentative.html]
  [Test that below-viewport invisible images that are not marked loading=lazy still load, and block the window load event]
    expected:
      if debug and (os == "win") and webrender: [PASS, FAIL]
      if debug and (os == "android"): [PASS, FAIL]

