[timeouts.py]
  disabled:
    if ccov and (os == "win"): https://bugzilla.mozilla.org/show_bug.cgi?id=1495002
  expected:
    if (os == "linux") and not fission and debug and not webrender: [ERROR, OK, TIMEOUT]
    if (os == "linux") and fission and not debug: [OK, TIMEOUT]
    if (os == "linux") and webrender: [ERROR, OK, TIMEOUT]
    if (os == "linux") and not webrender: [ERROR, OK, TIMEOUT]
    ERROR
  [test_timeouts[timeouts0\]]
    expected:
      if processor == "x86": [PASS, FAIL]

