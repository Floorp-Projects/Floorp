[close.py]
  expected:
    if (os == "linux") and not webrender and debug: [ERROR, OK, TIMEOUT]
    if (os == "linux") and not webrender and not debug: [ERROR, OK, TIMEOUT]
    if (os == "linux") and webrender: [ERROR, OK, TIMEOUT]
    ERROR
