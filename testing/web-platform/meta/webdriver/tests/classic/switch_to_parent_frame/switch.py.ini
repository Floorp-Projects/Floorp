[switch.py]
  [test_no_parent_browsing_context]
    expected:
      if (os == "linux") and not asan and not debug and not tsan and not swgl: PASS
      if os == "win": PASS
      if os == "mac": PASS
      if os == "android": PASS
      [PASS, FAIL]
