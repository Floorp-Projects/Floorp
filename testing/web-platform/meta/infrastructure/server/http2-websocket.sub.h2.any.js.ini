[http2-websocket.sub.h2.any.worker.html]
  expected:
    if (os == "linux") and not webrender and not debug: ["OK", "TIMEOUT"]
  [WSS over h2]
    expected:
      if (os == "linux") and not webrender and not debug: ["PASS", "FAIL", "TIMEOUT"]


[http2-websocket.sub.h2.any.html]
  expected:
    if (os == "linux") and not webrender and not debug: ["OK", "TIMEOUT"]
  [WSS over h2]
    expected:
      if (os == "linux") and not webrender and not debug: ["PASS", "FAIL", "TIMEOUT"]

