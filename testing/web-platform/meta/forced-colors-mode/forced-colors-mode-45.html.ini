[forced-colors-mode-45.html]
  expected:
    if fission and (os == "linux") and not swgl and not debug: [FAIL, PASS]
    if fission and (os == "linux") and swgl: [FAIL, PASS]
    if fission and (os == "mac") and debug: [FAIL, PASS]
    [PASS, FAIL]
