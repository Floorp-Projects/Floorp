[query-empty.tentative.https.any.html]

[query-empty.tentative.https.any.worker.html]

[query-empty.tentative.https.any.serviceworker.html]

[query-empty.tentative.https.any.sharedworker.html]
  expected:
    if not fission and (os == "linux") and not debug and (processor == "x86"): [OK, TIMEOUT]
    if not fission and (os == "linux") and not debug and (processor == "x86_64"): [OK, TIMEOUT]
    if fission and debug and (os == "linux"): [OK, TIMEOUT]
    if fission and not debug and (os == "linux"): [OK, TIMEOUT]
    if not fission and (os == "android") and swgl: [OK, TIMEOUT]
