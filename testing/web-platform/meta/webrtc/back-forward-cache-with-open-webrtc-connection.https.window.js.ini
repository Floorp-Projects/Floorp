[back-forward-cache-with-open-webrtc-connection.https.window.html]
  [Testing BFCache support for page with open WebRTC connection.]
    expected:
      if (os == "linux") and not swgl and not debug and fission and (processor == "x86_64"): [FAIL, PASS]
      if (os == "linux") and not swgl and debug and fission and not editorLegacyDirectionMode: [PASS, FAIL]
      if (os == "linux") and not swgl and debug and fission and editorLegacyDirectionMode: FAIL
      if (os == "linux") and swgl and fission: [FAIL, PASS]
      if (os == "win") and debug: [PASS, FAIL]
      if (os == "mac") and debug: [PASS, FAIL]
