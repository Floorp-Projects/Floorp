[RTCDataChannel-iceRestart.html]
  bug: https://bugzilla.mozilla.org/show_bug.cgi?id=1728342
  expected:
    if not asan and (os == "linux") and fission and (processor == "x86_64") and not swgl and not tsan: [ERROR, OK]
    if asan and fission: [ERROR, OK]
    ERROR
