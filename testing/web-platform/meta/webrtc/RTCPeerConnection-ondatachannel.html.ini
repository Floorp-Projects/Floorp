[RTCPeerConnection-ondatachannel.html]
  bug: https://bugzilla.mozilla.org/show_bug.cgi?id=1613433
  expected: [OK, TIMEOUT]
  [In-band negotiated channel created on remote peer should match the same configuration as local peer]
    bug: https://bugzilla.mozilla.org/show_bug.cgi?id=1613433
    expected:
      if not fission and (os == "linux") and not webrender and not debug: ["PASS", "TIMEOUT", "NOTRUN"]
      if fission and (os == "linux") and not debug and webrender: ["PASS", "NOTRUN", "TIMEOUT"]
      if fission and (os == "linux") and not debug and not webrender: ["PASS", "TIMEOUT", "NOTRUN"]
      if not fission and (os == "mac") and not debug: ["PASS", "TIMEOUT", "NOTRUN"]
      if fission and (os == "linux") and debug: ["PASS", "TIMEOUT", "NOTRUN"]
      if fission and (os == "win"): ["PASS", "TIMEOUT", "NOTRUN"]
      [PASS, TIMEOUT]

  [In-band negotiated channel created on remote peer should match the same (default) configuration as local peer]
    bug: https://bugzilla.mozilla.org/show_bug.cgi?id=1613433
    expected: [PASS, NOTRUN]

  [Open event should not be raised when sending and immediately closing the channel in the datachannel event]
    bug: https://bugzilla.mozilla.org/show_bug.cgi?id=1613433
    expected:
      if not fission and (os == "linux") and not webrender and not debug: ["PASS", "NOTRUN"]
      if fission and (os == "linux") and not debug and webrender: ["PASS", "NOTRUN"]
      if fission and (os == "linux") and not debug and not webrender: ["PASS", "NOTRUN"]
      if not fission and (os == "mac") and not debug: ["PASS", "NOTRUN"]
      if fission and (os == "win"): ["PASS", "NOTRUN"]

  [Negotiated channel should not fire datachannel event on remote peer]
    bug: https://bugzilla.mozilla.org/show_bug.cgi?id=1613433
    expected: [PASS, NOTRUN]

  [Open event should be raised when closing the channel in the datachannel event after enqueuing a task]
    expected:
      if fission and (os == "linux") and not debug and not webrender: ["PASS", "NOTRUN"]
      if fission and (os == "linux") and not debug and webrender: ["PASS", "NOTRUN"]
      if not fission and (os == "linux") and not webrender and not debug: ["PASS", "NOTRUN"]
      if not fission and (os == "mac") and not debug: ["PASS", "NOTRUN"]
      if fission and (os == "win"): ["PASS", "NOTRUN"]

  [Data channel event should fire when new data channel is announced to the remote peer]
    expected:
      if fission and (os == "linux") and not debug and not webrender: ["PASS", "TIMEOUT"]
      if fission and (os == "linux") and not debug and webrender: ["PASS", "TIMEOUT"]
      if not fission and (os == "linux") and not webrender and not debug: ["PASS", "TIMEOUT"]
      if not fission and (os == "mac") and not debug: ["PASS", "TIMEOUT"]
      if fission and (os == "win"): ["PASS", "TIMEOUT"]

  [Open event should not be raised when closing the channel in the datachannel event]
    expected:
      if (os == "linux") and webrender and fission and not debug: ["PASS", "NOTRUN"]
      if (os == "linux") and not webrender and not debug: ["PASS", "NOTRUN"]
      if (os == "win") and fission: ["PASS", "NOTRUN"]
      if (os == "mac") and not debug: ["PASS", "TIMEOUT", "NOTRUN"]

  [Should be able to send data in a datachannel event handler]
    expected:
      if (os == "linux") and webrender and fission and not debug: ["PASS", "NOTRUN"]
      if (os == "linux") and not webrender and not debug: ["PASS", "NOTRUN"]
      if (os == "win") and fission: ["PASS", "NOTRUN"]
      if (os == "mac") and not debug: ["PASS", "TIMEOUT", "NOTRUN"]

