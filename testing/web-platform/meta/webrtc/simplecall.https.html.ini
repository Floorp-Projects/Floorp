[simplecall.https.html]
  expected:
    if (os == "linux") and not fission and not debug and not webrender and (processor == "x86_64"): [OK, TIMEOUT, ERROR, CRASH]
    if (os == "linux") and not fission and not debug and not webrender and (processor == "x86"): [OK, ERROR, TIMEOUT, CRASH]
    if (os == "linux") and not fission and not debug and webrender: [OK, ERROR, TIMEOUT, CRASH]
    if (os == "linux") and fission and webrender: [OK, ERROR, CRASH]
    if (os == "linux") and fission and not webrender: [OK, ERROR, TIMEOUT, CRASH]
    if (os == "mac") and not debug: [OK, TIMEOUT]
  [Can set up a basic WebRTC call.]
    expected:
      if not debug and (os == "linux") and not webrender and not fission and (processor == "x86_64"): [PASS, TIMEOUT]
      if not debug and (os == "linux") and not webrender and not fission and (processor == "x86"): [PASS, TIMEOUT]
      if not debug and (os == "linux") and webrender and not fission: [PASS, TIMEOUT]
      if not debug and (os == "linux") and not webrender and fission: [PASS, TIMEOUT]
      if not debug and (os == "mac"): [PASS, TIMEOUT]
