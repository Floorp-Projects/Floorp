[simplecall.https.html]
  expected:
    if (os == "linux"): [OK, ERROR, CRASH]
    if (os == "mac") and not debug: [TIMEOUT, OK]
  [Can set up a basic WebRTC call.]
    expected:
      if (os == "mac") and not debug: [TIMEOUT, PASS]
