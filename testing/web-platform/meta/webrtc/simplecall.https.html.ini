[simplecall.https.html]
  expected:
    if (os == "linux") and not webrender and not debug and fission: ["OK", "ERROR", "TIMEOUT", "CRASH"]
    if (os == "linux") and not webrender and not debug and not fission: ["OK", "TIMEOUT", "ERROR", "CRASH"]
    if (os == "linux") and webrender and not fission and not debug: ["OK", "ERROR", "TIMEOUT", "CRASH"]
    if (os == "linux") and webrender and fission: ["OK", "ERROR", "CRASH"]
    if (os == "mac") and not debug: ["OK", "TIMEOUT"]
  [Can set up a basic WebRTC call.]
    expected:
      if (os == "linux") and not debug and not fission and not webrender: ["PASS", "TIMEOUT"]
      if (os == "linux") and not debug and not fission and webrender: ["PASS", "TIMEOUT"]
      if (os == "linux") and not debug and fission and not webrender: ["PASS", "TIMEOUT"]
      if (os == "mac") and not debug: ["PASS", "TIMEOUT"]

