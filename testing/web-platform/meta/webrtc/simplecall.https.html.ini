[simplecall.https.html]
  expected:
    if (os == "linux") and not fission and not debug and webrender and (processor == "x86_64"): [OK, TIMEOUT, ERROR, CRASH]
    if (os == "linux") and not fission and not debug and webrender and (processor == "x86"): [OK, ERROR, TIMEOUT, CRASH]
    if (os == "linux") and not fission and not debug and not webrender: [OK, ERROR, TIMEOUT, CRASH]
    if (os == "linux") and fission and not webrender: [OK, ERROR, TIMEOUT, CRASH]
    if (os == "linux") and fission and webrender: [OK, ERROR, CRASH]
    if (os == "mac") and not debug: [OK, TIMEOUT]
  [Can set up a basic WebRTC call.]
    expected:
      if (os == "linux") and not debug and not fission and webrender and (processor == "x86_64"): [PASS, TIMEOUT]
      if (os == "linux") and not debug and not fission and webrender and (processor == "x86"): [PASS, TIMEOUT]
      if (os == "linux") and not debug and fission and not webrender: [PASS, TIMEOUT]
      if (os == "linux") and not debug and not fission and not webrender: [PASS, TIMEOUT]
      if (os == "mac") and not debug: [PASS, TIMEOUT]
