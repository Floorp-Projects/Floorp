[simplecall.https.html]
  expected:
    if (os == "linux") and not webrender and not debug and (processor == "x86"): ["OK", "ERROR", "TIMEOUT", "CRASH"]
    if (os == "linux") and not webrender and not debug and (processor == "x86_64"): ["OK", "ERROR", "TIMEOUT", "CRASH"]
    if (os == "linux") and webrender and not debug and not fission: ["OK", "TIMEOUT", "ERROR", "CRASH"]
    if (os == "linux") and webrender and not debug and fission: ["OK", "ERROR", "CRASH"]
    if (os == "linux") and webrender and debug and fission: ["OK", "ERROR", "CRASH"]
    if (os == "mac") and not debug: ["OK", "TIMEOUT"]
  [Can set up a basic WebRTC call.]
    expected:
      if (os == "linux") and webrender and not debug and not fission: ["PASS", "TIMEOUT"]
      if (os == "linux") and not webrender and not debug: ["PASS", "TIMEOUT"]
      if (os == "mac") and not debug: ["PASS", "TIMEOUT"]

