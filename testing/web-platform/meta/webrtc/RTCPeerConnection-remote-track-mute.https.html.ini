[RTCPeerConnection-remote-track-mute.https.html]
  prefs: [media.peerconnection.mute_on_bye_or_timeout:true]
  expected:
    if not fission and (os == "linux") and not debug and (processor == "x86_64"): [OK, TIMEOUT]
    if fission and (os == "linux"): [OK, TIMEOUT]
  [pc.close() on one side causes mute events on the other]
    expected:
      if not fission and (os == "linux") and not debug and (processor == "x86_64"): [PASS, TIMEOUT]
      if fission and (os == "linux"): [PASS, TIMEOUT]
