[RTCPeerConnection-addIceCandidate-connectionSetup.html]
  expected:
    if debug and (os == "linux") and fission: ["TIMEOUT", "OK"]
    [OK, TIMEOUT]
  [Candidates are added at PC2; connection should work]
    expected:
      if debug and (os == "linux") and fission: ["NOTRUN", "PASS"]
      [PASS, NOTRUN]

  [Candidates are added at PC1; connection should work]
    expected:
      if debug and (os == "linux") and fission: ["TIMEOUT", "PASS"]
      [PASS, TIMEOUT]

