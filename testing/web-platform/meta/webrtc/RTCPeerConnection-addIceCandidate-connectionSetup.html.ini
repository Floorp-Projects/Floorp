[RTCPeerConnection-addIceCandidate-connectionSetup.html]
  expected:
    if (os == "linux") and not fission and (processor == "x86"): ["TIMEOUT", "OK"]
    if (os == "linux") and not fission and (processor == "x86_64"): ["OK", "TIMEOUT"]
    if os == "android": ["OK", "TIMEOUT"]
  [Candidates are added at PC2; connection should work]
    expected:
      if (os == "linux") and not fission and (processor == "x86_64"): ["PASS", "NOTRUN"]
      if (os == "linux") and not fission and (processor == "x86"): ["NOTRUN", "PASS"]
      if os == "android": ["PASS", "NOTRUN"]

  [Candidates are added at PC1; connection should work]
    expected:
      if (os == "linux") and not fission and (processor == "x86"): ["TIMEOUT", "PASS"]
      if (os == "linux") and not fission and (processor == "x86_64"): ["PASS", "TIMEOUT"]
      if os == "android": ["PASS", "TIMEOUT"]

