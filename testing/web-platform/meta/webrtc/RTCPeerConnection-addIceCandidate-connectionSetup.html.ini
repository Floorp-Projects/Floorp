[RTCPeerConnection-addIceCandidate-connectionSetup.html]
  expected:
    if not fission and (os == "linux") and webrender and not debug: ["TIMEOUT", "OK"]
    [OK, TIMEOUT]
  [Candidates are added at PC2; connection should work]
    expected:
      if not fission and (os == "linux") and webrender and not debug: ["NOTRUN", "PASS"]
      [PASS, NOTRUN]

  [Candidates are added at PC1; connection should work]
    expected:
      if not fission and (os == "linux") and webrender and not debug: ["TIMEOUT", "PASS"]
      [PASS, TIMEOUT]

