[nestedReport.html]
  expected:
    if (os == "android") and debug and webrender: ["ERROR", "TIMEOUT"]
    if (os == "android") and debug and not webrender: ["ERROR", "TIMEOUT"]
    ERROR
  [Nested report]
    expected: TIMEOUT

