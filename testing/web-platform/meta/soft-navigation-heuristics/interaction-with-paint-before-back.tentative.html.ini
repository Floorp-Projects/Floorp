[interaction-with-paint-before-back.tentative.html]
  expected:
    if not swgl and (processor == "x86_64") and (os == "linux") and debug and fission: [TIMEOUT, ERROR]
    if not swgl and (processor == "x86_64") and (os == "linux") and debug and not fission: [TIMEOUT, ERROR]
    if not swgl and (processor == "x86_64") and (os == "win") and not debug: [TIMEOUT, ERROR]
    if not swgl and (processor == "x86_64") and (os == "android") and not debug: [TIMEOUT, ERROR]
    TIMEOUT
