[navigate-cross-document-double.html]
  expected:
    if (os == "linux") and not swgl and fission and not debug and (processor == "x86"): [TIMEOUT, OK]
    if (os == "linux") and not swgl and fission and not debug and (processor == "x86_64"): [TIMEOUT, OK]
    if (os == "win") and (processor == "x86") and not debug: [TIMEOUT, OK]
    if (os == "linux") and swgl: [TIMEOUT, OK]
    if (os == "mac") and debug: [TIMEOUT, OK]
    if (os == "android") and swgl: [TIMEOUT, OK]
    if (os == "win") and (processor == "x86_64"): [TIMEOUT, OK]
    [OK, TIMEOUT]
  [event and promise ordering when navigate() is called to a cross-document destination, interrupting another navigate() to a cross-document destination]
    expected:
      if (os == "linux") and not swgl and fission and not debug and (processor == "x86"): [TIMEOUT, FAIL]
      if (os == "linux") and not swgl and fission and not debug and (processor == "x86_64"): [TIMEOUT, FAIL]
      if (os == "win") and (processor == "x86") and not debug: [TIMEOUT, FAIL]
      if (os == "win") and (processor == "x86_64"): [TIMEOUT, FAIL]
      if (os == "mac") and debug: [TIMEOUT, FAIL]
      if (os == "android") and swgl: [TIMEOUT, FAIL]
      if (os == "linux") and swgl: [TIMEOUT, FAIL]
      [FAIL, TIMEOUT]
