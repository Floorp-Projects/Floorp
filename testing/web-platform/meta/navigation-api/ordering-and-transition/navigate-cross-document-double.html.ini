[navigate-cross-document-double.html]
  expected:
    if fission and not debug and (os == "linux") and (processor == "x86_64"): [TIMEOUT, OK]
    if fission and not debug and (os == "linux") and (processor == "x86"): [TIMEOUT, OK]
    if fission and debug and (os == "linux"): [TIMEOUT, OK]
    if fission and debug and (os == "mac"): [TIMEOUT, OK]
    if fission and not debug and (os == "mac"): [TIMEOUT, OK]
    if fission and not debug and (os == "win"): [TIMEOUT, OK]
    [OK, TIMEOUT]
  [event and promise ordering when navigate() is called to a cross-document destination, interrupting another navigate() to a cross-document destination]
    expected:
      if fission and not debug and (os == "linux") and (processor == "x86"): [TIMEOUT, FAIL]
      if fission and not debug and (os == "linux") and (processor == "x86_64"): [TIMEOUT, FAIL]
      if fission and debug and (os == "linux"): [TIMEOUT, FAIL]
      if fission and debug and (os == "mac"): [TIMEOUT, FAIL]
      if fission and not debug and (os == "win"): [TIMEOUT, FAIL]
      if fission and not debug and (os == "mac"): [TIMEOUT, FAIL]
      [FAIL, TIMEOUT]
