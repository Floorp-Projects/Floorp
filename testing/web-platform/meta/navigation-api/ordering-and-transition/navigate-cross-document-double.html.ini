[navigate-cross-document-double.html]
  expected:
    if fission and debug and (os == "win") and (processor == "x86_64") and swgl: [OK, TIMEOUT]
    if not fission and not swgl and (os == "linux") and not debug: [OK, TIMEOUT]
    if not fission and not swgl and (os == "linux") and debug: [OK, TIMEOUT]
    if not fission and not swgl and (os == "android") and not debug: [OK, TIMEOUT]
    if not fission and not swgl and (os == "android") and debug: [OK, TIMEOUT]
    if fission and debug and (os == "linux") and not swgl: [OK, TIMEOUT]
    if not fission and swgl and (os == "android"): [OK, TIMEOUT]
    [TIMEOUT, OK]
  [event and promise ordering when navigate() is called to a cross-document destination, interrupting another navigate() to a cross-document destination]
    expected:
      if fission and debug and (os == "win") and (processor == "x86_64") and swgl: [FAIL, TIMEOUT]
      if fission and debug and (os == "linux") and not swgl: [FAIL, TIMEOUT]
      if not fission and not swgl and (os == "linux") and debug: [FAIL, TIMEOUT]
      if not fission and not swgl and (os == "linux") and not debug: [FAIL, TIMEOUT]
      if not fission and not swgl and (os == "android") and debug: [FAIL, TIMEOUT]
      if not fission and not swgl and (os == "android") and not debug: [FAIL, TIMEOUT]
      if not fission and swgl and (os == "android"): [FAIL, TIMEOUT]
      [TIMEOUT, FAIL]
