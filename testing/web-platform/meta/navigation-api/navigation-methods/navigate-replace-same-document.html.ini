[navigate-replace-same-document.html]
  expected:
    if fission and debug and (processor == "x86_64") and (os == "linux") and swgl: [OK, ERROR]
    if fission and debug and (processor == "x86_64") and (os == "win") and not swgl: [OK, ERROR]
    if not fission and (os == "android") and not swgl and debug: [OK, ERROR]
    if not fission and (os == "android") and not swgl and not debug: [OK, ERROR]
    if not fission and (os == "linux") and swgl: [OK, ERROR]
    if fission and debug and (processor == "x86"): [OK, ERROR]
    [ERROR, OK]
