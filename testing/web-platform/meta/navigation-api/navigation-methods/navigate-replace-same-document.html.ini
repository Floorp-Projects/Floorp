[navigate-replace-same-document.html]
  expected:
    if fission and not swgl and (os == "win") and debug and (processor == "x86_64"): [OK, ERROR]
    if not fission and (os == "linux") and not debug: [OK, ERROR]
    if not fission and (os == "android") and debug: [OK, ERROR]
    if fission and swgl and (os == "win"): [OK, ERROR]
    [ERROR, OK]
