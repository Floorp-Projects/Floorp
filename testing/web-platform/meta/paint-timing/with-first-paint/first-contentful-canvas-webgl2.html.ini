[first-contentful-canvas-webgl2.html]
  expected:
    if os == "linux": TIMEOUT
    if os == "mac": TIMEOUT
  [First contentful paint fires due to webgl2 canvas render.]
    expected:
      if os == "linux": TIMEOUT
      if os == "mac": TIMEOUT
      PRECONDITION_FAILED

