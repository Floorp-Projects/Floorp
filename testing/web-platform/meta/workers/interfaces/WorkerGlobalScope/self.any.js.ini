[self.any.serviceworker.html]

[self.any.worker.html]

[self.any.sharedworker.html]
  expected:
    if not fission and (os == "win") and not debug and (processor == "x86"): [OK, TIMEOUT]
    if not fission and (os == "linux") and debug and not swgl: [OK, TIMEOUT]
    if fission and (os == "linux") and not debug: [OK, TIMEOUT]
