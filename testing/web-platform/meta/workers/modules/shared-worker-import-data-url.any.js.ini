[shared-worker-import-data-url.any.html]
  expected:
    if debug and not fission and (os == "win") and not webrender and (processor == "x86_64"): ["ERROR", "TIMEOUT"]
    if debug and not fission and (os == "linux") and not webrender: ["ERROR", "TIMEOUT"]
    if debug and not fission and (os == "win") and webrender: ["ERROR", "TIMEOUT"]
    if not debug and (os == "linux") and fission: TIMEOUT
    if not debug and (os == "win"): TIMEOUT
    if not debug and (os == "android"): ["ERROR", "TIMEOUT"]
    if debug and fission: ERROR
    [TIMEOUT, ERROR]
  [Dynamic import.]
    expected: NOTRUN

  [Static import and then dynamic import.]
    expected: NOTRUN

  [eval(import()).]
    expected: NOTRUN

  [Dynamic import and then static import.]
    expected: NOTRUN

  [Static import.]
    expected: TIMEOUT

  [Nested dynamic import.]
    expected: NOTRUN

  [Nested static import.]
    expected: NOTRUN

