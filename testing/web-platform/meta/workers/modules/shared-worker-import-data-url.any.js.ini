[shared-worker-import-data-url.any.html]
  expected:
    if not fission and (os == "linux") and not webrender and (processor == "x86_64") and debug: ["ERROR", "TIMEOUT"]
    if not fission and (os == "linux") and not webrender and (processor == "x86_64") and not debug: ["TIMEOUT", "ERROR"]
    if fission and debug and webrender and (os == "linux"): ["TIMEOUT", "ERROR"]
    if fission and debug and webrender and (os == "win"): ["TIMEOUT", "ERROR"]
    if not fission and (os == "win") and not debug and (processor == "x86"): ["TIMEOUT", "ERROR"]
    if not fission and (os == "linux") and webrender and debug: ["TIMEOUT", "ERROR"]
    if not fission and (os == "linux") and webrender and not debug: ["ERROR", "TIMEOUT"]
    if not fission and (os == "linux") and not webrender and (processor == "x86"): ["TIMEOUT", "ERROR"]
    if not fission and (os == "win") and debug: ["TIMEOUT", "ERROR"]
    if not fission and (os == "mac"): ["TIMEOUT", "ERROR"]
    if not fission and (os == "android"): ["TIMEOUT", "ERROR"]
    TIMEOUT
  [Dynamic import.]
    expected: NOTRUN

  [Static import and then dynamic import.]
    expected: NOTRUN

  [eval(import()).]
    expected: NOTRUN

  [Dynamic import and then static import.]
    expected: NOTRUN

  [Static import.]
    expected: TIMEOUT

  [Nested dynamic import.]
    expected: NOTRUN

  [Nested static import.]
    expected: NOTRUN

