[shared-worker-import-data-url.any.html]
  expected:
    if (os == "win") and debug and not fission and not webrender and (processor == "x86_64"): ["ERROR", "TIMEOUT"]
    if (os == "linux") and debug and webrender and fission: ["ERROR", "TIMEOUT"]
    if (os == "linux") and debug and webrender and not fission: ["ERROR", "TIMEOUT"]
    if (os == "linux") and debug and not webrender and fission: ["ERROR", "TIMEOUT"]
    if (os == "linux") and debug and not webrender and not fission: ["ERROR", "TIMEOUT"]
    if (os == "android") and debug: ["ERROR", "TIMEOUT"]
    if (os == "mac") and not debug: ["ERROR", "TIMEOUT"]
    [TIMEOUT, ERROR]
  [Dynamic import.]
    expected: NOTRUN

  [Static import and then dynamic import.]
    expected: NOTRUN

  [eval(import()).]
    expected: NOTRUN

  [Dynamic import and then static import.]
    expected: NOTRUN

  [Static import.]
    expected: TIMEOUT

  [Nested dynamic import.]
    expected: NOTRUN

  [Nested static import.]
    expected: NOTRUN

