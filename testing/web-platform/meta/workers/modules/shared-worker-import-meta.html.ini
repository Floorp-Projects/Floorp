[shared-worker-import-meta.html]
  expected:
    if not fission and (processor == "x86_64") and debug and (os == "win") and webrender: ["ERROR", "OK"]
    if not fission and (processor == "x86_64") and debug and (os == "android"): ["ERROR", "OK"]
    if fission and (os == "linux") and not debug and webrender: ["ERROR", "OK"]
    if not fission and (processor == "x86") and debug: ["ERROR", "OK"]
    if not fission and (processor == "aarch64"): ["ERROR", "OK"]
    [OK, ERROR]
  [Test import.meta.url on the imported module script.]
    expected: FAIL

  [Test import.meta.url on the imported module script with a fragment.]
    expected: FAIL

  [Test import.meta.url on the top-level module script.]
    expected: FAIL

