[name-property.html]
  expected:
    if not fission and (os == "mac") and not debug: [OK, TIMEOUT]
    if not fission and (os == "linux") and swgl: [OK, TIMEOUT]
    if fission and (os == "linux") and not debug: [OK, TIMEOUT]
