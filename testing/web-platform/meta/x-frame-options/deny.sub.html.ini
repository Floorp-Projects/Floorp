[deny.sub.html]
  expected:
    if fission: TIMEOUT
    OK

  [`XFO: DENY` blocks same-origin framing.]
    expected:
      if fission: TIMEOUT
      PASS

  [`XFO: DENY` blocks cross-origin framing.]
    expected:
      if fission: TIMEOUT
      PASS
