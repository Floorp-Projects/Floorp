[disabled-by-permissions-policy-cross-origin.https.sub.html]
  expected:
    if fission and (os == "win") and debug and (processor == "x86_64"): [OK, TIMEOUT]
    if not fission: [OK, TIMEOUT]
    [TIMEOUT, OK]
  [share() is disabled by default 'self' by permissions policy for cross-origin iframes]
    expected:
      if (os == "mac") and not debug: [FAIL, TIMEOUT]
      if (os == "linux") and fission: [FAIL, TIMEOUT]
      FAIL

  [share() is disabled explicitly by permissions policy for cross-origin iframe]
    expected:
      if (os == "win") and not debug and (processor == "x86"): [TIMEOUT, FAIL, NOTRUN]
      [FAIL, TIMEOUT, NOTRUN]

  [share() not allowed, as only allowed to share with self]
    expected:
      if (os == "linux") and fission and not debug and (processor == "x86"): [TIMEOUT, NOTRUN, FAIL]
      if (os == "linux") and fission and not debug and (processor == "x86_64"): [TIMEOUT, NOTRUN, FAIL]
      if (os == "linux") and fission and debug and swgl: [TIMEOUT, FAIL, NOTRUN]
      if (os == "win") and not debug and (processor == "x86"): [NOTRUN, FAIL, TIMEOUT]
      if (os == "mac") and not debug: [TIMEOUT, NOTRUN, FAIL]
      if (os == "mac") and debug: [TIMEOUT, FAIL, NOTRUN]
      [FAIL, TIMEOUT, NOTRUN]

  [canShare() not allowed to share by default permissions policy cross-origin]
    expected:
      if (os == "linux") and fission and debug and swgl: [NOTRUN, PASS, TIMEOUT]
      if (os == "win") and debug and (processor == "x86"): [TIMEOUT, PASS, NOTRUN]
      if (os == "win") and not debug and (processor == "x86"): [NOTRUN, PASS, TIMEOUT]
      if (os == "win") and not debug and (processor == "x86_64"): [TIMEOUT, PASS, NOTRUN]
      if (os == "linux") and fission and not debug: [NOTRUN, PASS, TIMEOUT]
      if (os == "mac") and not debug: [NOTRUN, PASS, TIMEOUT]
      if (os == "mac") and debug: [NOTRUN, PASS, TIMEOUT]
      [PASS, TIMEOUT, NOTRUN]

  [canShare() is allowed by permissions policy to share cross-origin on a particular origin]
    expected:
      if (os == "linux") and fission and debug and not swgl: [TIMEOUT, PASS, FAIL, NOTRUN]
      if (os == "win") and debug and (processor == "x86_64"): [PASS, FAIL, TIMEOUT, NOTRUN]
      if (os == "linux") and not fission: [PASS, FAIL, TIMEOUT, NOTRUN]
      if os == "android": [PASS, FAIL, TIMEOUT, NOTRUN]
      [NOTRUN, PASS, FAIL, TIMEOUT]

  [canShare() with self]
    expected:
      if fission and (os == "win") and debug and (processor == "x86_64"): [PASS, NOTRUN]
      if not fission: [PASS, NOTRUN]
      [NOTRUN, PASS]
