[disabled-by-permissions-policy-cross-origin.https.sub.html]
  expected:
    if fission and (os == "win") and not swgl and not debug and (processor == "x86"): [OK, TIMEOUT]
    if fission and (os == "win") and not swgl and not debug and (processor == "x86_64"): [OK, TIMEOUT]
    if fission and (os == "win") and not swgl and debug and (processor == "x86"): [OK, TIMEOUT]
    if fission and (os == "linux") and debug: TIMEOUT
    if fission and (os == "linux") and not debug: [OK, TIMEOUT]
  [share() is disabled by default 'self' by permissions policy for cross-origin iframes]
    expected:
      if (os == "linux") and fission and debug and not swgl: [FAIL, TIMEOUT]
      FAIL

  [share() is disabled explicitly by permissions policy for cross-origin iframe]
    expected:
      if (os == "linux") and fission and debug and not swgl: [FAIL, TIMEOUT, NOTRUN]
      if (os == "linux") and fission and debug and swgl: [FAIL, TIMEOUT]
      FAIL

  [share() not allowed, as only allowed to share with self]
    expected:
      if fission and (os == "win") and not swgl and not debug and (processor == "x86_64"): [FAIL, TIMEOUT]
      if fission and (os == "win") and not swgl and debug and (processor == "x86"): [FAIL, TIMEOUT]
      if fission and (os == "linux") and debug and swgl: [TIMEOUT, FAIL, NOTRUN]
      if fission and (os == "linux") and debug and not swgl: [NOTRUN, TIMEOUT]
      if fission and (os == "linux") and not debug: [FAIL, TIMEOUT]
      FAIL

  [canShare() not allowed to share by default permissions policy cross-origin]
    expected:
      if fission and (os == "win") and not swgl and debug and (processor == "x86"): [PASS, NOTRUN]
      if fission and (os == "win") and not swgl and not debug and (processor == "x86_64"): [PASS, NOTRUN]
      if fission and (os == "linux") and debug and swgl: [NOTRUN, TIMEOUT]
      if fission and (os == "linux") and debug and not swgl: NOTRUN
      if fission and (os == "linux") and not debug: [PASS, TIMEOUT, NOTRUN]

  [canShare() is allowed by permissions policy to share cross-origin on a particular origin]
    expected:
      if fission and (os == "win") and debug and (processor == "x86"): [FAIL, NOTRUN]
      if fission and (os == "win") and not debug and (processor == "x86"): [FAIL, TIMEOUT]
      if fission and (os == "win") and not debug and (processor == "x86_64"): [FAIL, NOTRUN]
      if fission and (os == "linux") and not debug: [FAIL, NOTRUN]
      if fission and (os == "linux") and debug: NOTRUN
      FAIL

  [canShare() with self]
    expected:
      if fission and (os == "win") and not swgl and debug and (processor == "x86"): [PASS, NOTRUN]
      if fission and (os == "win") and not swgl and not debug and (processor == "x86_64"): [PASS, NOTRUN]
      if fission and (os == "win") and not swgl and not debug and (processor == "x86"): [PASS, NOTRUN]
      if fission and (os == "linux") and debug: NOTRUN
      if fission and (os == "linux") and not debug: [PASS, NOTRUN]
