[disabled-by-permissions-policy-cross-origin.https.sub.html]
  expected:
    if (os == "win") and debug and (processor == "x86_64") and not swgl: [TIMEOUT, OK]
    if (os == "linux") and fission: [TIMEOUT, OK]
    [OK, TIMEOUT]
  [share() is disabled by default 'self' by permissions policy for cross-origin iframes]
    expected:
      if (os == "mac") and not debug: [FAIL, TIMEOUT]
      if (os == "linux") and fission: [FAIL, TIMEOUT]
      FAIL

  [share() is disabled explicitly by permissions policy for cross-origin iframe]
    expected:
      if (os == "linux") and fission and not debug and (processor == "x86_64"): [TIMEOUT, FAIL, NOTRUN]
      if (os == "linux") and fission and not debug and (processor == "x86"): [TIMEOUT, FAIL, NOTRUN]
      if (os == "win") and debug and (processor == "x86_64") and not swgl: [TIMEOUT, FAIL, NOTRUN]
      [FAIL, TIMEOUT, NOTRUN]

  [share() not allowed, as only allowed to share with self]
    expected:
      if (os == "win") and (processor == "x86_64") and debug and not swgl: [NOTRUN, FAIL, TIMEOUT]
      if (os == "linux") and fission and not debug and (processor == "x86_64"): [NOTRUN, FAIL, TIMEOUT]
      if (os == "linux") and fission and not debug and (processor == "x86"): [NOTRUN, FAIL, TIMEOUT]
      if (os == "mac") and debug: [FAIL, NOTRUN, TIMEOUT]
      [FAIL, TIMEOUT, NOTRUN]

  [canShare() not allowed to share by default permissions policy cross-origin]
    expected:
      if (os == "linux") and fission and not swgl and not debug and (processor == "x86"): [NOTRUN, PASS, TIMEOUT]
      if (os == "linux") and fission and not swgl and not debug and (processor == "x86_64"): [NOTRUN, TIMEOUT, PASS]
      if (os == "linux") and fission and not swgl and debug: [TIMEOUT, PASS, NOTRUN]
      if (os == "win") and debug and (processor == "x86_64") and not swgl: [NOTRUN, PASS, TIMEOUT]
      if (os == "mac") and debug: [PASS, NOTRUN, TIMEOUT]
      [PASS, TIMEOUT, NOTRUN]

  [canShare() is allowed by permissions policy to share cross-origin on a particular origin]
    expected:
      if (os == "win") and debug and (processor == "x86_64") and not swgl: [NOTRUN, PASS, FAIL, TIMEOUT]
      if (os == "linux") and fission and swgl: [TIMEOUT, PASS, FAIL, NOTRUN]
      if (os == "linux") and fission and not swgl: [NOTRUN, PASS, FAIL, TIMEOUT]
      if (os == "mac") and debug: [PASS, NOTRUN, FAIL, TIMEOUT]
      [PASS, FAIL, TIMEOUT, NOTRUN]

  [canShare() with self]
    expected:
      if (os == "win") and debug and (processor == "x86_64") and not swgl: [NOTRUN, PASS]
      if (os == "linux") and fission: [NOTRUN, PASS]
      [PASS, NOTRUN]
