[interactionid-auxclick.html]
  expected:
    if debug and swgl: OK
    if debug and not swgl: [OK, TIMEOUT]
    if not debug and (os == "android"): [OK, TIMEOUT]
    [TIMEOUT, OK]
  [Event Timing: verify pointerdown & pointerup reporting for mouse aux click triggered contextmenu.]
    expected:
      if debug and swgl: FAIL
      if debug and not swgl: [FAIL, TIMEOUT]
      if not debug and (os == "android"): [FAIL, TIMEOUT]
      [TIMEOUT, FAIL]
