[interactionid-auxclick.html]
  expected:
    if not swgl and (os == "android") and debug: [OK, TIMEOUT]
    if swgl: TIMEOUT
    [TIMEOUT, OK]
  [Event Timing: verify pointerdown & pointerup reporting for mouse aux click triggered contextmenu.]
    expected:
      if not swgl and (os == "android") and debug: [FAIL, TIMEOUT]
      if swgl: TIMEOUT
      [TIMEOUT, FAIL]
