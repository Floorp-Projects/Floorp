[interactionid-press-key-no-effect.html]
  expected:
    if (os == "win") and debug and fission and (processor == "x86_64") and not swgl: [OK, TIMEOUT]
    if (os == "win") and not debug: OK
    if (os == "linux") and debug: TIMEOUT
    [TIMEOUT, OK]
  [Event Timing: compare event timing interactionId for key press with no effect.]
    expected:
      if (os == "win") and debug and fission and (processor == "x86_64") and not swgl: [FAIL, TIMEOUT]
      if (os == "win") and not debug: FAIL
      if (os == "linux") and debug: TIMEOUT
      [TIMEOUT, FAIL]
