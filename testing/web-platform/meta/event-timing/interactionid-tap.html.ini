[interactionid-tap.html]
  expected:
    if os == "android": OK
    [TIMEOUT, OK]
  [Event Timing: compare event timing interactionId for tap.]
    expected:
      if os == "android": FAIL
      [TIMEOUT, FAIL]
