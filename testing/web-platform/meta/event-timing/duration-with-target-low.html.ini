[duration-with-target-low.html]
  expected: [OK, TIMEOUT]
  [PerformanceObserver with durationThreshold of 300 and processingDelay of 0 doesn't see any entries in the observer]
    expected:
      if (os == "linux") and debug and not fission and not swgl: [PASS, FAIL, TIMEOUT]
      if (os == "linux") and debug and not fission and swgl: [PASS, FAIL, TIMEOUT]
      if (os == "win") and not swgl and debug and (processor == "x86_64"): [PASS, FAIL, TIMEOUT]
      if (os == "win") and not swgl and not debug and (processor == "x86_64"): [FAIL, TIMEOUT, PASS]
      if (os == "linux") and not debug and (processor == "x86"): [FAIL, TIMEOUT, PASS]
      if os == "android": [PASS, FAIL, TIMEOUT]
      [FAIL, PASS, TIMEOUT]
