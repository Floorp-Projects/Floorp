[304-update.any.html]
  [HTTP cache updates stored headers from a Last-Modified 304]
    expected: [PASS, FAIL]


[304-update.any.worker.html]
  [HTTP cache updates stored headers from a Last-Modified 304]
    expected:
      if (os == "linux") and swgl and fission: PASS
      if (os == "win") and debug: PASS
      if (os == "android") and swgl: PASS
      if os == "mac": PASS
      [PASS, FAIL]


[304-update.any.serviceworker.html]
  expected:
    if processor == "x86": [OK, TIMEOUT]
  [HTTP cache updates stored headers from a Last-Modified 304]
    expected:
      if swgl and (os == "android"): [FAIL, PASS]
      [PASS, FAIL]

  [Content-* header]
    expected:
      if processor == "x86": [PASS, TIMEOUT]


[304-update.any.sharedworker.html]
  [HTTP cache updates stored headers from a Last-Modified 304]
    expected:
      if not fission and not swgl and (os == "linux") and not debug: [PASS, FAIL]
      if fission and swgl and (os == "win"): [PASS, FAIL]
      if not fission and not swgl and (os == "android"): [PASS, FAIL]
