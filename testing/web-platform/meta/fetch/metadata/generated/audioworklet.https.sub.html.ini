[audioworklet.https.sub.html]
  expected:
    if (os == "linux") and debug and not fission and not swgl: [TIMEOUT, OK, ERROR]
    if (os == "linux") and debug and not fission and swgl: [ERROR, OK, TIMEOUT]
    if (os == "linux") and debug and fission: [TIMEOUT, ERROR]
    if (os == "linux") and not debug and not fission: [TIMEOUT, ERROR]
    if (os == "linux") and not debug and fission: [TIMEOUT, ERROR]
    if (os == "android") and fission: [OK, TIMEOUT]
  [sec-fetch-site - Same-Origin -> Same-Site -> Same-Origin redirect]
    expected:
      if (os == "linux") and not debug and not fission: [TIMEOUT, PASS]
      if (os == "linux") and not debug and fission: [PASS, TIMEOUT]
      if (os == "linux") and debug: [PASS, TIMEOUT]

  [sec-fetch-site - Cross-Site -> Same Origin]
    expected:
      if (os == "linux") and not debug and not fission: [NOTRUN, PASS]
      if (os == "linux") and not debug and fission: [PASS, NOTRUN]
      if (os == "linux") and debug: [PASS, NOTRUN]

  [sec-fetch-site - Cross-Site -> Same-Site]
    expected:
      if (os == "linux") and not debug and not fission: [NOTRUN, PASS]
      if (os == "linux") and not debug and fission: [PASS, NOTRUN]
      if (os == "linux") and debug: [PASS, NOTRUN]

  [sec-fetch-site - Cross-Site -> Cross-Site]
    expected:
      if (os == "linux") and not debug and not fission: [NOTRUN, PASS]
      if (os == "linux") and not debug and fission: [PASS, NOTRUN]
      if (os == "linux") and debug: [PASS, NOTRUN]

  [sec-fetch-site - Same-Origin -> Same Origin]
    expected:
      if (os == "linux") and not debug and not fission: [NOTRUN, PASS]
      if (os == "linux") and not debug and fission: [PASS, TIMEOUT, NOTRUN]
      if (os == "linux") and debug: [PASS, NOTRUN]

  [sec-fetch-site - Same-Origin -> Same-Site]
    expected:
      if (os == "linux") and not debug and not fission: [NOTRUN, PASS]
      if (os == "linux") and not debug and fission: [PASS, NOTRUN]
      if (os == "linux") and debug: [PASS, NOTRUN]

  [sec-fetch-site - Same-Origin -> Cross-Site]
    expected:
      if (os == "linux") and not debug and not fission: [NOTRUN, PASS]
      if (os == "linux") and not debug and fission: [PASS, NOTRUN]
      if (os == "linux") and debug: [PASS, NOTRUN]

  [sec-fetch-site - Same-Site -> Same Origin]
    expected:
      if (os == "linux") and not fission and not debug: [NOTRUN, PASS]
      if (os == "linux") and not fission and debug: [PASS, NOTRUN]
      if (os == "linux") and fission and not debug: [PASS, TIMEOUT, NOTRUN]
      if (os == "linux") and fission and debug: [PASS, TIMEOUT, NOTRUN]

  [sec-fetch-site - Same-Site -> Same-Site]
    expected:
      if (os == "linux") and not fission and debug and not swgl: [TIMEOUT, PASS, NOTRUN]
      if (os == "linux") and not fission and debug and swgl: [PASS, TIMEOUT, NOTRUN]
      if (os == "linux") and not fission and not debug: [NOTRUN, TIMEOUT]
      if (os == "linux") and fission: [TIMEOUT, NOTRUN]

  [sec-fetch-site - Same-Site -> Cross-Site]
    expected:
      if (os == "linux") and not swgl and debug and not fission: [NOTRUN, PASS, TIMEOUT]
      if (os == "linux") and not swgl and debug and fission: NOTRUN
      if (os == "linux") and not swgl and not debug: NOTRUN
      if (os == "linux") and swgl and fission: NOTRUN
      if (os == "linux") and swgl and not fission: [PASS, TIMEOUT, NOTRUN]

  [sec-fetch-mode]
    expected:
      if (os == "linux") and not swgl and debug and fission: NOTRUN
      if (os == "linux") and not swgl and debug and not fission: [NOTRUN, PASS, TIMEOUT]
      if (os == "linux") and swgl and fission: NOTRUN
      if (os == "linux") and swgl and not fission: [NOTRUN, PASS, TIMEOUT]
      if (os == "linux") and not swgl and not debug: NOTRUN

  [sec-fetch-dest]
    expected:
      if (os == "linux") and not swgl and debug and fission: NOTRUN
      if (os == "linux") and not swgl and debug and not fission: [NOTRUN, PASS, TIMEOUT]
      if (os == "linux") and not swgl and not debug: NOTRUN
      if (os == "linux") and swgl and not fission: [NOTRUN, PASS, TIMEOUT]
      if (os == "linux") and swgl and fission: NOTRUN

  [sec-fetch-user]
    expected:
      if (os == "linux") and not swgl and debug and not fission: [NOTRUN, PASS]
      if (os == "linux") and not swgl and debug and fission: NOTRUN
      if (os == "linux") and not swgl and not debug: NOTRUN
      if (os == "linux") and swgl and fission: NOTRUN
      if (os == "linux") and swgl and not fission: [NOTRUN, PASS]
