[window-open.https.sub.html]
  expected:
    if (os == "linux") and webrender and not debug and not fission: ["ERROR", "OK"]
    if (os == "win") and (processor == "x86_64") and webrender and not fission: OK
    if (os == "win") and (processor == "x86"): OK
    [OK, ERROR]
  [Cross-site window, forced, reloaded]
    expected:
      if (processor == "x86_64") and (os == "win") and debug and not webrender: ["FAIL", "PASS"]
      [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if (os == "linux") and debug and webrender and fission: PASS
      if (os == "linux") and debug and not webrender: PASS
      if (os == "mac") and debug: PASS
      if os == "win": PASS
      [PASS, FAIL]

