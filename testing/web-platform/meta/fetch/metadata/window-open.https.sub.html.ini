[window-open.https.sub.html]
  expected:
    if (os == "linux") and not fission and not debug and not webrender: ["OK", "ERROR"]
    if (os == "linux") and not fission and not debug and webrender: ["OK", "ERROR"]
    if (os == "linux") and not fission and debug and webrender: ["OK", "ERROR"]
    if (os == "linux") and not fission and debug and not webrender: ["ERROR", "OK"]
    if (os == "linux") and fission and debug: ["OK", "ERROR"]
    if (os == "linux") and fission and not debug: ["OK", "ERROR"]
  [Cross-site window, forced, reloaded]
    expected: [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if os == "android": ["PASS", "FAIL"]

