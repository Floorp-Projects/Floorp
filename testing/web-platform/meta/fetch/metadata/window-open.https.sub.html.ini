[window-open.https.sub.html]
  expected:
    if not fission and not debug and (os == "win") and webrender: ["ERROR", "OK"]
    if fission and (os == "linux") and not debug and not webrender: ["ERROR", "OK"]
    if fission and (os == "win") and not debug: ["ERROR", "OK"]
    if fission and (os == "linux") and debug: ["ERROR", "OK"]
    [OK, ERROR]
  [Cross-site window, forced, reloaded]
    expected:
      if (os == "win") and not webrender and debug and (processor == "x86"): ["FAIL", "PASS"]
      if (os == "win") and not webrender and not debug and (processor == "x86"): ["FAIL", "PASS"]
      if (os == "win") and not webrender and not debug and (processor == "x86_64"): ["FAIL", "PASS"]
      [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if (os == "android") and debug and not swgl: ["FAIL", "PASS"]
      if (os == "mac") and debug: PASS
      if os == "win": PASS
      [PASS, FAIL]

