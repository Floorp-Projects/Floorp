[window-open.https.sub.html]
  expected:
    if (os == "linux") and not fission and (processor == "x86_64") and not swgl and debug and not webrender: [ERROR, OK]
    if (os == "linux") and not fission and (processor == "x86_64") and not swgl and not debug and webrender: [ERROR, OK]
    if (os == "linux") and not fission and (processor == "x86"): [ERROR, OK]
    [OK, ERROR]
  [Cross-site window, forced, reloaded]
    expected:
      if (os == "win") and (processor == "x86_64") and debug and webrender and not fission and not swgl: [FAIL, PASS]
      [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if (os == "android") and debug and not swgl: [FAIL, PASS]
      if (os == "mac") and debug: PASS
      if os == "win": PASS
      [PASS, FAIL]
