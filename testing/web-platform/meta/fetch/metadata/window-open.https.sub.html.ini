[window-open.https.sub.html]
  expected:
    if (os == "linux") and fission and not debug and not webrender: ["ERROR", "OK"]
    if (os == "win") and fission and not debug: ["ERROR", "OK"]
    if (os == "linux") and not fission and swgl: ["ERROR", "OK"]
    [OK, ERROR]
  [Cross-site window, forced, reloaded]
    expected:
      if (os == "win") and debug and (processor == "x86_64") and swgl: ["FAIL", "PASS"]
      if (os == "win") and not debug and webrender and not fission: ["FAIL", "PASS"]
      if (os == "win") and not debug and not webrender and (processor == "x86_64"): ["FAIL", "PASS"]
      [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if (os == "android") and debug and webrender and not swgl: ["FAIL", "PASS"]
      if (os == "mac") and debug: PASS
      if os == "win": PASS
      [PASS, FAIL]

