[window-open.https.sub.html]
  expected: [OK, ERROR]
  [Cross-site window, forced, reloaded]
    expected:
      if (os == "win") and not swgl and not fission and not debug and (processor == "x86"): [FAIL, PASS]
      if (os == "win") and not swgl and not fission and debug and (processor == "x86_64"): [FAIL, PASS]
      if (os == "win") and not swgl and fission and not debug: [FAIL, PASS]
      [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if (os == "mac") and debug: PASS
      if os == "win": PASS
      [PASS, FAIL]
