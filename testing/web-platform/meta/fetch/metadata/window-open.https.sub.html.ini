[window-open.https.sub.html]
  expected:
    if (os == "android") and webrender and debug: ["ERROR", "OK"]
    [OK, ERROR]
  [Cross-site window, forced, reloaded]
    expected:
      if debug and not fission and swgl and (os == "win"): ["FAIL", "PASS"]
      if debug and fission: ["FAIL", "PASS"]
      [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if (os == "mac") and debug: PASS
      if os == "win": PASS
      [PASS, FAIL]

