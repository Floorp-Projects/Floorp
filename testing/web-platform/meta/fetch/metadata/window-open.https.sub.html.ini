[window-open.https.sub.html]
  expected: [OK, ERROR]
  [Cross-site window, forced, reloaded]
    expected: [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if (os == "win") and (processor == "x86_64") and not debug and fission: PASS
      if (os == "win") and (processor == "x86_64") and debug: PASS
      [PASS, FAIL]
