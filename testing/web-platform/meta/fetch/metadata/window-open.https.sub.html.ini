[window-open.https.sub.html]
  expected:
    if not swgl and (os == "linux") and webrender and not debug and not fission: ["ERROR", "OK"]
    if not swgl and (os == "win") and debug and not webrender and (processor == "x86_64"): ["ERROR", "OK"]
    if swgl and (os == "win"): ["ERROR", "OK"]
    if swgl and (os == "linux"): ["ERROR", "OK"]
    [OK, ERROR]
  [Cross-site window, forced, reloaded]
    expected:
      if (os == "linux") and debug and webrender and not fission and not swgl: ["FAIL", "PASS"]
      if (os == "win") and not fission and not debug and not webrender and (processor == "x86"): ["FAIL", "PASS"]
      if (os == "win") and not fission and debug and webrender and not swgl: ["FAIL", "PASS"]
      if (os == "win") and not fission and not debug and webrender: ["FAIL", "PASS"]
      [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if (os == "mac") and debug: PASS
      if (os == "android") and swgl: ["FAIL", "PASS"]
      if os == "win": PASS
      [PASS, FAIL]

