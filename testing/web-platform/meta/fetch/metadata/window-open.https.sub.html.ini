[window-open.https.sub.html]
  expected:
    if webrender and (os == "linux") and debug: ["ERROR", "OK"]
    if webrender and (os == "android") and not debug: ["ERROR", "OK"]
    if not webrender and (processor == "x86"): OK
    [OK, ERROR]
  [Cross-site window, forced, reloaded]
    expected:
      if (processor == "x86") and not debug: ["FAIL", "PASS"]
      [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if (os == "mac") and debug: PASS
      if (os == "linux") and debug: PASS
      if os == "win": PASS
      [PASS, FAIL]

