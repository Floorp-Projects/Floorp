[window-open.https.sub.html]
  expected:
    if (os == "linux") and not swgl and not webrender and fission: ["ERROR", "OK"]
    if (os == "win") and fission and not debug: ["ERROR", "OK"]
    if (os == "linux") and swgl: ["ERROR", "OK"]
    [OK, ERROR]
  [Cross-site window, forced, reloaded]
    expected:
      if (os == "win") and (processor == "x86_64") and webrender and debug and not fission and swgl: ["FAIL", "PASS"]
      if (os == "win") and (processor == "x86_64") and webrender and debug and fission: ["FAIL", "PASS"]
      if (os == "win") and (processor == "x86_64") and not webrender: ["FAIL", "PASS"]
      [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if (os == "mac") and debug: PASS
      if os == "win": PASS
      [PASS, FAIL]

