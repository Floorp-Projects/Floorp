[window-open.https.sub.html]
  expected:
    if (processor == "x86_64") and (os == "linux") and webrender and not debug and not fission: ["ERROR", "OK"]
    if processor == "x86": OK
    [OK, ERROR]
  [Cross-site window, forced, reloaded]
    expected:
      if (os == "win") and not webrender and debug and (processor == "x86"): ["FAIL", "PASS"]
      if (os == "win") and webrender and not debug and not fission: ["FAIL", "PASS"]
      if (os == "win") and not webrender and not debug: ["FAIL", "PASS"]
      [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if (os == "mac") and debug: PASS
      if os == "win": PASS
      [PASS, FAIL]

