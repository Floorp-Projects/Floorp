[window-open.https.sub.html]
  expected:
    if not debug and (os == "linux") and not webrender and (processor == "x86"): ["ERROR", "OK"]
    if not debug and (os == "win") and webrender and not fission: ["ERROR", "OK"]
    [OK, ERROR]
  [Cross-site window, forced, reloaded]
    expected:
      if not webrender and (processor == "x86_64") and (os == "win") and debug: ["FAIL", "PASS"]
      [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if (os == "mac") and debug: PASS
      if os == "win": PASS
      [PASS, FAIL]

