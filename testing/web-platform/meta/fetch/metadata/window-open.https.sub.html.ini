[window-open.https.sub.html]
  expected:
    if (os == "win") and fission and debug: [ERROR, OK]
    if (os == "mac") and debug: [ERROR, OK]
    [OK, ERROR]
  [Cross-site window, forced, reloaded]
    expected:
      if (os == "win") and not fission and (processor == "x86_64") and debug and not swgl: [FAIL, PASS]
      if (os == "win") and not fission and (processor == "x86") and not debug: [FAIL, PASS]
      if (os == "win") and not fission and (processor == "x86") and debug: [FAIL, PASS]
      [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if (os == "mac") and debug: PASS
      if os == "win": PASS
      [PASS, FAIL]
