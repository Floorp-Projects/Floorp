[window-open.https.sub.html]
  expected:
    if webrender and not debug and (os == "win") and not fission: ["ERROR", "OK"]
    if webrender and debug and (os == "android"): ["ERROR", "OK"]
    if webrender and not debug and (os == "linux"): ["ERROR", "OK"]
    if not webrender and (os == "win"): OK
    [OK, ERROR]
  [Cross-site window, forced, reloaded]
    expected:
      if (os == "win") and not webrender and (processor == "x86") and not debug: ["FAIL", "PASS"]
      [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if (os == "linux") and debug and webrender and not fission: ["PASS", "FAIL"]
      if (os == "linux") and not debug: ["PASS", "FAIL"]
      if os == "android": ["PASS", "FAIL"]

