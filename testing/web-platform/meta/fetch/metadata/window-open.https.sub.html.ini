[window-open.https.sub.html]
  expected:
    if (processor == "x86_64") and (os == "android") and not swgl and debug and not webrender: [ERROR, OK]
    if (processor == "x86_64") and (os == "win") and fission and debug: [ERROR, OK]
    [OK, ERROR]
  [Cross-site window, forced, reloaded]
    expected:
      if (os == "linux") and not fission and (processor == "x86_64") and not swgl and debug and webrender: [FAIL, PASS]
      if (os == "win") and (processor == "x86_64") and not webrender and debug: [FAIL, PASS]
      if (os == "win") and (processor == "x86"): [FAIL, PASS]
      [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if (os == "mac") and debug: PASS
      if os == "win": PASS
      [PASS, FAIL]
