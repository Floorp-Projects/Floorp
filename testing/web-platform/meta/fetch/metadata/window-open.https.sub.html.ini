[window-open.https.sub.html]
  expected:
    if (os == "linux") and (processor == "x86"): [ERROR, OK]
    if (os == "mac") and not debug: [ERROR, OK]
    if (os == "win") and fission: [ERROR, OK]
    [OK, ERROR]
  [Cross-site window, forced, reloaded]
    expected:
      if (processor == "x86_64") and (os == "win") and not debug and not fission: [FAIL, PASS]
      if (processor == "x86_64") and (os == "linux") and swgl: [FAIL, PASS]
      [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if (os == "win") and not debug and not fission and (processor == "x86"): [FAIL, PASS]
      if (os == "win") and not debug and fission: PASS
      if (os == "win") and debug: PASS
      if (os == "mac") and debug: PASS
      [PASS, FAIL]
