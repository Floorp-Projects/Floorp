[window-open.https.sub.html]
  expected:
    if (processor == "x86_64") and fission and (os == "win") and not debug: ["ERROR", "OK"]
    if (processor == "x86") and (os == "linux"): ["ERROR", "OK"]
    [OK, ERROR]
  [Cross-site window, forced, reloaded]
    expected:
      if (os == "win") and not swgl and not debug and webrender and not fission: ["FAIL", "PASS"]
      [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if (os == "mac") and debug: PASS
      if os == "win": PASS
      [PASS, FAIL]

