[window-open.https.sub.html]
  expected:
    if (os == "linux") and not swgl and fission and not debug: ["ERROR", "OK"]
    if (os == "mac") and not debug: ["ERROR", "OK"]
    if (os == "linux") and swgl: ["ERROR", "OK"]
    [OK, ERROR]
  [Cross-site window, forced, reloaded]
    expected:
      if (os == "win") and (processor == "x86_64") and debug and webrender and not swgl: ["FAIL", "PASS"]
      if (os == "win") and (processor == "x86") and not debug: ["FAIL", "PASS"]
      [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if (os == "linux") and (processor == "x86"): ["FAIL", "PASS"]
      if (os == "mac") and debug: PASS
      if os == "win": PASS
      [PASS, FAIL]

