[window-open.https.sub.html]
  expected:
    if (processor == "x86_64") and (os == "win") and fission: ["ERROR", "OK"]
    if (processor == "x86") and (os == "win"): OK
    [OK, ERROR]
  [Cross-site window, forced, reloaded]
    expected:
      if (processor == "x86_64") and (os == "win") and not webrender and not debug: ["FAIL", "PASS"]
      if (processor == "x86") and debug: ["FAIL", "PASS"]
      [PASS, FAIL]

  [Same-site window, forced, reloaded]
    expected:
      if (os == "android") and webrender and debug: ["FAIL", "PASS"]
      if (os == "mac") and debug: PASS
      if os == "win": PASS
      [PASS, FAIL]

