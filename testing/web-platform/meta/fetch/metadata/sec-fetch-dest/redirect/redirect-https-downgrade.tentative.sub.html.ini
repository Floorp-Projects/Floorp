[redirect-https-downgrade.tentative.sub.html]
  expected: TIMEOUT
  [redirect-https-downgrade]
    expected: FAIL

  [Https downgrade stylesheet]
    expected: TIMEOUT

  [Https downgrade font => No headers]
    expected: NOTRUN

  [Https downgrade prefetch => No headers]
    expected:
      if (os == "win") and (processor == "x86_64") and not debug and not webrender and not fission: TIMEOUT
      if (os == "win") and (processor == "x86_64") and not debug and not webrender and fission: ["TIMEOUT", "FAIL"]
      if (os == "linux") and debug and not webrender and not fission: ["FAIL", "TIMEOUT"]
      if (os == "linux") and debug and not webrender and fission: ["FAIL", "TIMEOUT"]
      if (os == "linux") and debug and webrender and not fission: ["FAIL", "TIMEOUT"]
      if (os == "win") and (processor == "x86_64") and not debug and webrender: TIMEOUT
      if (os == "win") and (processor == "x86_64") and debug and webrender: ["FAIL", "TIMEOUT"]
      if (os == "win") and (processor == "x86_64") and debug and not webrender: ["TIMEOUT", "FAIL"]
      FAIL

  [Https downgrade track]
    expected: NOTRUN

  [Https downgrade image => No headers]
    expected: NOTRUN

