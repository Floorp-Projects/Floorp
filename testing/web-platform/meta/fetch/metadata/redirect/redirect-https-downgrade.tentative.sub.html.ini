[redirect-https-downgrade.tentative.sub.html]
  expected: TIMEOUT
  [Https downgrade stylesheet]
    expected: TIMEOUT

  [Https downgrade font => No headers]
    expected: NOTRUN

  [Https downgrade prefetch => No headers]
    expected:
      if (os == "win") and (processor == "x86_64") and not fission and debug and webrender: ["FAIL", "TIMEOUT"]
      if (os == "win") and (processor == "x86_64") and not fission and debug and not webrender: ["FAIL", "TIMEOUT"]
      if (os == "win") and (processor == "x86_64") and not fission and not debug and webrender: ["FAIL", "TIMEOUT"]
      if (os == "win") and (processor == "x86") and debug and not fission: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "linux"): ["FAIL", "PASS"]
      if (os == "win") and (processor == "x86_64") and fission: ["FAIL", "TIMEOUT"]
      if (os == "mac") and debug: ["TIMEOUT", "FAIL"]
      [FAIL, PASS]

  [Https downgrade track]
    expected: NOTRUN

  [Https downgrade image => No headers]
    expected: NOTRUN

