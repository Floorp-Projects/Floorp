[redirect-keepalive.https.any.html]
  expected:
    if (os == "linux") and fission and not tsan and debug and swgl: [OK, TIMEOUT]
    if (os == "linux") and fission and not tsan and debug and not swgl: TIMEOUT
    if (os == "linux") and fission and not tsan and not debug: [OK, TIMEOUT]
    if (os == "win") and not debug and (processor == "x86_64"): [OK, TIMEOUT]
    if (os == "linux") and fission and tsan: [TIMEOUT, OK]
  [[keepalive\][iframe\][load\] mixed content redirect; setting up]
    expected:
      if (os == "linux") and fission and not tsan and debug and swgl: [PASS, TIMEOUT]
      if (os == "linux") and fission and not tsan and debug and not swgl: TIMEOUT
      if (os == "linux") and fission and not tsan and not debug: [PASS, TIMEOUT]
      if (os == "win") and not debug and (processor == "x86_64"): [PASS, TIMEOUT]
      if (os == "linux") and fission and tsan: [TIMEOUT, PASS]
