[orientation-reading.html]
  expected:
    if (os == "linux") and (processor == "x86_64") and not fission and not debug and webrender: ["ERROR", "OK"]
    if (os == "linux") and (processor == "x86_64") and not fission and debug and not webrender: ["ERROR", "OK"]
    if (os == "linux") and (processor == "x86_64") and fission and not debug: ["ERROR", "OK"]
    if (os == "mac") and debug: ["ERROR", "OK"]
    if (os == "mac") and not debug: ["ERROR", "OK"]
    if (os == "win") and (processor == "x86"): ["ERROR", "OK"]
    if (os == "linux") and (processor == "x86"): ["ERROR", "OK"]
    if os == "android": ["TIMEOUT", "OK", "ERROR"]
    [OK, ERROR]
  [Test that screen.orientation values change if the orientation changes]
    expected:
      if os == "android": NOTRUN
      FAIL

  [Test the orientations and associated angles]
    expected:
      if os == "android": TIMEOUT
      FAIL

