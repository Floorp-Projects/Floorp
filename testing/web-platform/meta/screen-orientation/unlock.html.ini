[unlock.html]
  expected:
    if os == "android": OK
    ERROR
  [unlock() aborts a pending lock request]
    expected:
      if os == "android": PASS
      NOTRUN

  [unlock() aborts a pending lock request across documents]
    expected:
      if os == "android": FAIL
      NOTRUN
