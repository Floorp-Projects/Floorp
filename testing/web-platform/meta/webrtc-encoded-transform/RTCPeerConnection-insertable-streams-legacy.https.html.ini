[RTCPeerConnection-insertable-streams-legacy.https.html]
  expected:
    if (os == "linux") and webrender and not swgl and fission and not debug: ["OK", "TIMEOUT"]
    if (os == "linux") and webrender and not swgl and fission and debug: ["OK", "TIMEOUT"]
    if (os == "linux") and webrender and not swgl and not fission: ["OK", "TIMEOUT"]
    if (os == "linux") and not webrender and not debug and not fission: ["OK", "TIMEOUT", "CRASH"]
    if (os == "linux") and not webrender and not debug and fission: ["OK", "TIMEOUT"]
    if (os == "linux") and webrender and swgl: ["TIMEOUT", "OK"]
    if (os == "linux") and not webrender and debug: ["OK", "TIMEOUT"]
  [Legacy API works]
    expected: FAIL

