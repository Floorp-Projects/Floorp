[RTCPeerConnection-insertable-streams-legacy.https.html]
  expected:
    if (os == "linux") and webrender and (processor == "x86_64") and debug and not fission: [OK, TIMEOUT]
    if (os == "linux") and webrender and (processor == "x86_64") and debug and fission: [OK, TIMEOUT]
    if (os == "linux") and not webrender and not fission and debug: [OK, TIMEOUT]
    if (os == "linux") and not webrender and not fission and not debug: [OK, TIMEOUT, CRASH]
    if (os == "linux") and webrender and (processor == "x86_64") and not debug: [OK, TIMEOUT]
    if (os == "linux") and not webrender and fission: [OK, TIMEOUT]
    if (os == "linux") and webrender and (processor == "x86"): [OK, TIMEOUT]
  [Legacy API works]
    expected: FAIL
