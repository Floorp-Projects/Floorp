[RTCPeerConnection-insertable-streams-legacy.https.html]
  expected:
    if (os == "linux") and not swgl and (processor == "x86_64") and not debug and not webrender and not fission: ["OK", "TIMEOUT"]
    if (os == "linux") and not swgl and (processor == "x86_64") and debug and webrender: ["OK", "TIMEOUT"]
    if (os == "linux") and not swgl and (processor == "x86_64") and debug and not webrender: ["OK", "TIMEOUT"]
    if (os == "linux") and not swgl and (processor == "x86"): ["OK", "TIMEOUT"]
    if (os == "linux") and swgl: ["TIMEOUT", "OK"]
  [Legacy API works]
    expected: FAIL

