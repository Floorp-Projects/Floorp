[RTCPeerConnection-insertable-streams-legacy.https.html]
  expected:
    if (os == "linux") and not fission and debug and swgl: [OK, TIMEOUT]
    if (os == "linux") and not fission and debug and not swgl: [OK, TIMEOUT]
    if (os == "linux") and not fission and not debug and not webrender: [OK, TIMEOUT, CRASH]
    if (os == "linux") and not fission and not debug and webrender: [OK, TIMEOUT]
    if (os == "linux") and fission: [OK, TIMEOUT]
  [Legacy API works]
    expected: FAIL
