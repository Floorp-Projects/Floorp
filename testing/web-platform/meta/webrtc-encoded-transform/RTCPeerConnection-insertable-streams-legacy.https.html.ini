[RTCPeerConnection-insertable-streams-legacy.https.html]
  expected:
    if (os == "linux") and not debug and not fission and not webrender: [OK, TIMEOUT, CRASH]
    if (os == "linux") and not debug and not fission and webrender: [TIMEOUT, OK]
    if (os == "linux") and debug and webrender and not fission: [OK, TIMEOUT]
    if (os == "linux") and debug and webrender and fission: [TIMEOUT, OK]
    if (os == "linux") and not debug and fission: [OK, TIMEOUT]
    if (os == "linux") and debug and not webrender: [TIMEOUT, OK]
  [Legacy API works]
    expected: FAIL
