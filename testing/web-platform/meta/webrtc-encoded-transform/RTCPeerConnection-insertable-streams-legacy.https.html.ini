[RTCPeerConnection-insertable-streams-legacy.https.html]
  expected:
    if (os == "linux") and webrender and not swgl and not debug and fission: ["OK", "TIMEOUT"]
    if (os == "linux") and webrender and not swgl and not debug and not fission: ["OK", "TIMEOUT"]
    if (os == "linux") and webrender and not swgl and debug and not fission: ["TIMEOUT", "OK"]
    if (os == "linux") and webrender and not swgl and debug and fission: ["OK", "TIMEOUT"]
    if (os == "linux") and not webrender and not debug and fission: ["OK", "TIMEOUT"]
    if (os == "linux") and not webrender and not debug and not fission: ["OK", "TIMEOUT", "CRASH"]
    if (os == "linux") and webrender and swgl: ["OK", "TIMEOUT"]
    if (os == "linux") and not webrender and debug: ["OK", "TIMEOUT"]
  [Legacy API works]
    expected: FAIL

