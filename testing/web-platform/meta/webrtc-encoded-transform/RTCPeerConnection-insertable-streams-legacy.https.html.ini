[RTCPeerConnection-insertable-streams-legacy.https.html]
  expected:
    if (os == "linux") and not fission and not debug and not webrender and (processor == "x86_64"): ["OK", "TIMEOUT"]
    if (os == "linux") and not fission and not debug and not webrender and (processor == "x86"): ["OK", "TIMEOUT"]
    if (os == "linux") and not fission and debug and webrender and not swgl: ["TIMEOUT", "OK"]
    if (os == "linux") and not fission and debug and webrender and swgl: ["OK", "TIMEOUT"]
    if (os == "linux") and not fission and debug and not webrender: ["OK", "TIMEOUT"]
    if (os == "linux") and fission and not debug and not webrender: ["OK", "TIMEOUT"]
    if (os == "linux") and fission and debug: ["OK", "TIMEOUT"]
  [Legacy API works]
    expected: FAIL

