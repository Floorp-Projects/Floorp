[RTCPeerConnection-insertable-streams-legacy.https.html]
  expected:
    if (os == "linux") and not swgl and not fission and (processor == "x86_64") and not debug and not webrender: ["OK", "TIMEOUT"]
    if (os == "linux") and not swgl and not fission and (processor == "x86_64") and debug and webrender: ["OK", "TIMEOUT"]
    if (os == "linux") and not swgl and not fission and (processor == "x86"): ["OK", "TIMEOUT"]
    if (os == "linux") and not swgl and fission and debug: ["OK", "TIMEOUT"]
    if (os == "linux") and swgl: ["OK", "TIMEOUT"]
  [Legacy API works]
    expected: FAIL

