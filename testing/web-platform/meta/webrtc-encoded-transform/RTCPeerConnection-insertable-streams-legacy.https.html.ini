[RTCPeerConnection-insertable-streams-legacy.https.html]
  expected:
    if (os == "linux") and debug and webrender and not swgl and not fission: ["OK", "TIMEOUT"]
    if (os == "linux") and debug and webrender and not swgl and fission: ["OK", "TIMEOUT"]
    if (os == "linux") and not debug and not webrender and not fission and (processor == "x86_64"): ["OK", "TIMEOUT"]
  [Legacy API works]
    expected: FAIL

