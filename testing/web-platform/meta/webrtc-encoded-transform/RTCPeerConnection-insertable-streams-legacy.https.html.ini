[RTCPeerConnection-insertable-streams-legacy.https.html]
  expected:
    if (os == "linux") and debug and webrender and not fission and swgl: ["OK", "TIMEOUT"]
    if (os == "linux") and debug and webrender and not fission and not swgl: ["OK", "TIMEOUT"]
    if (os == "linux") and not debug and not webrender and not fission: ["OK", "TIMEOUT"]
    if (os == "linux") and debug and webrender and fission: ["OK", "TIMEOUT"]
  [Legacy API works]
    expected: FAIL

