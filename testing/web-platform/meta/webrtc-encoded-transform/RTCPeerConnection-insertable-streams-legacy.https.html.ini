[RTCPeerConnection-insertable-streams-legacy.https.html]
  expected:
    if (os == "linux") and webrender and not fission and not swgl and debug: ["TIMEOUT", "OK"]
    if (os == "linux") and webrender and not fission and not swgl and not debug: ["OK", "TIMEOUT"]
    if (os == "linux") and not webrender and not debug and not fission and (processor == "x86_64"): ["OK", "TIMEOUT", "CRASH"]
    if (os == "linux") and not webrender and not debug and not fission and (processor == "x86"): ["OK", "TIMEOUT", "CRASH"]
    if (os == "linux") and webrender and not fission and swgl: ["OK", "TIMEOUT"]
    if (os == "linux") and not webrender and not debug and fission: ["OK", "TIMEOUT"]
    if (os == "linux") and webrender and fission: ["OK", "TIMEOUT"]
    if (os == "linux") and not webrender and debug: ["OK", "TIMEOUT"]
  [Legacy API works]
    expected: FAIL

