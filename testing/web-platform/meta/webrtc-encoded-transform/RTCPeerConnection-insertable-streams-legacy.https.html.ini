[RTCPeerConnection-insertable-streams-legacy.https.html]
  expected:
    if (os == "linux") and not webrender and (processor == "x86_64") and not debug and not fission: ["OK", "TIMEOUT"]
    if (os == "linux") and not webrender and (processor == "x86_64") and not debug and fission: ["OK", "TIMEOUT"]
    if (os == "linux") and webrender and debug and not fission and swgl: ["OK", "TIMEOUT"]
    if (os == "linux") and webrender and debug and not fission and not swgl: ["OK", "TIMEOUT"]
    if (os == "linux") and not webrender and (processor == "x86_64") and debug: ["OK", "TIMEOUT"]
    if (os == "linux") and webrender and debug and fission: ["TIMEOUT", "OK"]
    if (os == "linux") and not webrender and (processor == "x86"): ["OK", "TIMEOUT"]
  [Legacy API works]
    expected: FAIL

