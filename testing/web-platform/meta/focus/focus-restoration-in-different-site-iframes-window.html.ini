[focus-restoration-in-different-site-iframes-window.html]
  disabled:
    if (os == "linux") or debug: https://bugzilla.mozilla.org/show_bug.cgi?id=1642889
  [Check result]
    expected:
      if not fission and (os == "win") and webrender: ["PASS", "FAIL"]
      if not fission and (os == "mac"): ["PASS", "FAIL"]
      if fission: ["PASS", "FAIL"]

