[storagemanager-persist.https.window.html]
  expected:
    if (os == "android"): OK
    TIMEOUT
  [navigator.storage.persist() returns a promise that resolves.]
    expected:
      if (os == "android"): FAIL
      TIMEOUT

