[current-task-signal-async-priority.any.serviceworker.html]
  [Test that currentTaskSignal works through async promise resolution]
    expected: FAIL


[current-task-signal-async-priority.any.sharedworker.html]
  expected:
    if not fission and (os == "linux") and not swgl and not debug and (processor == "x86_64"): [OK, TIMEOUT]
    if not fission and (os == "linux") and swgl: [OK, TIMEOUT]
    if not fission and (os == "android") and not debug: [OK, TIMEOUT]
    if fission and (os == "linux") and not debug: [OK, TIMEOUT]
  [Test that currentTaskSignal works through async promise resolution]
    expected: FAIL


[current-task-signal-async-priority.any.worker.html]
  [Test that currentTaskSignal works through async promise resolution]
    expected: FAIL


[current-task-signal-async-priority.any.html]
  [Test that currentTaskSignal works through async promise resolution]
    expected: FAIL
