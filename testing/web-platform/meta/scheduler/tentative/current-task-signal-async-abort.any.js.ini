[current-task-signal-async-abort.any.serviceworker.html]
  [Test that currentTaskSignal works through promise resolution when aborting tasks]
    expected: FAIL


[current-task-signal-async-abort.any.html]
  [Test that currentTaskSignal works through promise resolution when aborting tasks]
    expected: FAIL


[current-task-signal-async-abort.any.sharedworker.html]
  expected:
    if (os == "linux") and not debug and (processor == "x86_64") and not fission: [OK, TIMEOUT]
    if (os == "linux") and not debug and (processor == "x86_64") and fission: [OK, TIMEOUT]
    if (os == "linux") and not debug and (processor == "x86"): [OK, TIMEOUT]
    if (os == "android") and debug and not swgl: [OK, TIMEOUT]
  [Test that currentTaskSignal works through promise resolution when aborting tasks]
    expected: FAIL


[current-task-signal-async-abort.any.worker.html]
  [Test that currentTaskSignal works through promise resolution when aborting tasks]
    expected: FAIL
