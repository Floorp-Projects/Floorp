[content-dpr-various-elements.html]
  expected:
    if (os == "win") and webrender and not debug: ["FAIL", "TIMEOUT"]
    if (os == "linux") and debug and webrender: ["FAIL", "TIMEOUT"]
    if (os == "mac") and debug: ["TIMEOUT", "FAIL"]
    FAIL
