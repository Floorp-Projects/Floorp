[webkit-animation-iteration-event.html]
  expected:
    if os == "android": OK
    if os == "mac": OK
    [OK, TIMEOUT]
  [webkitAnimationIteration event listener is case sensitive]
    expected:
      if (os == "linux") and not debug and fission: [PASS, NOTRUN]
      if (os == "win") and debug and fission: [PASS, FAIL, TIMEOUT, NOTRUN]
      if os == "mac": PASS
      if os == "android": PASS
      [PASS, FAIL, NOTRUN]

  [onwebkitanimationiteration event handler should trigger for an animation]
    expected: [PASS, TIMEOUT]

  [webkitAnimationIteration event listener should trigger for an animation]
    expected:
      if debug and fission and domstreams: [PASS, TIMEOUT, NOTRUN]
      if debug and fission and not domstreams: [PASS, TIMEOUT, NOTRUN]
      [PASS, NOTRUN]

  [webkitAnimationIteration event listener should not trigger if an unprefixed listener also exists]
    expected:
      if (os == "linux") and domstreams and debug and fission: [PASS, TIMEOUT, NOTRUN]
      if (os == "linux") and not domstreams: [PASS, TIMEOUT, NOTRUN]
      if os == "mac": PASS
      if os == "android": PASS
      [PASS, NOTRUN]

  [onwebkitanimationiteration event handler should not trigger if an unprefixed event handler also exists]
    expected: [PASS, TIMEOUT, NOTRUN]

  [event types for prefixed and unprefixed animationiteration event handlers should be named appropriately]
    expected:
      if domstreams and (os == "linux") and debug and fission: [PASS, TIMEOUT, NOTRUN]
      if not domstreams: [PASS, TIMEOUT, NOTRUN]
      [PASS, NOTRUN]

  [event types for prefixed and unprefixed animationiteration event listeners should be named appropriately]
    expected:
      if (os == "linux") and domstreams and debug and not fission: [PASS, NOTRUN]
      if (os == "linux") and domstreams and debug and fission: [PASS, TIMEOUT, NOTRUN]
      if (os == "linux") and domstreams and not debug: [PASS, NOTRUN]
      if (os == "win") and debug: [PASS, NOTRUN]
      if (os == "linux") and not domstreams: [PASS, TIMEOUT, NOTRUN]

  [webkitAnimationIteration event listener should not trigger if an unprefixed event handler also exists]
    expected:
      if domstreams and (os == "linux") and debug and fission: [PASS, TIMEOUT, NOTRUN]
      if not domstreams: [PASS, TIMEOUT, NOTRUN]
      [PASS, NOTRUN]

  [onwebkitanimationiteration event handler should not trigger if an unprefixed listener also exists]
    expected:
      if domstreams and (os == "linux") and debug and fission: [PASS, TIMEOUT, NOTRUN]
      if not domstreams: [PASS, TIMEOUT, NOTRUN]
      [PASS, NOTRUN]
