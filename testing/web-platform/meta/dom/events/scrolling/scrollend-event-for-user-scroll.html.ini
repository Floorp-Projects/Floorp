[scrollend-event-for-user-scroll.html]
  expected:
    if (os == "linux") and (asan or debug): TIMEOUT
    if not debug and (os == "linux"): TIMEOUT
    if (os == "win") and not debug and not webrender and (processor == "x86_64"): [TIMEOUT, ERROR]
    if not debug and (os == "mac") and not webrender: TIMEOUT
    if (os == "mac") and debug: ERROR
    if (os == "win") and debug: ERROR
    if not debug and (os == "win") and not webrender and (processor == "x86"): TIMEOUT
    if os == "android": OK
  [Tests that the target_div gets scrollend event when send DOWN key to target.]
    expected:
      if os == "android": FAIL
      if os == "linux" and (asan or debug): NOTRUN
      if not debug and (os == "linux"): NOTRUN
      if not debug and (os == "mac") and not webrender: NOTRUN
      if not debug and (os == "win") and not webrender and (processor == "x86"): NOTRUN

  [Tests that the target_div gets scrollend event when click scrollbar on target.]
    expected:
      if not debug and (os == "linux") and (processor == "x86"): [NOTRUN, FAIL]
      if not debug and (os == "linux") and (processor == "x86_64"): [FAIL, NOTRUN, TIMEOUT]
      if not debug and (os == "mac") and not webrender: NOTRUN
      if not debug and (os == "win") and not webrender and (processor == "x86"): [NOTRUN, FAIL]
      if debug: FAIL
      FAIL

  [Tests that the target_div gets scrollend event when drag the thumb of target.]
    expected:
      if (os == "linux") and not debug and (processor == "x86"): [NOTRUN, TIMEOUT]
      if (os == "linux") and not debug and (processor == "x86_64"): [TIMEOUT, NOTRUN]
      if (os == "linux") and debug: TIMEOUT
      if not debug and (os == "mac") and not webrender: NOTRUN
      if not debug and (os == "win") and not webrender and (processor == "x86"): [NOTRUN, FAIL]
      if os == "android": FAIL

  [Tests that the target_div gets scrollend event when dragging scroll on target.]
    expected:
      if not debug and (os == "linux") and (processor == "x86"): [TIMEOUT, FAIL]
      if not debug and (os == "linux") and (processor == "x86_64"): [FAIL, TIMEOUT]
      if not debug and (os == "mac") and not webrender: TIMEOUT
      if not debug and (os == "win") and not webrender and (processor == "x86"): [FAIL, TIMEOUT]
      if debug: FAIL
      FAIL

