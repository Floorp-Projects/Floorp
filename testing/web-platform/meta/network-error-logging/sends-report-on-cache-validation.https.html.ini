[sends-report-on-cache-validation.https.html]
  expected:
    if os == "mac" or os == "win": ["OK", "TIMEOUT"]  # Bug 1643177
  [\n    Test that NEL reports are sent for cache validation requests\n  ]
    expected:
      if os == "mac" or os == "win": ["FAIL", "TIMEOUT"]  # Bug 1643177
      FAIL

