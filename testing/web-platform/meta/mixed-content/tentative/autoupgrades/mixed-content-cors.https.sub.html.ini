[mixed-content-cors.https.sub.html]
  expected:
    if (os == "linux") and not swgl and not debug and not tsan and asan and not fission: [OK, TIMEOUT]
    if (os == "linux") and not swgl and not debug and not tsan and not asan: [OK, TIMEOUT]
    if (os == "linux") and swgl and fission: [OK, TIMEOUT]
    if (os == "linux") and swgl and not fission: [OK, TIMEOUT]
    if (os == "android") and not debug: [OK, TIMEOUT]
  [Cross-Origin audio should get upgraded even if CORS is set]
    expected:
      if (os == "linux") and not swgl and not debug and not tsan and asan and not fission: [PASS, TIMEOUT]
      if (os == "linux") and not swgl and not debug and not tsan and not asan: [PASS, TIMEOUT]
      if (os == "linux") and swgl: [PASS, TIMEOUT]
      if (os == "android") and not debug: [PASS, TIMEOUT]

  [Cross-Origin video should get upgraded even if CORS is set]
    expected:
      if (os == "linux") and swgl and fission: [PASS, TIMEOUT]
      if (os == "android") and not debug: [PASS, TIMEOUT]
