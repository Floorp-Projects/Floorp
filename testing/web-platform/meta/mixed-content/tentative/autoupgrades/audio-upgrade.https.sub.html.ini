[audio-upgrade.https.sub.html]
  expected:
    if (os == "linux") and not fission and not webrender: OK
    if (os == "linux") and fission: OK
    if os == "mac": OK
    [OK, TIMEOUT]
  [Audio autoupgraded]
    expected:
      if (os == "linux") and not fission and not webrender: PASS
      if (os == "linux") and fission: PASS
      if os == "mac": PASS
      [PASS, TIMEOUT]
