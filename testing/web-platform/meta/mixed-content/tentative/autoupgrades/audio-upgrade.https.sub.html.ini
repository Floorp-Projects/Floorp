[audio-upgrade.https.sub.html]
  disabled:
    if (os == "android") and debug: https://bugzilla.mozilla.org/show_bug.cgi?id=1401570
  expected:
    if (os == "linux") and not debug and fission and (processor == "x86_64") and not asan and not tsan: [TIMEOUT, OK]
    if (os == "linux") and debug and fission and not swgl: [TIMEOUT, OK]
    if os == "mac": OK
    if os == "android": [OK, ERROR, TIMEOUT]
    [OK, TIMEOUT]
  [Audio autoupgraded]
    expected:
      if (os == "linux") and fission: PASS
      if os == "mac": PASS
      [PASS, TIMEOUT]

  [Audio of other host autoupgraded]
    expected:
      if (os == "linux") and fission and (processor == "x86_64") and not swgl and not debug and not asan and not tsan: [TIMEOUT, PASS]
      if (os == "linux") and fission and (processor == "x86_64") and not swgl and debug: [TIMEOUT, PASS]
      if (os == "win") and not swgl: PASS
      if os == "mac": PASS
      if os == "android": PASS
      [PASS, TIMEOUT]
