[audio-upgrade.https.sub.html]
  expected:
    if (os == "linux") and not debug and webrender and fission: OK
    if (os == "linux") and not debug and not webrender: OK
    if (os == "linux") and debug: OK
    if os == "mac": OK
    [OK, TIMEOUT]
  [Audio autoupgraded]
    expected:
      if (os == "linux") and not debug and webrender and fission: PASS
      if (os == "linux") and not debug and not webrender: PASS
      if (os == "linux") and debug: PASS
      if os == "mac": PASS
      [PASS, TIMEOUT]

