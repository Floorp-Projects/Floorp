[cache-storage-reporting-shared-worker.https.html]
  expected:
    if (os == "win") and debug and (processor == "x86_64") and webrender: ["CRASH", "TIMEOUT"]
    if (os == "win") and debug and (processor == "x86_64") and not webrender: ["TIMEOUT", "CRASH"]
    TIMEOUT
  [COEP-Report-Only support on SharedWorker.]
    expected: NOTRUN

  [COEP support on SharedWorker.]
    expected: TIMEOUT

