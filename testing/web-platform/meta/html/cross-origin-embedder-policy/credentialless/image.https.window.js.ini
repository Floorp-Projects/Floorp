[image.https.window.html]
  [image cross-origin + anonymous]
    expected:
      if (os == "linux") and not editorLegacyDirectionMode and swgl and fission: PASS
      if (os == "linux") and editorLegacyDirectionMode: PASS
      if (os == "android") and debug: PASS
      if os == "mac": PASS
      if os == "win": PASS
      [PASS, FAIL]

  [image cross-origin + use-credentials]
    expected:
      if (os == "linux") and debug and fission and not swgl and not editorLegacyDirectionMode: [PASS, FAIL]
      if (os == "linux") and debug and not fission: [PASS, FAIL]
      if (os == "linux") and not debug: [PASS, FAIL]
