[none.https.html]
  expected:
    if (os == "android") and not nightly_build: [OK, TIMEOUT]
    if processor == "x86": [OK, TIMEOUT]
  ["none" top-level: navigating a frame from "require-corp" to "none" should succeed]
    expected:
      if (os == "android") and not nightly_build: [PASS, TIMEOUT]

  ["none" top-level: navigating a frame back from "require-corp" should succeed]
    expected:
      if processor == "x86": [PASS, TIMEOUT]
