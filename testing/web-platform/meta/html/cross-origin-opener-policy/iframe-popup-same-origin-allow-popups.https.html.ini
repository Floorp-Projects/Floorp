[iframe-popup-same-origin-allow-popups.https.html]
  expected:
    if debug and (os == "linux") and webrender and fission: ["OK", "TIMEOUT"]
    if debug and (os == "linux") and webrender and not fission: ["OK", "TIMEOUT"]
  [same-origin-allow-popups with SAME_ORIGIN iframe opening popup a SAME_ORIGIN with COOP: ]
    expected:
      if debug and (os == "linux") and webrender and not fission: ["PASS", "TIMEOUT"]
      if debug and (os == "linux") and webrender and fission: ["PASS", "TIMEOUT"]

  [same-origin-allow-popups with CROSS_ORIGIN iframe opening popup a SAME_SITE with COOP: same-origin-allow-popups]
    expected:
      if debug and (os == "linux") and webrender and fission: ["FAIL", "TIMEOUT"]
      if debug and (os == "linux") and webrender and not fission: ["FAIL", "TIMEOUT"]
      FAIL

  [same-origin-allow-popups with SAME_SITE iframe opening popup a SAME_ORIGIN with COOP: same-origin]
    expected:
      if fission and debug: ["PASS", "TIMEOUT"]

  [same-origin-allow-popups with CROSS_ORIGIN iframe opening popup a SAME_ORIGIN with COOP: same-origin-allow-popups]
    expected:
      if debug and (os == "linux") and webrender and fission: ["FAIL", "TIMEOUT"]
      if debug and (os == "linux") and webrender and not fission: ["FAIL", "TIMEOUT"]
      FAIL

  [same-origin-allow-popups with SAME_SITE iframe opening popup a CROSS_ORIGIN with COOP: ]
    expected:
      if debug and (os == "linux") and webrender and not fission: ["PASS", "TIMEOUT"]
      if debug and (os == "linux") and webrender and fission: ["PASS", "TIMEOUT"]

  [same-origin-allow-popups with SAME_SITE iframe opening popup a CROSS_ORIGIN with COOP: same-origin-allow-popups]
    expected:
      if debug and (os == "linux") and webrender and fission: ["FAIL", "TIMEOUT"]
      if debug and (os == "linux") and webrender and not fission: ["FAIL", "TIMEOUT"]
      FAIL

  [same-origin-allow-popups with CROSS_ORIGIN iframe opening popup a SAME_SITE with COOP: ]
    expected:
      if debug and (os == "linux") and webrender and not fission: ["PASS", "TIMEOUT"]
      if debug and (os == "linux") and webrender and fission: ["PASS", "TIMEOUT"]

  [same-origin-allow-popups with CROSS_ORIGIN iframe opening popup a CROSS_ORIGIN with COOP: same-origin-allow-popups]
    expected:
      if debug and (os == "linux") and webrender and not fission: ["FAIL", "TIMEOUT"]
      FAIL

  [same-origin-allow-popups with SAME_SITE iframe opening popup a SAME_SITE with COOP: same-origin]
    expected:
      if fission and debug: ["PASS", "TIMEOUT"]

  [same-origin-allow-popups with SAME_SITE iframe opening popup a SAME_SITE with COOP: ]
    expected:
      if debug and (os == "linux") and webrender and fission: ["PASS", "TIMEOUT"]
      if debug and (os == "linux") and webrender and not fission: ["PASS", "TIMEOUT"]

  [same-origin-allow-popups with CROSS_ORIGIN iframe opening popup a SAME_ORIGIN with COOP: ]
    expected:
      if debug and (os == "linux") and webrender and fission: ["PASS", "TIMEOUT"]
      if debug and (os == "linux") and webrender and not fission: ["PASS", "TIMEOUT"]

  [same-origin-allow-popups with SAME_ORIGIN iframe opening popup a SAME_SITE with COOP: same-origin-allow-popups]
    expected:
      if debug and (os == "linux") and webrender and not fission: ["FAIL", "TIMEOUT"]
      if debug and (os == "linux") and webrender and fission: ["FAIL", "TIMEOUT"]
      FAIL

  [same-origin-allow-popups with SAME_SITE iframe opening popup a SAME_ORIGIN with COOP: ]
    expected:
      if debug and (os == "linux") and webrender and fission: ["PASS", "TIMEOUT"]
      if debug and (os == "linux") and webrender and not fission: ["PASS", "TIMEOUT"]

  [same-origin-allow-popups with SAME_ORIGIN iframe opening popup a SAME_ORIGIN with COOP: same-origin-allow-popups]
    expected:
      if debug and (os == "linux") and webrender and not fission: ["PASS", "TIMEOUT"]
      if debug and (os == "linux") and webrender and fission: ["PASS", "TIMEOUT"]

  [same-origin-allow-popups with SAME_ORIGIN iframe opening popup a CROSS_ORIGIN with COOP: ]
    expected:
      if debug and (os == "linux") and webrender and fission: ["PASS", "TIMEOUT"]
      if debug and (os == "linux") and webrender and not fission: ["PASS", "TIMEOUT"]

  [same-origin-allow-popups with SAME_ORIGIN iframe opening popup a CROSS_ORIGIN with COOP: same-origin-allow-popups]
    expected:
      if debug and (os == "linux") and webrender and fission: ["FAIL", "TIMEOUT"]
      if debug and (os == "linux") and webrender and not fission: ["FAIL", "TIMEOUT"]
      FAIL

  [same-origin-allow-popups with SAME_SITE iframe opening popup a SAME_SITE with COOP: same-origin-allow-popups]
    expected:
      if debug and (os == "linux") and webrender and fission: ["FAIL", "TIMEOUT"]
      if debug and (os == "linux") and webrender and not fission: ["FAIL", "TIMEOUT"]
      FAIL

  [same-origin-allow-popups with SAME_ORIGIN iframe opening popup a SAME_SITE with COOP: ]
    expected:
      if debug and (os == "linux") and webrender and not fission: ["PASS", "TIMEOUT"]
      if debug and (os == "linux") and webrender and fission: ["PASS", "TIMEOUT"]

  [same-origin-allow-popups with SAME_SITE iframe opening popup a SAME_ORIGIN with COOP: same-origin-allow-popups]
    expected:
      if debug and (os == "linux") and webrender and fission: ["FAIL", "TIMEOUT"]
      if debug and (os == "linux") and webrender and not fission: ["FAIL", "TIMEOUT"]
      FAIL

