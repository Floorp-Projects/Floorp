[navigate-top-to-aboutblank.https.html]
  expected:
    if (os == "win") and not fission and debug and webrender and (processor == "x86_64") and not swgl: [TIMEOUT, OK, CRASH]
    if (os == "win") and not fission and debug and webrender and (processor == "x86_64") and swgl: [OK, TIMEOUT, CRASH]
    if (os == "linux") and webrender and not swgl and not fission and not debug: [TIMEOUT, OK, CRASH]
    if (os == "linux") and not webrender and not fission and not debug and (processor == "x86_64"): [OK, CRASH, TIMEOUT]
    if (os == "linux") and not webrender and not fission and not debug and (processor == "x86"): [OK, TIMEOUT, CRASH]
    if (os == "win") and not fission and debug and webrender and (processor == "x86"): [OK, TIMEOUT, CRASH]
    if (os == "linux") and not webrender and not fission and debug: [OK, ERROR, TIMEOUT, CRASH]
    if (os == "win") and not fission and debug and not webrender: [TIMEOUT, OK, CRASH]
    if (os == "linux") and webrender and swgl: [OK, ERROR, TIMEOUT, CRASH]
    if (os == "linux") and not webrender and fission: [OK, CRASH, TIMEOUT]
    if (os == "win") and not fission and not debug: [OK, TIMEOUT, CRASH]
    if (os == "win") and fission and debug: [CRASH, OK, TIMEOUT]
    if (os == "win") and fission and not debug: [OK, TIMEOUT, CRASH]
    if os == "mac": [OK, TIMEOUT, CRASH]
    [OK, CRASH]
  [Navigate top to about:blank from iframe with opener COOP: |header(Cross-Origin-Opener-Policy,same-origin), iframe origin: https://www1.web-platform.test:8443]
    expected: FAIL

  [Navigate top to about:blank from iframe with opener COOP: |header(Cross-Origin-Opener-Policy,same-origin), iframe origin: https://web-platform.test:8443]
    expected: FAIL

  [Navigate top to about:blank from iframe with opener COOP: |header(Cross-Origin-Opener-Policy,same-origin-allow-popups), iframe origin: https://www1.web-platform.test:8443]
    expected: FAIL
