[resource-popup.https.html]
  expected:
    if (os == "win") and fission and debug: ["TIMEOUT", "OK"]
    if (os == "linux") and not webrender and not fission: ["OK", "TIMEOUT", "CRASH"]
    if (os == "linux") and not webrender and fission: ["OK", "TIMEOUT", "CRASH"]
    [OK, TIMEOUT]
  [/common/dummy.xml - parent COOP: ""; child COOP: "same-origin"]
    expected:
      if (os == "android") and webrender and not swgl and not debug: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "android") and webrender and not swgl and debug: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "android") and webrender and swgl: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "android") and not webrender: ["FAIL", "PASS", "TIMEOUT"]
      [PASS, FAIL, TIMEOUT]

  [/images/red.png - parent COOP: ""; child COOP: "same-origin"]
    expected:
      if (os == "android") and not swgl and debug and not webrender: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "android") and not swgl and debug and webrender: ["FAIL", "PASS", "TIMEOUT"]
      [PASS, FAIL, TIMEOUT]

  [/media/2x2-green.mp4 - parent COOP: ""; child COOP: "same-origin"]
    expected:
      if fission and webrender and (os == "win") and debug: ["TIMEOUT", "PASS"]
      [PASS, TIMEOUT]

  [/common/text-plain.txt - parent COOP: ""; child COOP: "same-origin"]
    expected: [PASS, FAIL, TIMEOUT]

