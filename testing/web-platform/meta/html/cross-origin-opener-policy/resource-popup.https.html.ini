[resource-popup.https.html]
  expected:
    if debug and (os == "linux") and not fission and swgl: [OK, ERROR, TIMEOUT]
    if debug and (os == "linux") and fission and not swgl: [TIMEOUT, OK, ERROR]
    if debug and (os == "linux") and fission and swgl: [TIMEOUT, OK, ERROR]
    if debug and (os == "win") and (processor == "x86_64") and not swgl: [TIMEOUT, OK, ERROR]
    if debug and (os == "win") and (processor == "x86_64") and swgl: [OK, ERROR, TIMEOUT]
    if debug and (os == "android"): [OK, ERROR, TIMEOUT]
    [OK, TIMEOUT]
  [/common/dummy.xml - parent COOP: ""; child COOP: "same-origin"]
    expected:
      if (os == "android") and not swgl and debug: [FAIL, PASS, TIMEOUT]
      if (os == "android") and swgl: [FAIL, PASS, TIMEOUT]
      [PASS, FAIL, TIMEOUT]

  [/images/red.png - parent COOP: ""; child COOP: "same-origin"]
    expected:
      if (os == "android") and debug and not swgl: [FAIL, PASS, TIMEOUT]
      [PASS, FAIL, TIMEOUT]

  [/media/2x2-green.mp4 - parent COOP: ""; child COOP: "same-origin"]
    expected:
      if (os == "win") and debug and (processor == "x86_64") and not swgl: [TIMEOUT, PASS]
      if (os == "linux") and not swgl and fission and debug: [TIMEOUT, PASS]
      if (os == "linux") and swgl and fission: [TIMEOUT, PASS]
      [PASS, TIMEOUT]

  [/common/text-plain.txt - parent COOP: ""; child COOP: "same-origin"]
    expected: [PASS, FAIL, TIMEOUT]

  [/common/text-plain.txt - parent COOP: "same-origin"; child COOP: "same-origin"]
    expected:
      if (os == "linux") and not fission: [PASS, FAIL]
