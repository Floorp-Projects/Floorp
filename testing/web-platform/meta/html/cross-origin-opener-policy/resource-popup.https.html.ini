[resource-popup.https.html]
  expected:
    if (os == "linux") and not swgl and (processor == "x86_64") and fission and not debug: [TIMEOUT, OK]
    if (os == "linux") and not swgl and (processor == "x86_64") and fission and debug: [TIMEOUT, OK]
    if (os == "linux") and not swgl and (processor == "x86_64") and not fission and not debug: [OK, TIMEOUT]
    if (os == "linux") and not swgl and (processor == "x86_64") and not fission and debug: [OK, TIMEOUT]
    if (os == "linux") and swgl and not fission: [TIMEOUT, OK, ERROR]
    if (os == "linux") and not swgl and (processor == "x86"): [OK, TIMEOUT]
    if (os == "win") and (processor == "x86"): [OK, TIMEOUT]
    if os == "mac": [OK, TIMEOUT]
    [OK, ERROR, TIMEOUT]
  [/common/dummy.xml - parent COOP: ""; child COOP: "same-origin"]
    expected:
      if (os == "win") and not swgl and debug and (processor == "x86_64"): [FAIL, PASS, TIMEOUT]
      if (os == "win") and swgl: [FAIL, PASS, TIMEOUT]
      if os == "android": [FAIL, PASS, TIMEOUT]
      [PASS, FAIL, TIMEOUT]

  [/images/red.png - parent COOP: ""; child COOP: "same-origin"]
    expected:
      if os == "android": [FAIL, PASS, TIMEOUT]
      [PASS, FAIL, TIMEOUT]

  [/media/2x2-green.mp4 - parent COOP: ""; child COOP: "same-origin"]
    expected:
      if (os == "linux") and not swgl and (processor == "x86_64") and fission and debug: [TIMEOUT, PASS]
      if (os == "linux") and not swgl and (processor == "x86_64") and fission and not debug: [TIMEOUT, PASS]
      if (os == "linux") and swgl and not fission: [TIMEOUT, PASS]
      [PASS, TIMEOUT]

  [/common/text-plain.txt - parent COOP: ""; child COOP: "same-origin"]
    expected:
      if (os == "android") and not swgl and debug: [FAIL, PASS, TIMEOUT]
      if (os == "android") and not swgl and not debug: [FAIL, PASS, TIMEOUT]
      [PASS, FAIL, TIMEOUT]

  [/common/text-plain.txt - parent COOP: "same-origin"; child COOP: "same-origin"]
    expected:
      if (os == "linux") and not fission: [PASS, FAIL]
