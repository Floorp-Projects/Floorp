[popup-same-origin-allow-popups-with-cross-origin.https.html]
  expected:
    if (os == "win") and not debug and not fission and (processor == "x86_64"): [TIMEOUT, OK, CRASH]
    if (os == "linux") and fission and not debug and webrender: [OK, CRASH]
    if (os == "mac") and debug: [TIMEOUT, OK, CRASH]
    [OK, TIMEOUT, CRASH]
