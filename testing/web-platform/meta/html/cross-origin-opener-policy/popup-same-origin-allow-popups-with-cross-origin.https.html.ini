[popup-same-origin-allow-popups-with-cross-origin.https.html]
  expected:
    if (os == "linux") and (processor == "x86_64") and not fission and debug and not webrender: [OK, CRASH]
    if (os == "linux") and (processor == "x86_64") and fission and not debug: [OK, CRASH]
    if (os == "win") and not fission and (processor == "x86_64") and not swgl: [TIMEOUT, OK, CRASH]
    if (os == "linux") and (processor == "x86"): [TIMEOUT, OK, CRASH]
    [OK, TIMEOUT, CRASH]
