[popup-same-origin-allow-popups-with-same-site.https.html]
  expected:
    if (os == "android") and not fission and not debug: [OK, TIMEOUT, CRASH]
    if (os == "android") and not fission and debug: [OK, ERROR, TIMEOUT, CRASH]
    if (os == "android") and fission: [OK, TIMEOUT, ERROR, CRASH]
