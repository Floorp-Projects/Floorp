[no-https.html]
  expected:
    if not debug and (os == "linux") and (processor == "x86"): [OK, TIMEOUT]
    if not debug and (os == "linux") and (processor == "x86_64"): [OK, TIMEOUT]
    if not debug and (os == "mac"): [OK, TIMEOUT]
    if not debug and (os == "win"): [OK, TIMEOUT]
