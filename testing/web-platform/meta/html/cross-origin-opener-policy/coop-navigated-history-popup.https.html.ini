[coop-navigated-history-popup.https.html]
  expected:
    if processor == "aarch64": OK
    TIMEOUT
  [Popup navigating to other origin with COOP: same-origin and back in history]
    expected:
      if processor == "aarch64": PASS
      TIMEOUT

