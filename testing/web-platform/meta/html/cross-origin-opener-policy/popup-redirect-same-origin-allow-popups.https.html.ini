[popup-redirect-same-origin-allow-popups.https.html]
  expected:
    if (os == "linux") and debug and webrender and fission: [ERROR, CRASH]
    if (os == "linux") and debug and webrender and not fission: [OK, ERROR, CRASH]
    if (os == "linux") and debug and not webrender: [OK, ERROR, CRASH]
    if (os == "linux") and not debug: [OK, ERROR, CRASH]
  [Cross origin popup redirects to same-origin with same-origin-allow-popups]
    expected: FAIL

  [Same origin popup redirects to same-origin with same-origin-allow-popups]
    expected: FAIL

