[intrinsic_sizes.htm]
  [default object size after src is removed]
    expected:
      if fission and (processor == "x86_64") and debug and not swgl and (os == "linux"): [FAIL, PASS]
      if fission and (processor == "x86_64") and debug and not swgl and (os == "win"): [FAIL, PASS]
      if not fission and debug and (os == "android") and not swgl: [FAIL, PASS]
      if fission and (processor == "x86_64") and not debug and (os == "win"): [FAIL, PASS]
      if fission and (processor == "x86_64") and not debug and (os == "linux"): [FAIL, PASS]
      if fission and (processor == "x86_64") and not debug and (os == "mac"): [PASS, FAIL]
      FAIL

  [default object size after poster is removed]
    expected: FAIL
