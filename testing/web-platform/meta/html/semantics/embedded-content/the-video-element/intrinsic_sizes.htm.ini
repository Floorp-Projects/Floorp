[intrinsic_sizes.htm]
  [default object size after src is removed]
    expected:
      if fission and debug and not swgl and (os == "win") and (processor == "x86_64"): [FAIL, PASS]
      if fission and debug and not swgl and (os == "linux"): [FAIL, PASS]
      if not fission and (os == "android") and debug and not swgl: [FAIL, PASS]
      if fission and not debug and (os == "linux"): [FAIL, PASS]
      if fission and not debug and (os == "mac"): [FAIL, PASS]
      if fission and not debug and (os == "win"): [FAIL, PASS]
      FAIL

  [default object size after poster is removed]
    expected: FAIL
