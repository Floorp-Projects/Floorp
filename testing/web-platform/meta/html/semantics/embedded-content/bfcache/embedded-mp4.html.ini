[embedded-mp4.html]
  disabled:
    if os == "win": https://bugzilla.mozilla.org/show_bug.cgi?id=1845961
    if os == "mac": https://bugzilla.mozilla.org/show_bug.cgi?id=1845961
  expected:
    if (os == "win") and not debug and ccov: OK
    if (os == "linux") and not debug and not asan and not tsan: [TIMEOUT, OK]
    if (os == "android") and debug: OK
    [OK, TIMEOUT]
  [Page with <embed type=undefined src=/media/white.mp4>]
    expected:
      if not fission and (os == "linux"): [PASS, PRECONDITION_FAILED]
      if not fission and (os == "android"): [PRECONDITION_FAILED, PASS]

  [Page with <object type=undefined data=/media/white.mp4>]
    expected:
      if (os == "win") and not debug and ccov: PASS
      if (os == "linux") and not asan and debug and not fission: [PASS, TIMEOUT, PRECONDITION_FAILED]
      if (os == "linux") and not asan and not debug and not tsan: [TIMEOUT, PASS]
      if (os == "linux") and asan and not fission: [PASS, PRECONDITION_FAILED, TIMEOUT]
      if (os == "android") and debug: [PRECONDITION_FAILED, PASS]
      if (os == "android") and not debug: [PASS, PRECONDITION_FAILED]
      [PASS, TIMEOUT]
