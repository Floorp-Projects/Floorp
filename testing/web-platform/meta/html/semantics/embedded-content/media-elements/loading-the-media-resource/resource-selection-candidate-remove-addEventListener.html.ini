[resource-selection-candidate-remove-addEventListener.html]
  expected:
    if (os == "linux") and not fission and not swgl and not debug: [OK, TIMEOUT]
    if (os == "linux") and not fission and not swgl and debug: [TIMEOUT, OK]
    if (os == "linux") and not fission and swgl: [TIMEOUT, OK]
    if (os == "linux") and fission and not debug: [TIMEOUT, OK]
    TIMEOUT
  [removing the candidate source, addEventListener]
    expected:
      if (os == "linux") and not fission and not swgl and not debug: [PASS, TIMEOUT]
      if (os == "linux") and not fission and not swgl and debug: [TIMEOUT, PASS]
      if (os == "linux") and not fission and swgl: [TIMEOUT, PASS]
      if (os == "linux") and fission and not debug: [TIMEOUT, PASS]
      TIMEOUT
