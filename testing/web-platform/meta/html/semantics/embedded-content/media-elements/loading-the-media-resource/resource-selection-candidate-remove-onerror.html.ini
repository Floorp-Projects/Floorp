[resource-selection-candidate-remove-onerror.html]
  expected:
    if (os == "linux") and not swgl and fission and not debug and (processor == "x86_64"): [TIMEOUT, OK]
    if (os == "linux") and not swgl and fission and not debug and (processor == "x86"): [TIMEOUT, OK]
    if (os == "linux") and not swgl and fission and debug: [TIMEOUT, OK]
    if (os == "linux") and not swgl and not fission: [TIMEOUT, OK]
    TIMEOUT
  [removing the candidate source, onerror]
    expected:
      if (os == "linux") and not swgl and fission and not debug and (processor == "x86_64"): [TIMEOUT, PASS]
      if (os == "linux") and not swgl and fission and not debug and (processor == "x86"): [TIMEOUT, PASS]
      if (os == "linux") and not swgl and fission and debug: [TIMEOUT, PASS]
      if (os == "linux") and not swgl and not fission: [TIMEOUT, PASS]
      TIMEOUT
