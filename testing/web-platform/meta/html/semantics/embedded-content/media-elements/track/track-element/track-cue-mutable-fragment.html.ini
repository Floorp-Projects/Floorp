[track-cue-mutable-fragment.html]
  expected:
    if (os == "linux") and debug and not editorLegacyDirectionMode and fission and not swgl: [OK, TIMEOUT]
    if (os == "linux") and debug and editorLegacyDirectionMode: [OK, TIMEOUT]
    if (os == "linux") and not debug: [OK, TIMEOUT]
  [Cue fragment is mutable]
    expected:
      if (os == "linux") and debug and not editorLegacyDirectionMode and fission and not swgl: [PASS, TIMEOUT]
      if (os == "linux") and debug and editorLegacyDirectionMode: [PASS, TIMEOUT]
      if (os == "linux") and not debug: [PASS, TIMEOUT]
