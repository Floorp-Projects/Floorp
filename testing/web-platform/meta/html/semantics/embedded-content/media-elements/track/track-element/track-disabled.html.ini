[track-disabled.html]
  expected:
    if (processor == "x86_64") and webrender and not fission and (os == "win") and not debug: ["OK", "TIMEOUT"]
    if processor == "aarch64": ["OK", "TIMEOUT"]
  [Disabling a track]
    expected:
      if processor == "aarch64": ["PASS", "TIMEOUT"]

