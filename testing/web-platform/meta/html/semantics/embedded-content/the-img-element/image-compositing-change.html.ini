[image-compositing-change.html]
  expected:
    if not webrender and (os == "linux") and not fission: [TIMEOUT, PASS]
    if not webrender and (os == "linux") and fission: [TIMEOUT, PASS]
    if not webrender and (os == "mac"): [TIMEOUT, PASS]
