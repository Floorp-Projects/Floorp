[image-compositing-change.html]
  expected:
    if not webrender and not debug and (processor == "x86_64") and (os == "linux"): ["PASS", "TIMEOUT"]
    if not webrender and not debug and (processor == "x86_64") and (os == "win"): ["TIMEOUT", "PASS"]
    if not webrender and not debug and (processor == "x86_64") and (os == "android"): ["PASS", "TIMEOUT"]
    if not webrender and not debug and (processor == "x86"): ["PASS", "TIMEOUT"]
    if not webrender and debug: ["TIMEOUT", "PASS"]
