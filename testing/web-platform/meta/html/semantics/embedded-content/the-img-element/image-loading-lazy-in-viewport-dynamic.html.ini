[image-loading-lazy-in-viewport-dynamic.html]
  [Test that in viewport images with loading='lazy' and changed to loading='eager' do not block the window load event.]
    expected:
      if not fission and (os == "linux") and (processor == "x86_64") and not debug and not webrender: [FAIL, PASS]
      if not fission and (os == "win") and debug and (processor == "x86_64"): [FAIL, PASS]
      if not fission and (os == "linux") and (processor == "x86_64") and debug: [FAIL, PASS]
      if not fission and (os == "mac") and debug: [FAIL, PASS]
      if not fission and (os == "linux") and (processor == "x86"): [FAIL, PASS]
      if not fission and (os == "android"): [FAIL, PASS]
      [PASS, FAIL]
