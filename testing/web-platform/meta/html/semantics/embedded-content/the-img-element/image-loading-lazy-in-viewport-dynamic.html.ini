[image-loading-lazy-in-viewport-dynamic.html]
  [Test that in viewport images with loading='lazy' and changed to loading='eager' do not block the window load event.]
    expected:
      if not fission and (os == "win") and (processor == "x86_64") and debug and swgl: ["PASS", "FAIL"]
      if not fission and (os == "linux") and not webrender and not debug and (processor == "x86"): ["PASS", "FAIL"]
      if not fission and (os == "win") and (processor == "x86_64") and not debug: ["PASS", "FAIL"]
      if not fission and (os == "linux") and not webrender and debug: ["PASS", "FAIL"]
      if not fission and (os == "mac") and not debug: ["PASS", "FAIL"]
      if fission: ["PASS", "FAIL"]
      [FAIL, PASS]

