[image-loading-lazy-in-viewport-dynamic.html]
  [Test that in viewport images with loading='lazy' and changed to loading='eager' do not block the window load event.]
    expected:
      if not fission and not debug and (os == "linux") and not webrender and (processor == "x86_64"): [PASS, FAIL]
      if not fission and not debug and (os == "win"): [PASS, FAIL]
      if not fission and not debug and (os == "mac"): [PASS, FAIL]
      if fission: [PASS, FAIL]
      [FAIL, PASS]
