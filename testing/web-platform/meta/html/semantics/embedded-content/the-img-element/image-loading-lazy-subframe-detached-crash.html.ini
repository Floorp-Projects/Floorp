[image-loading-lazy-subframe-detached-crash.html]
  expected:
    if debug and (os == "win") and (processor == "x86_64") and not swgl: [PASS, TIMEOUT]
    if debug and (os == "win") and (processor == "x86_64") and swgl: TIMEOUT
    if debug and (os == "linux") and not swgl and not fission: [CRASH, ERROR]
    if debug and (os == "linux") and not swgl and fission: CRASH
    if debug and (os == "linux") and swgl and not fission: [PASS, ERROR, CRASH]
    if debug and (os == "win") and (processor == "x86"): TIMEOUT
    if debug and (os == "mac") and not swgl: [PASS, ERROR, CRASH]
    if debug and (os == "android"): CRASH
