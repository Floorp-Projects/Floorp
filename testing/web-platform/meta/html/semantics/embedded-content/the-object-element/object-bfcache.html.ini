[object-bfcache.html]
  expected:
    if os == "android": OK
    TIMEOUT
  [Page with <object type=text/html data=/404>]
    expected:
      if not debug and (os == "linux") and not fission: TIMEOUT
      if not debug and (os == "linux") and fission: [PASS, TIMEOUT]
      if not debug and (os == "win"): TIMEOUT

  [Page with <object type=undefined data=/resources/blank.html>]
    expected:
      if not debug and (os == "linux") and fission: [PASS, NOTRUN]
      if not debug and (os == "linux") and not fission: NOTRUN
      if not debug and (os == "win"): NOTRUN

  [Page with <object type=text/html data=undefined>]
    expected:
      if (os == "linux") and debug and not fission and not swgl: [PASS, TIMEOUT]
      if (os == "linux") and not debug and not fission: NOTRUN
      if (os == "linux") and not debug and fission: [NOTRUN, PASS, TIMEOUT]
      if (os == "win") and not debug: NOTRUN
      if (os == "win") and debug: TIMEOUT

  [Page with <object type=image/png data=/images/blue.png>]
    expected:
      if (os == "linux") and debug and not fission and not swgl: [TIMEOUT, PASS, NOTRUN]
      if (os == "linux") and debug and not fission and swgl: TIMEOUT
      if (os == "linux") and debug and fission: TIMEOUT
      if (os == "linux") and not debug and fission: [NOTRUN, TIMEOUT]
      if os == "android": PASS
      NOTRUN

  [Page with <object type=application/javascript data=/resources/test-only-api.js>]
    expected:
      if (os == "linux") and not fission and debug and not swgl: [NOTRUN, TIMEOUT]
      if os == "android": PASS
      NOTRUN

  [Page with <object type=undefined data=/media/white.mp4>]
    expected:
      if os == "android": PASS
      NOTRUN
