[embed-bfcache.html]
  expected:
    if os == "android": OK
    TIMEOUT
  [Page with <embed type=text/html src=/404>]
    expected:
      if (os == "win") and debug and not swgl and (processor == "x86_64"): [PASS, TIMEOUT]
      if (os == "win") and debug and not swgl and (processor == "x86"): [PASS, TIMEOUT]
      if (os == "linux") and not debug and fission: [PASS, TIMEOUT]
      if (os == "linux") and not debug and not fission: TIMEOUT
      if (os == "win") and not debug: TIMEOUT

  [Page with <embed type=undefined src=/resources/blank.html>]
    expected:
      if (os == "win") and debug and not swgl and (processor == "x86"): [PASS, NOTRUN]
      if (os == "win") and debug and not swgl and (processor == "x86_64"): [PASS, NOTRUN]
      if (os == "linux") and not debug and not fission: NOTRUN
      if (os == "linux") and not debug and fission: [PASS, NOTRUN]
      if (os == "win") and not debug: NOTRUN

  [Page with <embed type=text/html src=undefined>]
    expected:
      if (os == "win") and debug and not swgl and (processor == "x86"): [TIMEOUT, NOTRUN]
      if (os == "win") and debug and not swgl and (processor == "x86_64"): [TIMEOUT, NOTRUN]
      if (os == "linux") and debug and not fission and swgl: [PASS, TIMEOUT]
      if (os == "win") and debug and swgl: TIMEOUT
      if (os == "linux") and not debug and fission: [NOTRUN, PASS, TIMEOUT]
      if (os == "linux") and not debug and not fission: NOTRUN
      if (os == "win") and not debug: NOTRUN

  [Page with <embed type=image/png src=/images/blue.png>]
    expected:
      if (os == "linux") and debug and not fission and swgl: [TIMEOUT, NOTRUN]
      if (os == "linux") and debug and not fission and not swgl: TIMEOUT
      if (os == "linux") and debug and fission: TIMEOUT
      if (os == "linux") and not debug and fission: [NOTRUN, TIMEOUT]
      if os == "android": PASS
      NOTRUN

  [Page with <embed type=application/javascript src=/resources/test-only-api.js>]
    expected:
      if os == "android": PASS
      NOTRUN

  [Page with <embed type=undefined src=/media/white.mp4>]
    expected:
      if os == "android": PASS
      NOTRUN
