[update-the-rendering.html]
  ["Flush autofocus candidates" should be happen after the first animation frame callbacks, and before a resize event in the next iteration of window event loop.]
    expected:
      if not sw-e10s and debug and (os == "linux") and not webrender: [PASS, FAIL]
      if sw-e10s: [PASS, FAIL]
      if (os == "linux") and (processor == "x86_64") and debug and webrender: [PASS, FAIL]
      if (os == "linux") and (processor == "x86_64") and not debug and webrender: [FAIL, PASS]
      if (os == "linux") and (processor == "x86_64") and not debug and not webrender: [FAIL, PASS]
      if (os == "win") and debug and (processor == "x86"): [FAIL, PASS]
      if (os == "win") and (processor == "x86_64") and debug and webrender: ["FAIL", "PASS"]
      if (os == "win") and (processor == "x86_64") and debug and not webrender: ["FAIL", "PASS"]
      FAIL

