[form-validation-reportValidity.html]
  expected:
    if not swgl and (os == "linux") and not debug and fission and (processor == "x86_64"): [OK, CRASH]
    if not swgl and (os == "linux") and debug and not fission: [OK, TIMEOUT]
    if swgl and (os == "linux"): [OK, TIMEOUT, CRASH]
