[delay-load-event-2.html]
  expected:
    if not fission and (os == "android") and debug and swgl: [ERROR, OK]
    if not fission and (os == "android") and debug and not swgl: [ERROR, OK]
    if not fission and (os == "linux") and debug: [ERROR, OK]
    if not fission and (os == "android") and not debug: [ERROR, OK]
    [OK, ERROR]
