[charset-bom.any.worker.html]
  expected: ERROR

[charset-bom.any.html]
  expected: ERROR

[charset-bom.any.sharedworker.html]
  expected:
    if (os == "linux") and not fission and not debug and (processor == "x86_64"): [ERROR, TIMEOUT]
    if (os == "linux") and not fission and not debug and (processor == "x86"): [ERROR, TIMEOUT]
    if (os == "linux") and not fission and debug and not swgl: [ERROR, TIMEOUT]
    if (os == "linux") and not fission and debug and swgl: [ERROR, TIMEOUT]
    if (os == "linux") and fission and not debug: [ERROR, TIMEOUT]
    if (os == "linux") and fission and debug: [ERROR, TIMEOUT]
    if (os == "win") and (processor == "x86") and not debug: [ERROR, TIMEOUT]
    ERROR
