[basic.any.html]
  expected:
    if not debug and (os == "linux"): [FAIL, TIMEOUT, OK]
    if (os == "mac") and not debug: [FAIL, OK, TIMEOUT]
    if (os == "win") and not debug: [FAIL, OK, TIMEOUT]
    TIMEOUT

[basic.any.sharedworker.html]
  expected: TIMEOUT
  [import() should drain the microtask queue when fetching a new module]
    expected: TIMEOUT

  [import() should not drain the microtask queue when loading an already loaded module]
    expected: FAIL


[basic.any.worker.html]
  expected: [TIMEOUT, OK]
  [import() should not drain the microtask queue when loading an already loaded module]
    expected: FAIL

  [import() should drain the microtask queue when fetching a new module]
    expected: TIMEOUT
