[offscreencanvas.commit.w.html]
  expected:
    if (os == "win") and (processor == "x86_64") and debug and webrender and not swgl: ["ERROR", "OK"]
    if (os == "linux") and webrender and not fission and not swgl: ["ERROR", "OK"]
    if (os == "win") and (processor == "x86") and debug: ["ERROR", "OK"]
    if (os == "linux") and webrender and fission: ["ERROR", "OK"]
    if (os == "linux") and not webrender and debug: ["ERROR", "OK"]
    if (os == "mac") and debug: ["ERROR", "OK"]
    [OK, ERROR]
  [Test that calling OffscreenCanvas's commit pushes its contents to its placeholder.]
    expected: FAIL

  [Test that calling commit on an OffscreenCanvas that is not transferred from a HTMLCanvasElement throws an exception in a worker.]
    expected: FAIL

