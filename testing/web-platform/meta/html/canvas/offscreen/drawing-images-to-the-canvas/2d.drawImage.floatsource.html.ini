[2d.drawImage.floatsource.html]
  expected:
    if fission and (processor == "x86_64") and (os == "linux") and debug and swgl: [ERROR, OK]
    if fission and (processor == "x86") and debug: OK
    if not fission and (os == "android") and swgl: OK
    if not fission and (os == "linux"): OK
    [OK, ERROR]
