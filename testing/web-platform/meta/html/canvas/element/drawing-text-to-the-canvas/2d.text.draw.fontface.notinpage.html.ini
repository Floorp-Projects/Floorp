[2d.text.draw.fontface.notinpage.html]
  [@font-face fonts should work even if they are not used in the page]
    expected:
      if (os == "win") and debug and (processor == "x86_64") and not webrender: ["FAIL", "PASS"]
      [PASS, FAIL]

