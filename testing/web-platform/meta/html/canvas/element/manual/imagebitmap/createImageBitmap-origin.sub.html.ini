[createImageBitmap-origin.sub.html]
  expected:
    if (os == "win") and not webrender and debug and (processor == "x86_64"): ["OK", "TIMEOUT"]
    if (os == "mac") and not debug: ["OK", "TIMEOUT"]
    if (os == "win") and webrender: TIMEOUT
    [TIMEOUT, OK]
  [redirected to cross-origin HTMLVideoElement: origin unclear 2dContext.drawImage]
    expected:
      if (os == "win") and debug and not webrender and (processor == "x86_64"): ["PASS", "NOTRUN"]
      if (os == "linux") and not fission and not debug and not webrender: ["NOTRUN", "PASS", "TIMEOUT"]
      if (os == "linux") and fission: ["PASS", "NOTRUN"]
      if os == "android": ["PASS", "NOTRUN"]
      if os == "mac": ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [redirected to cross-origin HTMLVideoElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if (os == "win") and debug and not webrender and (processor == "x86_64"): ["PASS", "TIMEOUT", "NOTRUN"]
      if (os == "linux") and fission and debug: ["PASS", "TIMEOUT", "NOTRUN"]
      if (os == "linux") and fission and not debug: ["TIMEOUT", "PASS", "NOTRUN"]
      if (os == "android") and debug: ["TIMEOUT", "PASS", "NOTRUN"]
      if (os == "android") and not debug: ["TIMEOUT", "PASS", "NOTRUN"]
      if os == "mac": ["PASS", "TIMEOUT", "NOTRUN"]
      [NOTRUN, PASS, TIMEOUT]

  [unclean HTMLCanvasElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if (os == "win") and not webrender and debug and (processor == "x86_64"): ["PASS", "NOTRUN"]
      if (os == "mac") and not debug: ["PASS", "NOTRUN"]
      if (os == "win") and webrender: NOTRUN
      [NOTRUN, PASS]

  [unclean HTMLCanvasElement: origin unclear getImageData]
    expected:
      if (os == "win") and not webrender and debug and (processor == "x86_64"): ["PASS", "NOTRUN"]
      if (os == "mac") and not debug: ["PASS", "NOTRUN"]
      if (os == "win") and webrender: NOTRUN
      [NOTRUN, PASS]

  [cross-origin HTMLVideoElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if (os == "win") and debug and not webrender and (processor == "x86_64"): ["PASS", "TIMEOUT"]
      if (os == "linux") and fission: ["PASS", "TIMEOUT"]
      if os == "android": ["PASS", "TIMEOUT"]
      if os == "mac": ["PASS", "TIMEOUT"]
      [TIMEOUT, PASS]

  [redirected to same-origin HTMLVideoElement: origin unclear getImageData]
    expected:
      if (os == "win") and (processor == "x86_64") and debug and not webrender: ["PASS", "NOTRUN"]
      if (os == "linux") and fission and debug: ["PASS", "NOTRUN"]
      if os == "mac": ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [redirected to same-origin HTMLVideoElement: origin unclear 2dContext.drawImage]
    expected:
      if (os == "win") and (processor == "x86_64") and debug and not webrender: ["PASS", "NOTRUN"]
      if (os == "linux") and fission and debug: ["PASS", "NOTRUN"]
      if os == "mac": ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [unclean ImageBitmap: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if (os == "win") and not webrender and debug and (processor == "x86_64"): ["PASS", "NOTRUN"]
      if (os == "mac") and not debug: ["PASS", "NOTRUN"]
      if (os == "win") and webrender: NOTRUN
      [NOTRUN, PASS]

  [redirected to same-origin HTMLVideoElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if (os == "win") and not webrender and (processor == "x86_64") and debug: ["PASS", "TIMEOUT", "NOTRUN"]
      if (os == "linux") and fission and not debug: ["NOTRUN", "PASS"]
      if (os == "linux") and fission and debug: ["TIMEOUT", "PASS", "NOTRUN"]
      if (os == "win") and webrender and debug: ["NOTRUN", "TIMEOUT"]
      if (os == "win") and webrender and not debug: ["NOTRUN", "TIMEOUT"]
      if (os == "mac") and not debug: ["PASS", "TIMEOUT", "NOTRUN"]
      if (os == "mac") and debug: ["TIMEOUT", "PASS", "NOTRUN"]
      [NOTRUN, PASS, TIMEOUT]

  [redirected to cross-origin HTMLVideoElement: origin unclear getImageData]
    expected:
      if (os == "win") and debug and not webrender and (processor == "x86_64"): ["PASS", "NOTRUN"]
      if (os == "linux") and fission: ["PASS", "NOTRUN"]
      if os == "mac": ["PASS", "NOTRUN"]
      if os == "android": ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [unclean ImageBitmap: origin unclear getImageData]
    expected:
      if (os == "win") and not webrender and debug and (processor == "x86_64"): ["PASS", "NOTRUN"]
      if (os == "win") and webrender: NOTRUN
      if (os == "mac") and not debug: ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [unclean HTMLCanvasElement: origin unclear 2dContext.drawImage]
    expected:
      if (os == "win") and not webrender and debug and (processor == "x86_64"): ["PASS", "NOTRUN"]
      if (os == "win") and webrender: NOTRUN
      if (os == "mac") and not debug: ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [unclean ImageBitmap: origin unclear 2dContext.drawImage]
    expected:
      if (os == "win") and not webrender and debug and (processor == "x86_64"): ["PASS", "NOTRUN"]
      if (os == "win") and webrender: NOTRUN
      if (os == "mac") and not debug: ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

