[createImageBitmap-origin.sub.html]
  expected:
    if (os == "linux") and not fission and debug and not webrender: ["OK", "TIMEOUT"]
    if (os == "win") and webrender: TIMEOUT
    [TIMEOUT, OK]
  [redirected to cross-origin HTMLVideoElement: origin unclear 2dContext.drawImage]
    expected:
      if (os == "win") and (processor == "x86_64") and not debug and webrender and not fission: ["NOTRUN", "PASS"]
      if (os == "linux") and webrender and not debug and fission: ["NOTRUN", "PASS"]
      if (os == "linux") and webrender and not debug and not fission: ["NOTRUN", "PASS"]
      if (os == "linux") and webrender and debug: ["NOTRUN", "PASS"]
      if (os == "linux") and not webrender and not debug: ["PASS", "TIMEOUT", "NOTRUN"]
      if (os == "android") and debug: ["NOTRUN", "PASS"]
      if (os == "mac") and debug: ["NOTRUN", "PASS"]
      [PASS, NOTRUN]

  [redirected to cross-origin HTMLVideoElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if (os == "win") and (processor == "x86_64") and not debug and webrender and not fission: ["NOTRUN", "TIMEOUT", "PASS"]
      if (os == "win") and (processor == "x86_64") and not debug and not webrender: ["TIMEOUT", "NOTRUN", "PASS"]
      if (os == "win") and (processor == "x86_64") and debug and not webrender: ["PASS", "TIMEOUT", "NOTRUN"]
      if (os == "linux") and webrender and not debug and fission: ["NOTRUN", "TIMEOUT", "PASS"]
      if (os == "linux") and webrender and not debug and not fission: ["NOTRUN", "PASS", "TIMEOUT"]
      if (os == "linux") and not webrender and debug: ["PASS", "TIMEOUT", "NOTRUN"]
      if (os == "linux") and webrender and debug: ["NOTRUN", "PASS", "TIMEOUT"]
      if (os == "mac") and debug: ["NOTRUN", "PASS", "TIMEOUT"]
      if (os == "mac") and not debug: ["PASS", "TIMEOUT", "NOTRUN"]
      if (os == "android") and not debug: ["PASS", "TIMEOUT", "NOTRUN"]
      if (os == "android") and debug: ["NOTRUN", "PASS", "TIMEOUT"]
      [TIMEOUT, PASS, NOTRUN]

  [unclean HTMLCanvasElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if (os == "linux") and not fission and debug and not webrender: ["PASS", "NOTRUN"]
      if (os == "win") and webrender: NOTRUN
      [NOTRUN, PASS]

  [unclean HTMLCanvasElement: origin unclear getImageData]
    expected:
      if (os == "linux") and not fission and debug and not webrender: ["PASS", "NOTRUN"]
      if (os == "win") and webrender: NOTRUN
      [NOTRUN, PASS]

  [cross-origin HTMLVideoElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if (os == "win") and (processor == "x86_64") and not debug and webrender and not fission: ["TIMEOUT", "PASS"]
      if (os == "linux") and not debug and webrender and fission: ["TIMEOUT", "PASS"]
      if (os == "linux") and not debug and webrender and not fission: ["TIMEOUT", "PASS"]
      if (os == "linux") and debug and webrender: ["TIMEOUT", "PASS"]
      if (os == "mac") and debug: ["TIMEOUT", "PASS"]
      if (os == "android") and debug: ["TIMEOUT", "PASS"]
      [PASS, TIMEOUT]

  [redirected to same-origin HTMLVideoElement: origin unclear getImageData]
    expected:
      if (os == "win") and (processor == "x86_64") and debug and not webrender: ["PASS", "NOTRUN"]
      if (os == "linux") and debug and not webrender: ["PASS", "NOTRUN"]
      if (os == "mac") and not debug: ["PASS", "NOTRUN"]
      if (os == "android") and not debug: ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [redirected to same-origin HTMLVideoElement: origin unclear 2dContext.drawImage]
    expected:
      if (os == "win") and (processor == "x86_64") and debug and not webrender: ["PASS", "NOTRUN"]
      if (os == "linux") and debug and not webrender: ["PASS", "NOTRUN"]
      if (os == "mac") and not debug: ["PASS", "NOTRUN"]
      if (os == "android") and not debug: ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [unclean ImageBitmap: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if (os == "linux") and not fission and debug and not webrender: ["PASS", "NOTRUN"]
      if (os == "win") and webrender: NOTRUN
      [NOTRUN, PASS]

  [redirected to same-origin HTMLVideoElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if (os == "linux") and not fission and debug and not webrender: ["PASS", "TIMEOUT", "NOTRUN"]
      if (os == "win") and not webrender and debug and (processor == "x86_64"): ["TIMEOUT", "PASS", "NOTRUN"]
      if (os == "win") and webrender and not debug and fission: ["NOTRUN", "TIMEOUT"]
      if (os == "win") and webrender and not debug and not fission: ["NOTRUN", "TIMEOUT"]
      if (os == "linux") and fission and not debug: ["NOTRUN", "PASS"]
      if (os == "win") and webrender and debug: ["NOTRUN", "TIMEOUT"]
      if (os == "mac") and not debug: ["PASS", "TIMEOUT", "NOTRUN"]
      if (os == "android") and not debug: ["TIMEOUT", "PASS", "NOTRUN"]
      [NOTRUN, PASS, TIMEOUT]

  [redirected to cross-origin HTMLVideoElement: origin unclear getImageData]
    expected:
      if (os == "win") and (processor == "x86_64") and not debug and webrender and not fission: ["NOTRUN", "PASS"]
      if (os == "linux") and not debug and webrender and not fission: ["NOTRUN", "PASS"]
      if (os == "linux") and not debug and webrender and fission: ["NOTRUN", "PASS"]
      if (os == "linux") and debug and webrender: ["NOTRUN", "PASS"]
      if (os == "mac") and debug: ["NOTRUN", "PASS"]
      if (os == "android") and debug: ["NOTRUN", "PASS"]
      [PASS, NOTRUN]

  [unclean ImageBitmap: origin unclear getImageData]
    expected:
      if (os == "linux") and not fission and debug and not webrender: ["PASS", "NOTRUN"]
      if (os == "win") and webrender: NOTRUN
      [NOTRUN, PASS]

  [unclean HTMLCanvasElement: origin unclear 2dContext.drawImage]
    expected:
      if (os == "linux") and not fission and debug and not webrender: ["PASS", "NOTRUN"]
      if (os == "win") and webrender: NOTRUN
      [NOTRUN, PASS]

  [unclean ImageBitmap: origin unclear 2dContext.drawImage]
    expected:
      if (os == "linux") and not fission and debug and not webrender: ["PASS", "NOTRUN"]
      if (os == "win") and webrender: NOTRUN
      [NOTRUN, PASS]

