[createImageBitmap-origin.sub.html]
  expected:
    if (os == "linux") and debug and webrender and fission: ["OK", "TIMEOUT"]
    if (os == "linux") and debug and not webrender: ["OK", "TIMEOUT"]
    if (os == "android") and debug: ["OK", "TIMEOUT"]
    [TIMEOUT, OK]
  [redirected to cross-origin HTMLVideoElement: origin unclear 2dContext.drawImage]
    expected:
      if not fission and debug and (os == "win") and not webrender and (processor == "x86_64"): ["NOTRUN", "PASS", "FAIL"]
      if not fission and not debug and (os == "win") and (processor == "x86"): ["NOTRUN", "PASS"]
      if not fission and not debug and (os == "android"): ["NOTRUN", "PASS"]
      if not fission and not debug and (os == "linux"): ["NOTRUN", "PASS"]
      if not fission and debug and (os == "mac"): ["NOTRUN", "PASS"]
      if fission and (os == "win"): ["NOTRUN", "PASS"]
      [PASS, NOTRUN]

  [redirected to cross-origin HTMLVideoElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if not fission and not debug and (os == "win") and (processor == "x86_64") and webrender: ["TIMEOUT", "NOTRUN", "PASS"]
      if not fission and not debug and (os == "win") and (processor == "x86_64") and not webrender: ["TIMEOUT", "PASS", "NOTRUN"]
      if not fission and debug and (os == "win") and not webrender and (processor == "x86"): ["PASS", "TIMEOUT", "NOTRUN"]
      if not fission and debug and (os == "win") and webrender: ["TIMEOUT", "PASS", "NOTRUN"]
      if fission and (os == "linux") and debug: ["PASS", "TIMEOUT", "NOTRUN"]
      if fission and (os == "linux") and not debug: ["TIMEOUT", "PASS", "NOTRUN"]
      if not fission and not debug and (os == "mac"): ["PASS", "TIMEOUT", "NOTRUN"]
      if not fission and debug and (os == "android"): ["PASS", "TIMEOUT", "NOTRUN"]
      if not fission and debug and (os == "linux"): ["PASS", "TIMEOUT", "NOTRUN"]
      if fission and (os == "win"): ["NOTRUN", "TIMEOUT", "PASS"]
      [NOTRUN, PASS, TIMEOUT]

  [unclean HTMLCanvasElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if (os == "linux") and debug and webrender and fission: ["PASS", "NOTRUN"]
      if (os == "linux") and debug and not webrender: ["PASS", "NOTRUN"]
      if (os == "android") and debug: ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [unclean HTMLCanvasElement: origin unclear getImageData]
    expected:
      if (os == "linux") and debug and webrender and fission: ["PASS", "NOTRUN"]
      if (os == "linux") and debug and not webrender: ["PASS", "NOTRUN"]
      if (os == "android") and debug: ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [cross-origin HTMLVideoElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if not fission and debug and (os == "win") and not webrender and (processor == "x86_64"): ["TIMEOUT", "PASS", "FAIL"]
      if not fission and not debug and (os == "win") and (processor == "x86"): ["TIMEOUT", "PASS"]
      if not fission and debug and (os == "mac"): ["TIMEOUT", "PASS"]
      if not fission and not debug and (os == "android"): ["TIMEOUT", "PASS"]
      if not fission and not debug and (os == "linux"): ["TIMEOUT", "PASS"]
      if fission and (os == "win"): ["TIMEOUT", "PASS"]
      [PASS, TIMEOUT]

  [redirected to same-origin HTMLVideoElement: origin unclear getImageData]
    expected:
      if not fission and debug and (os == "win") and (processor == "x86"): ["PASS", "NOTRUN"]
      if fission and (os == "linux") and debug: ["PASS", "NOTRUN"]
      if not fission and not debug and (os == "mac"): ["PASS", "NOTRUN"]
      if not fission and debug and (os == "linux"): ["PASS", "NOTRUN"]
      if not fission and debug and (os == "android"): ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [redirected to same-origin HTMLVideoElement: origin unclear 2dContext.drawImage]
    expected:
      if not fission and debug and (os == "win") and (processor == "x86"): ["PASS", "NOTRUN"]
      if fission and (os == "linux") and debug: ["PASS", "NOTRUN"]
      if not fission and debug and (os == "android"): ["PASS", "NOTRUN"]
      if not fission and debug and (os == "linux"): ["PASS", "NOTRUN"]
      if not fission and not debug and (os == "mac"): ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [unclean ImageBitmap: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if (os == "linux") and debug and webrender and fission: ["PASS", "NOTRUN"]
      if (os == "linux") and debug and not webrender: ["PASS", "NOTRUN"]
      if (os == "android") and debug: ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [redirected to same-origin HTMLVideoElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if not fission and (os == "win") and not webrender and debug and (processor == "x86"): ["TIMEOUT", "PASS", "NOTRUN"]
      if not fission and (os == "linux") and debug and not webrender: ["PASS", "TIMEOUT", "NOTRUN"]
      if not fission and (os == "linux") and debug and webrender: ["TIMEOUT", "PASS", "NOTRUN"]
      if not fission and (os == "android") and debug: ["PASS", "TIMEOUT", "NOTRUN"]
      if not fission and (os == "mac") and not debug: ["TIMEOUT", "PASS", "NOTRUN"]
      if fission and (os == "linux") and debug: ["PASS", "TIMEOUT", "NOTRUN"]
      if fission and (os == "win"): ["NOTRUN", "TIMEOUT", "PASS"]
      [NOTRUN, PASS, TIMEOUT]

  [redirected to cross-origin HTMLVideoElement: origin unclear getImageData]
    expected:
      if not fission and debug and (os == "win") and not webrender and (processor == "x86_64"): ["NOTRUN", "PASS", "FAIL"]
      if not fission and not debug and (os == "win") and (processor == "x86"): ["NOTRUN", "PASS"]
      if not fission and debug and (os == "mac"): ["NOTRUN", "PASS"]
      if not fission and not debug and (os == "android"): ["NOTRUN", "PASS"]
      if not fission and not debug and (os == "linux"): ["NOTRUN", "PASS"]
      if fission and (os == "win"): ["NOTRUN", "PASS"]
      [PASS, NOTRUN]

  [unclean ImageBitmap: origin unclear getImageData]
    expected:
      if (os == "linux") and debug and webrender and fission: ["PASS", "NOTRUN"]
      if (os == "linux") and debug and not webrender: ["PASS", "NOTRUN"]
      if (os == "android") and debug: ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [unclean HTMLCanvasElement: origin unclear 2dContext.drawImage]
    expected:
      if (os == "linux") and debug and webrender and fission: ["PASS", "NOTRUN"]
      if (os == "linux") and debug and not webrender: ["PASS", "NOTRUN"]
      if (os == "android") and debug: ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [unclean ImageBitmap: origin unclear 2dContext.drawImage]
    expected:
      if (os == "linux") and debug and webrender and fission: ["PASS", "NOTRUN"]
      if (os == "linux") and debug and not webrender: ["PASS", "NOTRUN"]
      if (os == "android") and debug: ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [cross-origin HTMLVideoElement: origin unclear 2dContext.drawImage]
    expected:
      if (os == "win") and debug and not webrender and (processor == "x86_64"): ["PASS", "FAIL"]

  [cross-origin HTMLVideoElement: origin unclear getImageData]
    expected:
      if (os == "win") and debug and not webrender and (processor == "x86_64"): ["PASS", "FAIL"]

