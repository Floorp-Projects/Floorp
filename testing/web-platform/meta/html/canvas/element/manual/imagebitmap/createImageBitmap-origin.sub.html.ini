[createImageBitmap-origin.sub.html]
  expected: [TIMEOUT, OK]
  [redirected to cross-origin HTMLVideoElement: origin unclear 2dContext.drawImage]
    expected:
      if (os == "win") and not fission and debug and not webrender and (processor == "x86"): ["PASS", "NOTRUN"]
      if (os == "win") and not fission and debug and not webrender and (processor == "x86_64"): ["PASS", "NOTRUN"]
      if (os == "win") and not fission and not debug and (processor == "x86_64"): ["PASS", "NOTRUN"]
      if (os == "linux") and webrender and not debug and not fission: ["PASS", "NOTRUN"]
      if (os == "linux") and webrender and debug and fission: ["PASS", "NOTRUN"]
      if (os == "mac") and debug: ["PASS", "NOTRUN"]
      if (os == "win") and fission: ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [redirected to cross-origin HTMLVideoElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if (os == "linux") and not debug and webrender and not fission: ["TIMEOUT", "PASS", "NOTRUN"]
      if (os == "linux") and debug and webrender and fission: ["PASS", "TIMEOUT", "NOTRUN"]
      if (os == "win") and not webrender and debug and (processor == "x86"): ["TIMEOUT", "PASS", "NOTRUN"]
      if (os == "win") and not webrender and debug and (processor == "x86_64"): ["TIMEOUT", "PASS", "NOTRUN"]
      if (os == "win") and not webrender and not debug and (processor == "x86_64"): ["TIMEOUT", "PASS", "NOTRUN"]
      if (os == "win") and webrender and not debug and fission: ["PASS", "TIMEOUT", "NOTRUN"]
      if (os == "win") and webrender and not debug and not fission: ["PASS", "TIMEOUT", "NOTRUN"]
      if (os == "mac") and debug: ["TIMEOUT", "PASS", "NOTRUN"]
      [NOTRUN, PASS, TIMEOUT]

  [unclean HTMLCanvasElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected: [NOTRUN, PASS]

  [unclean HTMLCanvasElement: origin unclear getImageData]
    expected: [NOTRUN, PASS]

  [cross-origin HTMLVideoElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if (os == "win") and not fission and debug and not webrender and (processor == "x86"): ["PASS", "TIMEOUT"]
      if (os == "win") and not fission and debug and not webrender and (processor == "x86_64"): ["PASS", "TIMEOUT"]
      if (os == "linux") and webrender and debug and fission: ["PASS", "TIMEOUT"]
      if (os == "linux") and webrender and not debug and not fission: ["PASS", "TIMEOUT"]
      if (os == "win") and not fission and not debug and (processor == "x86_64"): ["PASS", "TIMEOUT"]
      if (os == "win") and fission: ["PASS", "TIMEOUT"]
      if (os == "mac") and debug: ["PASS", "TIMEOUT"]
      [TIMEOUT, PASS]

  [redirected to same-origin HTMLVideoElement: origin unclear getImageData]
    expected:
      if (os == "linux") and fission and debug: ["PASS", "NOTRUN"]
      if (os == "win") and not debug and webrender: ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [redirected to same-origin HTMLVideoElement: origin unclear 2dContext.drawImage]
    expected:
      if (os == "linux") and fission and debug: ["PASS", "NOTRUN"]
      if (os == "win") and not debug and webrender: ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [unclean ImageBitmap: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected: [NOTRUN, PASS]

  [redirected to same-origin HTMLVideoElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if (os == "win") and not debug and webrender: ["TIMEOUT", "PASS", "NOTRUN"]
      if (os == "linux") and fission and debug: ["TIMEOUT", "PASS", "NOTRUN"]
      [NOTRUN, PASS, TIMEOUT]

  [redirected to cross-origin HTMLVideoElement: origin unclear getImageData]
    expected:
      if (os == "win") and not fission and debug and not webrender and (processor == "x86_64"): ["PASS", "NOTRUN"]
      if (os == "win") and not fission and debug and not webrender and (processor == "x86"): ["PASS", "NOTRUN"]
      if (os == "win") and not fission and not debug and (processor == "x86_64"): ["PASS", "NOTRUN"]
      if (os == "linux") and webrender and debug and fission: ["PASS", "NOTRUN"]
      if (os == "linux") and webrender and not debug and not fission: ["PASS", "NOTRUN"]
      if (os == "mac") and debug: ["PASS", "NOTRUN"]
      if (os == "win") and fission: ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [unclean ImageBitmap: origin unclear getImageData]
    expected: [NOTRUN, PASS]

  [unclean HTMLCanvasElement: origin unclear 2dContext.drawImage]
    expected: [NOTRUN, PASS]

  [unclean ImageBitmap: origin unclear 2dContext.drawImage]
    expected: [NOTRUN, PASS]

