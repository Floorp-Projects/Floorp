[createImageBitmap-origin.sub.html]
  expected: [TIMEOUT, OK]
  [redirected to cross-origin HTMLVideoElement: origin unclear 2dContext.drawImage]
    expected:
      if webrender and not debug and (os == "linux") and fission: ["PASS", "NOTRUN"]
      if not webrender and (os == "win") and debug and (processor == "x86_64"): ["PASS", "NOTRUN", "FAIL"]
      if webrender and debug and (os == "win"): ["PASS", "NOTRUN"]
      if not webrender and (os == "linux") and not debug: ["PASS", "NOTRUN"]
      if not webrender and (os == "mac") and not debug: ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [redirected to cross-origin HTMLVideoElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if webrender and not debug and fission and (os == "linux"): ["PASS", "TIMEOUT", "NOTRUN"]
      if not webrender and (os == "win") and debug and (processor == "x86_64"): ["TIMEOUT", "NOTRUN", "PASS"]
      if webrender and debug and (os == "win"): ["TIMEOUT", "PASS", "NOTRUN"]
      if not webrender and (os == "mac") and not debug: ["TIMEOUT", "PASS", "NOTRUN"]
      if not webrender and (os == "linux") and not debug: ["TIMEOUT", "NOTRUN", "PASS"]
      [NOTRUN, PASS, TIMEOUT]

  [unclean HTMLCanvasElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected: [NOTRUN, PASS]

  [unclean HTMLCanvasElement: origin unclear getImageData]
    expected: [NOTRUN, PASS]

  [cross-origin HTMLVideoElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if not webrender and (os == "win") and debug and (processor == "x86_64"): ["PASS", "TIMEOUT", "FAIL"]
      if webrender and not debug and (os == "linux") and fission: ["PASS", "TIMEOUT"]
      if not webrender and (os == "linux") and not debug: ["PASS", "TIMEOUT"]
      if not webrender and (os == "mac") and not debug: ["PASS", "TIMEOUT"]
      if webrender and debug and (os == "win"): ["PASS", "TIMEOUT"]
      [TIMEOUT, PASS]

  [redirected to same-origin HTMLVideoElement: origin unclear getImageData]
    expected:
      if webrender and fission and (os == "linux") and not debug: ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [redirected to same-origin HTMLVideoElement: origin unclear 2dContext.drawImage]
    expected:
      if webrender and fission and (os == "linux") and not debug: ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [unclean ImageBitmap: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected: [NOTRUN, PASS]

  [redirected to same-origin HTMLVideoElement: origin unclear bitmaprenderer.transferFromImageBitmap]
    expected:
      if webrender and fission and (os == "linux") and not debug: ["TIMEOUT", "PASS", "NOTRUN"]
      [NOTRUN, PASS, TIMEOUT]

  [redirected to cross-origin HTMLVideoElement: origin unclear getImageData]
    expected:
      if not webrender and (os == "win") and debug and (processor == "x86_64"): ["PASS", "NOTRUN", "FAIL"]
      if webrender and not debug and (os == "linux") and fission: ["PASS", "NOTRUN"]
      if not webrender and (os == "linux") and not debug: ["PASS", "NOTRUN"]
      if not webrender and (os == "mac") and not debug: ["PASS", "NOTRUN"]
      if webrender and debug and (os == "win"): ["PASS", "NOTRUN"]
      [NOTRUN, PASS]

  [unclean ImageBitmap: origin unclear getImageData]
    expected: [NOTRUN, PASS]

  [unclean HTMLCanvasElement: origin unclear 2dContext.drawImage]
    expected: [NOTRUN, PASS]

  [unclean ImageBitmap: origin unclear 2dContext.drawImage]
    expected: [NOTRUN, PASS]

  [cross-origin HTMLVideoElement: origin unclear 2dContext.drawImage]
    expected:
      if (os == "win") and debug and not webrender and (processor == "x86_64"): ["PASS", "FAIL"]

  [cross-origin HTMLVideoElement: origin unclear getImageData]
    expected:
      if (os == "win") and debug and not webrender and (processor == "x86_64"): ["PASS", "FAIL"]

