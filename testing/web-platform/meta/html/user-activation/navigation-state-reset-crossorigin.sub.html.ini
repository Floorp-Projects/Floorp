[navigation-state-reset-crossorigin.sub.html]
  expected:
    if not fission: OK
    if os == "android": OK
    TIMEOUT
  [Post-navigation state reset.]
    expected:
      if not fission: PASS
      if os == "android": PASS
      TIMEOUT
