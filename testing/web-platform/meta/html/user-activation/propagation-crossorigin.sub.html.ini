[propagation-crossorigin.sub.html]
  expected:
    if not fission: OK
    if os == "android": OK
    TIMEOUT
  [Propagation test]
    expected:
      if not fission: PASS
      if os == "android": PASS
      NOTRUN
