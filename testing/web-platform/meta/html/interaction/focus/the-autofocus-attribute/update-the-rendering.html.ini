[update-the-rendering.html]
  ["Flush autofocus candidates" should be happen after the first animation frame callbacks, and before a resize event in the next iteration of window event loop.]
    expected:
      if (os == "linux") and debug: [PASS, FAIL]
      if debug and not webrender and (os == "win") and (processor == "x86_64"): ["FAIL", "PASS"]
      if debug and not webrender and (os == "win") and (processor == "x86"): ["FAIL", "PASS"]
      if debug and not webrender and (os == "android"): ["FAIL", "PASS"]
      if not debug and (os == "linux") and not webrender: ["FAIL", "PASS"]
      FAIL

