[document-has-system-focus.html]
  expected:
    if not fission and not webrender and (os == "win") and debug and (processor == "x86"): ["OK", "TIMEOUT"]
    if not fission and not webrender and (os == "win") and debug and (processor == "x86_64"): ["TIMEOUT", "OK"]
    if fission and (os == "linux") and not webrender: ["OK", "TIMEOUT"]
    if fission and (os == "linux") and webrender: ["TIMEOUT", "OK"]
    if not fission and not webrender and (os == "linux"): ["OK", "TIMEOUT"]
    if fission and (os == "win"): ["OK", "TIMEOUT"]
  [Top-level document receives blur/focus events and loses system focus during opening/closing of a popup]
    expected:
      if not fission and not webrender and (os == "win") and debug and (processor == "x86_64"): ["TIMEOUT", "PASS"]
      if not fission and not webrender and (os == "win") and debug and (processor == "x86"): ["PASS", "TIMEOUT"]
      if fission and (os == "linux") and not webrender: ["PASS", "TIMEOUT"]
      if fission and (os == "linux") and webrender: ["TIMEOUT", "PASS"]
      if not fission and not webrender and (os == "linux"): ["PASS", "TIMEOUT"]
      if fission and (os == "win"): ["PASS", "TIMEOUT"]

