[appearance-animation-002.html]
  expected:
    if debug and swgl and (os == "android"): [ERROR, TIMEOUT, CRASH]
    if debug and swgl and (os == "win"): [ERROR, TIMEOUT, CRASH]
    if debug and swgl and (os == "linux"): [ERROR, TIMEOUT, CRASH]
    if debug and not swgl: ERROR
    FAIL
