[reporterror.any.worker.html]
  [self.reportError() doesn't invoke getters]
    expected:
      if os == "win": PASS
      if os == "android": PASS
      [PASS, FAIL]


[reporterror.any.html]
  [self.reportError() doesn't invoke getters]
    expected:
      if os == "win": PASS
      if os == "android": PASS
      [PASS, FAIL]
