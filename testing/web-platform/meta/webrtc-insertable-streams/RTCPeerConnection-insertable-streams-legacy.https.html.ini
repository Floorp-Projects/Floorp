[RTCPeerConnection-insertable-streams-legacy.https.html]
  expected:
    if (os == "linux") and debug and webrender and not fission and not swgl: ["TIMEOUT", "OK", "ERROR"]
    if (os == "linux") and debug and webrender and not fission and swgl: ["OK", "ERROR", "TIMEOUT"]
    if (os == "linux") and debug and webrender and fission: ["OK", "ERROR", "TIMEOUT"]
    if (os == "linux") and debug and not webrender: ["TIMEOUT", "OK"]
    if (os == "linux") and not debug: ["OK", "ERROR", "TIMEOUT", "CRASH"]
  [Legacy API works]
    expected: FAIL

