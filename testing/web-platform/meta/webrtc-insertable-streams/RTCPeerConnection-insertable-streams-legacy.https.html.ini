[RTCPeerConnection-insertable-streams-legacy.https.html]
  expected:
    if (os == "linux") and webrender and debug and not fission and not swgl: ["TIMEOUT", "OK", "ERROR"]
    if (os == "linux") and webrender and debug and not fission and swgl: ["OK", "ERROR", "TIMEOUT"]
    if (os == "linux") and webrender and not debug and fission: ["OK", "TIMEOUT", "ERROR", "CRASH"]
    if (os == "linux") and webrender and not debug and not fission: ["OK", "ERROR", "TIMEOUT", "CRASH"]
    if (os == "linux") and webrender and debug and fission: ["OK", "ERROR", "TIMEOUT"]
    if (os == "linux") and not webrender and not debug: ["OK", "ERROR", "TIMEOUT", "CRASH"]
    if (os == "linux") and not webrender and debug: ["TIMEOUT", "OK"]
  [Legacy API works]
    expected: FAIL

