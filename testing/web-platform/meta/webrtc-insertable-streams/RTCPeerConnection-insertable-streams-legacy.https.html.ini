[RTCPeerConnection-insertable-streams-legacy.https.html]
  expected:
    if (os == "linux") and webrender and debug: ["TIMEOUT", "OK", "ERROR"]
    if (os == "linux") and webrender and not debug: ["OK", "ERROR", "TIMEOUT", "CRASH"]
    if (os == "linux") and not webrender and not debug: ["OK", "ERROR", "TIMEOUT", "CRASH"]
    if (os == "linux") and not webrender and debug: ["OK", "TIMEOUT"]
  [Legacy API works]
    expected: FAIL

