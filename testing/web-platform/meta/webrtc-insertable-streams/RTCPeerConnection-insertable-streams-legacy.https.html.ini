[RTCPeerConnection-insertable-streams-legacy.https.html]
  expected:
    if (os == "linux") and not debug and webrender and not fission: ["OK", "ERROR", "TIMEOUT", "CRASH"]
    if (os == "linux") and not debug and webrender and fission: ["OK", "ERROR", "TIMEOUT", "CRASH"]
    if (os == "linux") and debug and webrender and not fission: ["OK", "ERROR", "TIMEOUT"]
    if (os == "linux") and debug and webrender and fission: ["OK", "ERROR", "TIMEOUT"]
    if (os == "linux") and not debug and not webrender: ["OK", "ERROR", "TIMEOUT", "CRASH"]
    if (os == "linux") and debug and not webrender: ["OK", "TIMEOUT"]
  [Legacy API works]
    expected: FAIL

