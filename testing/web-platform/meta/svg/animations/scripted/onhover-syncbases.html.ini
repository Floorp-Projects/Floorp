[onhover-syncbases.html]
  expected: [OK, ERROR]
  [Check if onhover events reset correctly when triggered multiple times]
    expected:
      if not swgl and (os == "win") and not debug and (processor == "x86_64"): [FAIL, PASS]
      if not swgl and (os == "android") and debug: [FAIL, PASS]
      [PASS, FAIL]
