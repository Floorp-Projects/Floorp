[onhover-syncbases.html]
  expected:
    if not webrender: OK
    [OK, ERROR]
  [Check if onhover events reset correctly when triggered multiple times]
    expected:
      if (os == "linux") and webrender and not debug and (processor == "x86_64") and not fission: [FAIL, PASS]
      [PASS, FAIL]
