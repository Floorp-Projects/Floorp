[onhover-syncbases.html]
  expected:
    if webrender and (os == "linux") and not debug: [OK, ERROR]
    if not debug and not webrender and (os == "win") and (processor == "x86_64"): ["OK", "ERROR"]
  [Check if onhover events reset correctly when triggred multiple times]
    expected:
      if webrender and (os == "linux") and debug: FAIL
      if webrender and (os == "linux") and not debug: [FAIL, TIMEOUT]
      if (os == "win") and not debug and not webrender and (processor == "x86_64"): ["PASS", "TIMEOUT"]
      if (os == "android") and debug: ["PASS", "FAIL"]

