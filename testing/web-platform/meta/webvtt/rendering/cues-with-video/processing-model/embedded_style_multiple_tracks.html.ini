[embedded_style_multiple_tracks.html]
  expected:
    if not webrender and debug and (os == "mac"): [PASS, FAIL]
    if not webrender and debug and (os == "android"): [PASS, FAIL]
    if not webrender and debug and (os == "linux"): [PASS, FAIL]
    if not webrender and not debug: [PASS, FAIL]
