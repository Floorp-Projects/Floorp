[navigate_cue_position.html]
  disabled:
    if os == "win": https://bugzilla.mozilla.org/show_bug.cgi?id=1557548
    if os == "android": https://bugzilla.mozilla.org/show_bug.cgi?id=1536762
  expected:
    if debug and not fission and (os == "mac") and webrender: ["PASS", "FAIL"]
    if debug and not fission and (os == "mac") and not webrender: ["PASS", "FAIL"]
    if debug and not fission and (os == "linux"): ["PASS", "FAIL"]
