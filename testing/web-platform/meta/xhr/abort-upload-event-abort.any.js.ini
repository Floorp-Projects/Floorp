[abort-upload-event-abort.any.worker.html]
  expected:
    if (os == "linux") and webrender and not fission and debug: ["OK", "TIMEOUT"]
    if (os == "linux") and webrender and not fission and not debug: ["OK", "TIMEOUT"]
  [XMLHttpRequest: The abort() method: Fire a progress event named abort on the XMLHttpRequestUpload object]
    expected:
      if (os == "linux") and webrender and not fission and debug: ["PASS", "TIMEOUT"]
      if (os == "linux") and webrender and not fission and not debug: ["PASS", "TIMEOUT"]


[abort-upload-event-abort.any.html]
