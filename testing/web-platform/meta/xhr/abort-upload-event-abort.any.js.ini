[abort-upload-event-abort.any.worker.html]
  expected:
    if (os == "linux") and webrender and not fission: ["OK", "TIMEOUT"]
    if (os == "linux") and not webrender and (processor == "x86"): ["OK", "TIMEOUT"]
  [XMLHttpRequest: The abort() method: Fire a progress event named abort on the XMLHttpRequestUpload object]
    expected:
      if (os == "linux") and not webrender and (processor == "x86"): ["PASS", "TIMEOUT"]
      if (os == "linux") and webrender and not fission: ["PASS", "TIMEOUT"]


[abort-upload-event-abort.any.html]
