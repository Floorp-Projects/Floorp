[abort-upload-event-loadend.any.html]

[abort-upload-event-loadend.any.worker.html]
  expected:
    if (os == "linux") and not fission and not debug and not webrender and (processor == "x86"): ["OK", "TIMEOUT"]
    if (os == "linux") and not fission and not debug and not webrender and (processor == "x86_64"): ["OK", "TIMEOUT"]
    if (os == "linux") and not fission and not debug and webrender: ["OK", "TIMEOUT"]
    if (os == "linux") and not fission and debug: ["OK", "TIMEOUT"]
    if (os == "linux") and fission and debug: ["OK", "TIMEOUT"]
  [XMLHttpRequest: The abort() method: Fire a progress event named loadend on the XMLHttpRequestUpload object]
    expected:
      if (os == "linux") and not fission and not debug and not webrender and (processor == "x86"): ["PASS", "TIMEOUT"]
      if (os == "linux") and not fission and not debug and not webrender and (processor == "x86_64"): ["PASS", "TIMEOUT"]
      if (os == "linux") and not fission and not debug and webrender: ["PASS", "TIMEOUT"]
      if (os == "linux") and fission and debug: ["PASS", "TIMEOUT"]
      if (os == "linux") and not fission and debug: ["PASS", "TIMEOUT"]

