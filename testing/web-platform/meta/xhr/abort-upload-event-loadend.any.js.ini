[abort-upload-event-loadend.any.html]

[abort-upload-event-loadend.any.worker.html]
  expected:
    if (os == "linux") and not fission and debug and webrender: ["OK", "TIMEOUT"]
    if (os == "linux") and not fission and debug and not webrender: ["OK", "TIMEOUT"]
    if (os == "linux") and not fission and not debug: ["OK", "TIMEOUT"]
  [XMLHttpRequest: The abort() method: Fire a progress event named loadend on the XMLHttpRequestUpload object]
    expected:
      if (os == "linux") and not fission and debug and webrender: ["PASS", "TIMEOUT"]
      if (os == "linux") and not fission and debug and not webrender: ["PASS", "TIMEOUT"]
      if (os == "linux") and not fission and not debug: ["PASS", "TIMEOUT"]

