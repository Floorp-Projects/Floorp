[fetch-frame-resource.https.html]
  expected:
    if (os == "linux") and fission and not debug and (processor == "x86_64"): [OK, ERROR]
    if (os == "linux") and fission and debug and swgl: [OK, ERROR]
    if (os == "linux") and fission and debug and not swgl: [ERROR, OK]
    if (os == "linux") and not fission and debug and swgl: [CRASH, OK, ERROR]
    if (os == "linux") and not fission and debug and not swgl: [ERROR, CRASH, OK]
    if (os == "linux") and not fission and not debug: [OK, ERROR, CRASH]
    if (os == "android") and debug and not swgl: [OK, ERROR]
    if (os == "android") and not debug: [ERROR, CRASH]
