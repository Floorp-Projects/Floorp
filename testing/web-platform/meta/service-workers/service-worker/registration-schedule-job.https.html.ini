[registration-schedule-job.https.html]
  [different type]
    expected: FAIL

  [different updateViaCache]
    expected:
      if not webrender and not fission and (os == "android") and not debug: [PASS, FAIL]
      if not webrender and not fission and (os == "linux") and debug: [PASS, FAIL]
      [FAIL, PASS]
