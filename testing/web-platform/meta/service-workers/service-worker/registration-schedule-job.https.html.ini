[registration-schedule-job.https.html]
  [different type]
    expected: FAIL

  [different updateViaCache]
    expected:
      if not fission and (os == "linux") and (processor == "x86_64") and not swgl and debug and webrender: [PASS, FAIL]
      if not fission and (os == "win") and swgl: [PASS, FAIL]
      if not fission and (os == "mac") and debug: [PASS, FAIL]
      [FAIL, PASS]
