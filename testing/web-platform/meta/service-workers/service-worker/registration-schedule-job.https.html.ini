[registration-schedule-job.https.html]
  [different type]
    expected: FAIL

  [different updateViaCache]
    expected:
      if (os == "linux") and (processor == "x86_64") and debug and not swgl: [PASS, FAIL]
      if (os == "win") and not fission and swgl: [PASS, FAIL]
      if (os == "win") and fission and debug: [PASS, FAIL]
      if (os == "linux") and (processor == "x86"): [PASS, FAIL]
      [FAIL, PASS]
