[registration-schedule-job.https.html]
  [different type]
    expected: FAIL

  [different updateViaCache]
    expected:
      if debug and (os == "win") and fission and (processor == "x86_64") and swgl: [PASS, FAIL]
      if debug and (os == "linux") and not swgl and fission and not domstreams: [PASS, FAIL]
      if debug and (os == "android") and not swgl: [PASS, FAIL]
      if not debug and (os == "linux") and not fission: [PASS, FAIL]
      if debug and (os == "mac"): [PASS, FAIL]
      [FAIL, PASS]
