[fetch-request-redirect.https.html]
  max-asserts: 2
  [Redirected audio resource should be loaded. - default]
    expected:
      if (os == "win") and debug and not webrender and (processor == "x86_64"): ["PASS", "FAIL"]

  [Normal audio resource should be loaded. - default]
    expected:
      if (os == "win") and debug and not webrender and (processor == "x86_64"): ["PASS", "FAIL"]

  [Loading redirected audio with Request.redirect=follow should succeed. - default]
    expected:
      if (os == "win") and debug and not webrender and (processor == "x86_64"): ["PASS", "FAIL"]

