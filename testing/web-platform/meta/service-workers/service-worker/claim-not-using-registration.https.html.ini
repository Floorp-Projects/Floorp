[claim-not-using-registration.https.html]
  expected:
    if (os == "linux") and not debug and not webrender and not fission and (processor == "x86_64"): ["OK", "TIMEOUT"]
  [Test claim client which is not using registration]
    expected:
      if (os == "linux") and not debug and not webrender and not fission and (processor == "x86_64"): ["PASS", "TIMEOUT"]

  [Test claim client when there's a longer-matched registration not already used by the page]
    expected:
      if (os == "linux") and not debug and not webrender and not fission and (processor == "x86_64"): ["PASS", "NOTRUN"]

