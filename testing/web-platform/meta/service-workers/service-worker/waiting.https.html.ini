[waiting.https.html]
  [waiting is set after installation]
    expected:
      if not debug and (processor == "x86_64") and (os == "linux") and fission and webrender: ["FAIL", "PASS"]
      if not debug and (processor == "x86_64") and (os == "linux") and not fission: ["FAIL", "PASS"]
      if debug and (os == "linux") and not webrender and fission: ["FAIL", "PASS"]
      if debug and (os == "linux") and webrender: ["FAIL", "PASS"]
      if not debug and (processor == "aarch64"): ["FAIL", "PASS"]
      if debug and (os == "win"): ["FAIL", "PASS"]
      FAIL

