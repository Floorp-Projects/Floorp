[waiting.https.html]
  expected:
    if fission and (os == "linux") and debug: ["OK", "TIMEOUT"]
    if fission and (os == "linux") and not debug: ["OK", "TIMEOUT"]
  [waiting is set after installation]
    expected:
      if (os == "linux") and fission and debug: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "linux") and fission and not debug: ["FAIL", "TIMEOUT", "PASS"]
      if (os == "mac") and not debug: ["FAIL", "PASS"]
      if (os == "linux") and not fission: ["FAIL", "PASS"]
      FAIL

  [The ServiceWorker objects returned from waiting attribute getter that represent the same service worker are the same objects]
    expected:
      if fission and (os == "linux") and debug: ["PASS", "NOTRUN"]
      if fission and (os == "linux") and not debug: ["PASS", "NOTRUN"]

