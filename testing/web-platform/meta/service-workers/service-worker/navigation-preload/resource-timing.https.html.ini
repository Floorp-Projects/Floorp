[resource-timing.https.html]
  expected:
    if os == "win": OK
    if os == "android": OK
    [OK, TIMEOUT]
  [Navigation Preload Resource Timing.]
    expected:
      if os == "win": FAIL
      if os == "android": FAIL
      [FAIL, TIMEOUT]

