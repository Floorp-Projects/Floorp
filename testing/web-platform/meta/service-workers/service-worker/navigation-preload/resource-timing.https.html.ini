[resource-timing.https.html]
  expected:
    if os == "win": OK
    if os == "android": OK
    [OK, TIMEOUT]
  [Navigation Preload Resource Timing.]
    expected:
      if os == "android": FAIL
      if os == "win": FAIL
      [FAIL, TIMEOUT]

