[iframe-all-local-schemes-inherit-self.sub.html]
  expected:
    if fission and (os == "linux") and not debug and webrender: ["TIMEOUT", "OK"]
    [OK, TIMEOUT]
  [<iframe>'s about:blank inherits policy.]
    expected:
      if fission and (os == "linux") and not debug and webrender: ["TIMEOUT", "PASS"]
      [PASS, TIMEOUT]

