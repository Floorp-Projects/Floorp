[iframe-all-local-schemes-inherit-self.sub.html]
  expected:
    if fission and (os == "linux") and not debug and (processor == "x86"): [OK, TIMEOUT]
    if fission and (os == "win") and not debug and (processor == "x86"): [OK, TIMEOUT]
    if fission and (os == "mac") and debug: [OK, TIMEOUT]
    if fission and (os == "mac") and not debug: [OK, TIMEOUT]
    if fission and (os == "linux") and debug: [OK, TIMEOUT]
    if fission and (os == "win") and debug: [OK, TIMEOUT]
    [TIMEOUT, OK]
  [<iframe>'s about:blank inherits policy.]
    expected:
      if fission and (os == "win") and not debug and (processor == "x86"): [PASS, TIMEOUT]
      if fission and (os == "linux") and not debug and (processor == "x86"): [PASS, TIMEOUT]
      if fission and (os == "win") and debug: [PASS, TIMEOUT]
      if fission and (os == "linux") and debug: [PASS, TIMEOUT]
      if fission and (os == "mac") and debug: [PASS, TIMEOUT]
      if fission and (os == "mac") and not debug: [PASS, TIMEOUT]
      [TIMEOUT, PASS]
