[iframe-all-local-schemes-inherit-self.sub.html]
  expected:
    if fission and not debug and (processor == "x86_64") and (os == "linux"): [TIMEOUT, OK]
    if fission and not debug and (processor == "x86_64") and (os == "win"): [TIMEOUT, OK]
    if not fission and (os == "android"): [TIMEOUT, OK]
    [OK, TIMEOUT]
  [<iframe>'s about:blank inherits policy.]
    expected:
      if fission and not debug and (processor == "x86_64") and (os == "win"): [TIMEOUT, PASS]
      if fission and not debug and (processor == "x86_64") and (os == "linux"): [TIMEOUT, PASS]
      if not fission and (os == "android"): [TIMEOUT, PASS]
      [PASS, TIMEOUT]
