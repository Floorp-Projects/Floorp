[iframe-all-local-schemes-inherit-self.sub.html]
  expected:
    if (os == "linux") and not swgl and not debug and (processor == "x86"): [OK, TIMEOUT]
    if (os == "linux") and swgl and fission: [OK, TIMEOUT]
    if (os == "linux") and not swgl and debug: [OK, TIMEOUT]
    if (os == "win") and not debug and (processor == "x86_64"): [OK, TIMEOUT]
    if (os == "win") and debug: [OK, TIMEOUT]
    if os == "mac": [OK, TIMEOUT]
    [TIMEOUT, OK]
  [<iframe>'s about:blank inherits policy.]
    expected:
      if (os == "linux") and not swgl and not debug and (processor == "x86"): [PASS, TIMEOUT]
      if (os == "win") and not debug and (processor == "x86_64"): [PASS, TIMEOUT]
      if (os == "linux") and swgl and fission: [PASS, TIMEOUT]
      if (os == "linux") and not swgl and debug: [PASS, TIMEOUT]
      if (os == "win") and debug: [PASS, TIMEOUT]
      if os == "mac": [PASS, TIMEOUT]
      [TIMEOUT, PASS]
