[iframe-all-local-schemes-inherit-self.sub.html]
  expected:
    if not fission and (os == "linux") and debug and not swgl: [TIMEOUT, OK]
    if fission and (os == "win") and not debug and (processor == "x86"): [TIMEOUT, OK]
    [OK, TIMEOUT]
  [<iframe>'s about:blank inherits policy.]
    expected:
      if fission and (os == "win") and not debug and (processor == "x86"): [TIMEOUT, PASS]
      if not fission and (os == "linux") and debug and not swgl: [TIMEOUT, PASS]
      [PASS, TIMEOUT]
