[spv-only-sent-to-initiator.sub.html]
  expected:
    if fission and not debug and not webrender: ["OK", "TIMEOUT"]
    if not debug and (os == "mac"): ["OK", "TIMEOUT"]
  [Test that no spv event is raised]
    expected:
      if fission and not debug and not webrender: ["FAIL", "NOTRUN"]
      if (os == "android") and not debug and not asan: ["FAIL", "NOTRUN"]
      if not debug and not asan and (os == "linux") and (bits == 64) and (os_version == '18.04'): ["FAIL", "NOTRUN"]
      if not debug and not asan and (os == "mac") and (bits == 64) and (os_version == '10.14'): ["FAIL", "NOTRUN"]
      FAIL

  [Violation report status OK.]
    expected:
      if webrender and not debug and not fission: ["FAIL", "PASS"]
      FAIL

