[spv-only-sent-to-initiator.sub.html]
  expected:
    if (os == "linux") and (processor == "x86_64") and not fission and debug and not swgl: [TIMEOUT, OK]
    if (os == "android") and fission: [TIMEOUT, OK]
    if (os == "mac") and not debug: [TIMEOUT, OK]
    if (os == "mac") and debug: [TIMEOUT, OK]
    [OK, TIMEOUT]
  [Test that no spv event is raised]
    expected:
      if (os == "linux") and (processor == "x86_64") and not fission and debug and not swgl: [NOTRUN, FAIL]
      if (os == "android") and fission: [NOTRUN, FAIL]
      if (os == "mac") and debug: [NOTRUN, FAIL]
      if (os == "mac") and not debug: [NOTRUN, FAIL]
      [FAIL, NOTRUN]

  [Violation report status OK.]
    expected: FAIL
