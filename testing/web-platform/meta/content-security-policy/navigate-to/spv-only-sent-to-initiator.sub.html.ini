[spv-only-sent-to-initiator.sub.html]
  expected:
    if not fission and (os == "android") and not debug and not webrender: [TIMEOUT, OK]
    if not fission and (os == "mac") and debug: [TIMEOUT, OK]
    [OK, TIMEOUT]
  [Test that no spv event is raised]
    expected:
      if not fission and (os == "android") and not debug and not webrender: [NOTRUN, FAIL]
      if not fission and (os == "mac") and debug: [NOTRUN, FAIL]
      [FAIL, NOTRUN]

  [Violation report status OK.]
    expected: FAIL

