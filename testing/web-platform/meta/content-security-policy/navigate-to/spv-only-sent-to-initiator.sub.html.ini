[spv-only-sent-to-initiator.sub.html]
  expected:
    if not fission and (os == "linux") and debug and not swgl: [TIMEOUT, OK]
    if not fission and (os == "linux") and debug and swgl: [TIMEOUT, OK]
    if fission and (os == "linux") and debug and not swgl: [TIMEOUT, OK]
    if fission and (os == "mac") and debug: [TIMEOUT, OK]
    if fission and (os == "android"): [TIMEOUT, OK]
    [OK, TIMEOUT]
  [Test that no spv event is raised]
    expected:
      if not fission and (os == "linux") and debug and not swgl: [NOTRUN, FAIL]
      if not fission and (os == "linux") and debug and swgl: [NOTRUN, FAIL]
      if fission and (os == "linux") and debug and not swgl: [NOTRUN, FAIL]
      if fission and (os == "mac") and debug: [NOTRUN, FAIL]
      if fission and (os == "android"): [NOTRUN, FAIL]
      [FAIL, NOTRUN]

  [Violation report status OK.]
    expected: FAIL
