[spv-only-sent-to-initiator.sub.html]
  expected:
    if fission and (processor == "x86_64") and not swgl and (os == "linux") and not debug: [TIMEOUT, OK]
    if fission and (processor == "x86_64") and not swgl and (os == "mac") and debug: [TIMEOUT, OK]
    [OK, TIMEOUT]
  [Test that no spv event is raised]
    expected:
      if fission and (processor == "x86_64") and not swgl and (os == "mac") and debug: [NOTRUN, FAIL]
      if fission and (processor == "x86_64") and not swgl and (os == "linux") and not debug: [NOTRUN, FAIL]
      [FAIL, NOTRUN]

  [Violation report status OK.]
    expected: FAIL
