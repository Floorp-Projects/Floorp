[spv-only-sent-to-initiator.sub.html]
  expected:
    if (os == "linux") and not fission and (processor == "x86_64") and not swgl and debug and webrender: ["TIMEOUT", "OK"]
    if os == "mac": ["TIMEOUT", "OK"]
    [OK, TIMEOUT]
  [Test that no spv event is raised]
    expected:
      if (os == "linux") and not fission and (processor == "x86_64") and not swgl and debug and webrender: ["NOTRUN", "FAIL"]
      if os == "mac": ["NOTRUN", "FAIL"]
      [FAIL, NOTRUN]

  [Violation report status OK.]
    expected: FAIL

