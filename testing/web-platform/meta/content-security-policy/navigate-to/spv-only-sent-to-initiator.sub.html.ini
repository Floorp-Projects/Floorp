[spv-only-sent-to-initiator.sub.html]
  expected:
    if fission and (processor == "x86_64") and (os == "linux") and not debug: [TIMEOUT, OK]
    if fission and (processor == "x86_64") and (os == "linux") and debug: [TIMEOUT, OK]
    if fission and (processor == "x86_64") and (os == "mac") and not debug: [TIMEOUT, OK]
    [OK, TIMEOUT]
  [Test that no spv event is raised]
    expected:
      if fission and (processor == "x86_64") and (os == "mac") and not debug: [NOTRUN, FAIL]
      if fission and (processor == "x86_64") and (os == "linux") and debug: [NOTRUN, FAIL]
      if fission and (processor == "x86_64") and (os == "linux") and not debug: [NOTRUN, FAIL]
      [FAIL, NOTRUN]

  [Violation report status OK.]
    expected: FAIL
