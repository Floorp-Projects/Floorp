[spv-only-sent-to-initiator.sub.html]
  expected:
    if not fission and (os == "linux") and debug and webrender and not swgl: ["TIMEOUT", "OK"]
    if not fission and (os == "mac") and debug: ["TIMEOUT", "OK"]
    if not fission and (os == "mac") and not debug: ["TIMEOUT", "OK"]
    [OK, TIMEOUT]
  [Test that no spv event is raised]
    expected:
      if not fission and (os == "linux") and debug and webrender and not swgl: ["NOTRUN", "FAIL"]
      if not fission and (os == "mac") and debug: ["NOTRUN", "FAIL"]
      if not fission and (os == "mac") and not debug: ["NOTRUN", "FAIL"]
      [FAIL, NOTRUN]

  [Violation report status OK.]
    expected: FAIL

