[spv-only-sent-to-initiator.sub.html]
  expected:
    if fission and debug and (os == "linux") and not swgl: [TIMEOUT, OK]
    if fission and debug and (os == "linux") and swgl: [TIMEOUT, OK]
    if fission and not debug and (os == "linux") and (processor == "x86"): [TIMEOUT, OK]
    if fission and not debug and (os == "mac"): [TIMEOUT, OK]
    [OK, TIMEOUT]
  [Test that no spv event is raised]
    expected:
      if fission and not debug and (os == "linux") and (processor == "x86"): [NOTRUN, FAIL]
      if fission and debug and (os == "linux") and swgl: [NOTRUN, FAIL]
      if fission and debug and (os == "linux") and not swgl: [NOTRUN, FAIL]
      if fission and not debug and (os == "mac"): [NOTRUN, FAIL]
      [FAIL, NOTRUN]

  [Violation report status OK.]
    expected: FAIL
