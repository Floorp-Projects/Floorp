[spv-only-sent-to-initiator.sub.html]
  expected:
    if (os == "android") and not debug and webrender: [TIMEOUT, OK]
    if webrender and (os == "android") and debug: [TIMEOUT, OK]
    if webrender and (os == "linux") and not debug: [TIMEOUT, OK]
    [OK, TIMEOUT]
  [Test that no spv event is raised]
    expected:
      if (os == "android") and not debug and webrender: [NOTRUN, FAIL]
      if webrender and (os == "linux") and not debug: [NOTRUN, FAIL]
      if webrender and (os == "android") and debug: [NOTRUN, FAIL]
      [FAIL, NOTRUN]

