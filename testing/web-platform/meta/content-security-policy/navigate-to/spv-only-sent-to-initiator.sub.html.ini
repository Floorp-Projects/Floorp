[spv-only-sent-to-initiator.sub.html]
  expected:
    if not debug and not fission and (processor == "x86_64") and (os == "android") and not webrender: ["TIMEOUT", "OK"]
    if not debug and not fission and (processor == "x86_64") and (os == "linux") and webrender: ["TIMEOUT", "OK"]
    if debug and (os == "android") and webrender: ["TIMEOUT", "OK"]
    if not debug and fission: ["TIMEOUT", "OK"]
    [OK, TIMEOUT]
  [Test that no spv event is raised]
    expected:
      if not debug and not fission and (processor == "x86_64") and (os == "android") and not webrender: ["NOTRUN", "FAIL"]
      if not debug and not fission and (processor == "x86_64") and (os == "linux") and webrender: ["NOTRUN", "FAIL"]
      if debug and (os == "android") and webrender: ["NOTRUN", "FAIL"]
      if not debug and fission: ["NOTRUN", "FAIL"]
      [FAIL, NOTRUN]

  [Violation report status OK.]
    expected: FAIL

