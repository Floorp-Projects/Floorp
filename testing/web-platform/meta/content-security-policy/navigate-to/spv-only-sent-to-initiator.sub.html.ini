[spv-only-sent-to-initiator.sub.html]
  expected:
    if fission and debug and not swgl and (os == "mac"): [TIMEOUT, OK]
    if fission and debug and not swgl and (os == "linux"): [TIMEOUT, OK]
    if fission and debug and swgl: [TIMEOUT, OK]
    [OK, TIMEOUT]
  [Test that no spv event is raised]
    expected:
      if fission and debug and not swgl and (os == "mac"): [NOTRUN, FAIL]
      if fission and debug and not swgl and (os == "linux"): [NOTRUN, FAIL]
      if fission and debug and swgl: [NOTRUN, FAIL]
      [FAIL, NOTRUN]

  [Violation report status OK.]
    expected: FAIL
