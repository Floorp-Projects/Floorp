[spv-only-sent-to-initiator.sub.html]
  expected:
    if (os == "linux") and not debug and not fission and webrender: ["TIMEOUT", "OK"]
    if (os == "android") and not debug and not webrender: ["TIMEOUT", "OK"]
    if (os == "mac") and debug: ["TIMEOUT", "OK"]
    if (os == "mac") and not debug: ["TIMEOUT", "OK"]
    [OK, TIMEOUT]
  [Test that no spv event is raised]
    expected:
      if (os == "linux") and not debug and not fission and webrender: ["NOTRUN", "FAIL"]
      if (os == "android") and not debug and not webrender: ["NOTRUN", "FAIL"]
      if (os == "mac") and not debug: ["NOTRUN", "FAIL"]
      if (os == "mac") and debug: ["NOTRUN", "FAIL"]
      [FAIL, NOTRUN]

  [Violation report status OK.]
    expected: FAIL

