[freeze.html]
  expected:
    if os == "android": OK
    if os == "win": OK
    [OK, TIMEOUT]
  [Test freeze callback.]
    expected:
      if os == "win": FAIL
      if os == "android": FAIL
      [FAIL, TIMEOUT]

