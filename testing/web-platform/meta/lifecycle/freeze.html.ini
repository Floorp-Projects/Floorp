[freeze.html]
  expected:
    if os == "win": OK
    if os == "android": OK
    [OK, TIMEOUT]
  [Test freeze callback.]
    expected:
      if os == "win": FAIL
      if os == "android": FAIL
      [FAIL, TIMEOUT]

