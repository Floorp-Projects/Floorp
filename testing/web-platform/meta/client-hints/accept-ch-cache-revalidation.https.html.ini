[accept-ch-cache-revalidation.https.html]
  expected:
    if (os == "linux") and not webrender and not fission: ["OK", "TIMEOUT"]
  [Same headers sent for revalidation request]
    expected:
      if (os == "linux") and not webrender and not fission: ["PASS", "TIMEOUT"]

