[pointerevent_setpointercapture_inactive_button_mouse.html]
  prefs: [test.events.async.enabled:true]
  disabled:
    if verify and (os == "mac"): https://bugzilla.mozilla.org/show_bug.cgi?id=1668959
  expected:
    if not debug and webrender and (os == "linux") and not fission: ["OK", "TIMEOUT"]
    if not debug and webrender and (os == "linux") and fission: ["OK", "TIMEOUT"]
    if not debug and webrender and (os == "win") and not fission: ["OK", "TIMEOUT"]
    if not debug and webrender and (os == "win") and fission: ["OK", "TIMEOUT"]
  [setPointerCapture + inactive button state]
    expected:
      if not debug and webrender and (os == "win") and fission: ["PASS", "NOTRUN"]
      if not debug and webrender and (os == "win") and not fission: ["PASS", "NOTRUN"]
      if not debug and webrender and (os == "linux") and not fission: ["PASS", "NOTRUN"]
      if not debug and webrender and (os == "linux") and fission: ["PASS", "NOTRUN"]

