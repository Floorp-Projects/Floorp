[pointerevent_setpointercapture_inactive_button_mouse.html]
  prefs: [test.events.async.enabled:true]
  disabled:
    if verify and (os == "mac"): https://bugzilla.mozilla.org/show_bug.cgi?id=1668959
  expected:
    if not fission and webrender and (os == "win") and not debug: ["OK", "TIMEOUT"]
    if not fission and webrender and (os == "linux") and not debug: ["OK", "TIMEOUT"]
    if fission and (os == "linux") and not debug: ["OK", "TIMEOUT"]
    if fission and (os == "win"): ["OK", "TIMEOUT"]
  [setPointerCapture + inactive button state]
    expected:
      if not fission and webrender and (os == "win") and not debug: ["PASS", "NOTRUN"]
      if not fission and webrender and (os == "linux") and not debug: ["PASS", "NOTRUN"]
      if fission and (os == "linux") and not debug: ["PASS", "NOTRUN"]
      if fission and (os == "win"): ["PASS", "NOTRUN"]

