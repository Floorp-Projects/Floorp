[pointerevent_mouse-pointer-preventdefault.html]
  expected:
    if (os == "mac") and not debug: ["OK", "TIMEOUT"]
  [Verifies the effect of pointer event prevent-default on pointermove]
    expected:
      if (os == "mac") and not debug: ["PASS", "TIMEOUT"]

