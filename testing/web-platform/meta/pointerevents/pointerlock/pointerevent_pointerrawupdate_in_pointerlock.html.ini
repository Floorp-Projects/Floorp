[pointerevent_pointerrawupdate_in_pointerlock.html]
  expected:
    if not fission and (os == "linux") and debug: [OK, TIMEOUT]
    if fission and (os == "linux") and not debug: [OK, TIMEOUT]
    if fission and (os == "linux") and debug: [OK, TIMEOUT]
    if fission and (os == "win") and debug: [OK, TIMEOUT]
  [pointerrawupdate event received]
    expected:
      if fission and (os == "win") and debug: [FAIL, NOTRUN]
      if fission and (os == "linux") and not debug: [FAIL, NOTRUN]
      if fission and (os == "linux") and debug: [FAIL, NOTRUN]
      if not fission and (os == "linux") and debug: [FAIL, NOTRUN]
      FAIL
