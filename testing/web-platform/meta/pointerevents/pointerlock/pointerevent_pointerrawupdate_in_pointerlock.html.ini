[pointerevent_pointerrawupdate_in_pointerlock.html]
  expected:
    if (os == "linux") and fission and debug and not swgl and not domstreams: [OK, TIMEOUT]
    if (os == "linux") and fission and debug and not swgl and domstreams: [OK, TIMEOUT]
    if (os == "linux") and fission and debug and swgl: [OK, TIMEOUT]
    if (os == "linux") and not fission and debug: [OK, TIMEOUT]
    if (os == "linux") and fission and not debug: [OK, TIMEOUT]
    if (os == "win") and debug and fission: [OK, TIMEOUT]
  [pointerrawupdate event received]
    expected:
      if (os == "linux") and fission and debug and not swgl and not domstreams: [FAIL, NOTRUN]
      if (os == "linux") and fission and debug and not swgl and domstreams: [FAIL, NOTRUN]
      if (os == "linux") and fission and debug and swgl: [FAIL, NOTRUN]
      if (os == "linux") and not fission and debug: [FAIL, NOTRUN]
      if (os == "linux") and fission and not debug: [FAIL, NOTRUN]
      if (os == "win") and debug and fission: [FAIL, NOTRUN]
      FAIL
