[pointerevent_movementxy.html]
  expected:
    if webrender and fission and not debug and (os == "win"): ["OK", "ERROR"]
    if not webrender and (os == "mac") and debug: ["OK", "ERROR"]
    if webrender and fission and debug: ["OK", "ERROR"]
    [ERROR, OK]
  [mouse pointerevent attributes]
    expected: FAIL

