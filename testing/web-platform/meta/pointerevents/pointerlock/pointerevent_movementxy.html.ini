[pointerevent_movementxy.html]
  expected:
    if not webrender and (os == "linux") and debug and not sw-e10s: [ERROR, OK]
    if webrender and (os == "win") and not debug: [ERROR, OK]
    if webrender and (os == "win") and debug: [OK, ERROR]
    if (os == "linux") and debug and sw-e10s: [ERROR, OK]
    if (os == "linux") and not debug and webrender: [ERROR, OK]
    ERROR
  [mouse pointerevent attributes]
    expected: FAIL

