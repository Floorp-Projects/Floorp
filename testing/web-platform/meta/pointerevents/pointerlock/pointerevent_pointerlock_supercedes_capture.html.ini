[pointerevent_pointerlock_supercedes_capture.html]
  [mouse no pointercapture while pointerlock]
    expected:
      if (os == "win") and not fission and debug and webrender: [PASS, FAIL]
      if (os == "win") and not fission and not debug and not webrender: [PASS, FAIL]
      if (os == "linux") and debug and not fission and webrender: [PASS, FAIL]
      if (os == "linux") and debug and not fission and not webrender: [PASS, FAIL]
      if (os == "linux") and not debug and webrender and not fission: [PASS, FAIL]
      if os == "mac": [PASS, FAIL]

