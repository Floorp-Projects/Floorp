[pointerevent_getCoalescedEvents_when_pointerlocked.html]
  [mouse pointermove getCoalescedEvents when lock test]
    expected:
      if not fission and (os == "win") and not swgl and not debug and (processor == "x86"): [FAIL, PASS]
      if not fission and (os == "win") and not swgl and debug and (processor == "x86"): [FAIL, PASS]
      if not fission and (os == "linux") and swgl: [FAIL, PASS]
      if fission and (os == "win"): [FAIL, PASS]
      [PASS, FAIL]
