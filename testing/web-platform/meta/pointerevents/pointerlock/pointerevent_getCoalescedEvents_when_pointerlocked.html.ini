[pointerevent_getCoalescedEvents_when_pointerlocked.html]
  [mouse pointermove getCoalescedEvents when lock test]
    expected:
      if (os == "linux") and debug and not swgl and webrender and fission: [FAIL, PASS]
      if (os == "linux") and debug and not swgl and not webrender: [FAIL, PASS]
      if (os == "linux") and debug and swgl: [FAIL, PASS]
      if (os == "android") and not debug and not webrender: [FAIL, PASS]
      if (os == "win") and not debug and webrender: [FAIL, PASS]
      [PASS, FAIL]
