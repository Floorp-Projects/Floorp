[pointerevent_getCoalescedEvents_when_pointerlocked.html]
  [mouse pointermove getCoalescedEvents when lock test]
    expected:
      if not fission and (os == "linux") and not swgl and not debug and (processor == "x86"): [FAIL, PASS]
      if not fission and (os == "linux") and not swgl and not debug and (processor == "x86_64"): [FAIL, PASS]
      if fission and not debug and (os == "win"): [FAIL, PASS]
      if fission and not debug and (os == "linux"): [FAIL, PASS]
      if fission and debug and (os == "win"): [FAIL, PASS]
      [PASS, FAIL]
