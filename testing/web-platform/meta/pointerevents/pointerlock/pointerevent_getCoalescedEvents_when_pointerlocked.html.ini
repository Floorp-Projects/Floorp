[pointerevent_getCoalescedEvents_when_pointerlocked.html]
  [mouse pointermove getCoalescedEvents when lock test]
    expected:
      if debug and not webrender and (os == "win") and (processor == "x86_64"): ["FAIL", "PASS"]
      if debug and webrender and not fission: ["FAIL", "PASS"]
      if not debug and fission and (os == "linux"): ["FAIL", "PASS"]
      [PASS, FAIL]

