[pointerevent_getCoalescedEvents_when_pointerlocked.html]
  [mouse pointermove getCoalescedEvents when lock test]
    expected:
      if (os == "win") and (processor == "x86_64") and not swgl and debug and not fission: [FAIL, PASS]
      if (os == "win") and (processor == "x86_64") and not swgl and not debug and fission: [FAIL, PASS]
      if (os == "win") and (processor == "x86_64") and swgl: [FAIL, PASS]
      [PASS, FAIL]
