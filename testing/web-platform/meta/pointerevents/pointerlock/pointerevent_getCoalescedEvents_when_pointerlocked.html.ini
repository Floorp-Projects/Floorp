[pointerevent_getCoalescedEvents_when_pointerlocked.html]
  [mouse pointermove getCoalescedEvents when lock test]
    expected:
      if (os == "win") and debug and not fission and (processor == "x86_64") and webrender and not swgl: [FAIL, PASS]
      if (os == "win") and debug and fission: [FAIL, PASS]
      if (os == "android") and not debug and not webrender: [FAIL, PASS]
      [PASS, FAIL]
