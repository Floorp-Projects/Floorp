[pointerevent_getCoalescedEvents_when_pointerlocked.html]
  [mouse pointermove getCoalescedEvents when lock test]
    expected:
      if (os == "linux") and debug and webrender and not swgl and not fission: [FAIL, PASS]
      if (os == "win") and not swgl and not debug and webrender: [FAIL, PASS]
      if (os == "linux") and debug and webrender and swgl: [FAIL, PASS]
      if (os == "win") and swgl: [FAIL, PASS]
      [PASS, FAIL]
