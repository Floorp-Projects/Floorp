[pointerevent_getCoalescedEvents_when_pointerlocked.html]
  [mouse pointermove getCoalescedEvents when lock test]
    expected:
      if fission and (os == "linux") and not swgl and (processor == "x86"): [PASS, FAIL]
      if not fission and (os == "linux") and not swgl and debug: [PASS, FAIL]
      if not fission and (os == "linux") and not swgl and not debug: [PASS, FAIL]
      if fission and (os == "linux") and swgl: [PASS, FAIL]
      if not fission and (os == "linux") and swgl: [PASS, FAIL]
      if not fission and (os == "android"): [PASS, FAIL]
      [FAIL, PASS]
