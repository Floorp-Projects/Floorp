[pointerevent_movementxy_with_pointerlock.html]
  expected:
    if (os == "win") and not webrender: OK
    if os == "android": OK
    if os == "mac": [OK, ERROR, TIMEOUT]
    [OK, ERROR]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if (os == "win") and debug and webrender and not fission and (processor == "x86"): [FAIL, PASS, TIMEOUT]
      if (os == "win") and debug and webrender and fission: [FAIL, PASS, TIMEOUT]
      [PASS, FAIL, TIMEOUT]
