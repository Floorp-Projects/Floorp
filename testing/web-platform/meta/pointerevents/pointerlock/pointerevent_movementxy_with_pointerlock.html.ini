[pointerevent_movementxy_with_pointerlock.html]
  expected:
    if (os == "win") and not webrender: OK
    if os == "android": OK
    if os == "mac": ["OK", "ERROR", "TIMEOUT"]
    [OK, ERROR]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if not fission and webrender and (os == "linux") and debug and not swgl: ["FAIL", "PASS", "TIMEOUT"]
      if not fission and webrender and (os == "win") and not debug: ["FAIL", "PASS", "TIMEOUT"]
      [PASS, FAIL, TIMEOUT]

