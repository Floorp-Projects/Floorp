[pointerevent_movementxy_with_pointerlock.html]
  expected:
    if os == "mac": [OK, ERROR, TIMEOUT]
    if os == "android": OK
    [OK, ERROR]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if (os == "win") and not swgl and not debug and not fission and (processor == "x86_64"): [FAIL, PASS, TIMEOUT]
      if (os == "linux") and swgl: [FAIL, PASS, TIMEOUT]
      [PASS, FAIL, TIMEOUT]
