[pointerevent_movementxy_with_pointerlock.html]
  expected:
    if os == "android": OK
    if os == "mac": [OK, ERROR, TIMEOUT]
    [OK, ERROR]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if (os == "linux") and not fission and (processor == "x86_64") and webrender and not debug: [FAIL, PASS, TIMEOUT]
      if (os == "win") and fission and not debug: [FAIL, PASS, TIMEOUT]
      if (os == "win") and not fission and swgl: [FAIL, PASS, TIMEOUT]
      if (os == "mac") and debug: [FAIL, PASS, TIMEOUT]
      [PASS, FAIL, TIMEOUT]
