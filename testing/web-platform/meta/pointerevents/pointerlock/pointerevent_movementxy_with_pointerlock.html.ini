[pointerevent_movementxy_with_pointerlock.html]
  disabled:
    if tsan: https://bugzilla.mozilla.org/show_bug.cgi?id=1701693
  expected:
    if (os == "win") and not webrender: OK
    if os == "android": OK
    if os == "mac": ["OK", "ERROR", "TIMEOUT"]
    [OK, ERROR]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if (os == "win") and not fission and (processor == "x86_64") and not swgl and debug and webrender: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "win") and not fission and (processor == "x86_64") and not swgl and not debug and not webrender: ["FAIL", "PASS", "TIMEOUT"]
      [PASS, FAIL, TIMEOUT]

