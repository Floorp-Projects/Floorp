[pointerevent_movementxy_with_pointerlock.html]
  expected:
    if (os == "win") and not webrender: OK
    if os == "mac": ["OK", "ERROR", "TIMEOUT"]
    if os == "android": OK
    [OK, ERROR]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if (os == "win") and debug and (processor == "x86_64") and webrender and not fission and not swgl: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "win") and debug and (processor == "x86"): ["FAIL", "PASS", "TIMEOUT"]
      if (os == "linux") and debug and fission: ["FAIL", "PASS", "TIMEOUT"]
      [PASS, FAIL, TIMEOUT]

