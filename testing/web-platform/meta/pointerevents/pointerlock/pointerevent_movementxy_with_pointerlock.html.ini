[pointerevent_movementxy_with_pointerlock.html]
  expected:
    if (os == "win") and not webrender: OK
    if os == "android": OK
    if os == "mac": ["OK", "ERROR", "TIMEOUT"]
    [OK, ERROR]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if (os == "win") and not swgl and (processor == "x86_64") and debug and not webrender: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "linux") and webrender and not swgl and not debug and fission: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "linux") and webrender and swgl: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "mac") and debug: ["FAIL", "PASS", "TIMEOUT"]
      [PASS, FAIL, TIMEOUT]

