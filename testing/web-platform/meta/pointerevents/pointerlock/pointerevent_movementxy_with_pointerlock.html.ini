[pointerevent_movementxy_with_pointerlock.html]
  expected:
    if os == "mac": [OK, ERROR, TIMEOUT]
    if os == "android": OK
    [OK, ERROR]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if (os == "win") and not fission and debug and (processor == "x86_64") and not swgl: [FAIL, PASS, TIMEOUT]
      if (os == "win") and not fission and debug and (processor == "x86_64") and swgl: [FAIL, PASS, TIMEOUT]
      if (os == "linux") and not swgl and fission and debug: [FAIL, PASS, TIMEOUT]
      if (os == "win") and not fission and not debug and (processor == "x86"): [FAIL, PASS, TIMEOUT]
      [PASS, FAIL, TIMEOUT]
