[pointerevent_movementxy_with_pointerlock.html]
  expected:
    if os == "mac": [OK, ERROR, TIMEOUT]
    if os == "android": OK
    [OK, ERROR]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if not swgl and (os == "linux") and (processor == "x86_64") and debug and fission: [FAIL, PASS, TIMEOUT]
      [PASS, FAIL, TIMEOUT]
