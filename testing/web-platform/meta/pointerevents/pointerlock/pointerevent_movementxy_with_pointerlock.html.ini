[pointerevent_movementxy_with_pointerlock.html]
  expected:
    if os == "mac": [OK, ERROR, TIMEOUT]
    if os == "android": OK
    [OK, ERROR]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if (os == "linux") and (processor == "x86_64") and fission and not swgl and debug: [FAIL, PASS, TIMEOUT]
      if (os == "linux") and (processor == "x86_64") and fission and not swgl and not debug: [FAIL, PASS, TIMEOUT]
      [PASS, FAIL, TIMEOUT]
