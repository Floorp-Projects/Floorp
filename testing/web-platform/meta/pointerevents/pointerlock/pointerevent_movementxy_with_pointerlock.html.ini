[pointerevent_movementxy_with_pointerlock.html]
  expected:
    if (os == "win") and webrender and not debug and not fission: ["ERROR", "OK"]
    if (os == "linux") and not debug and not fission and webrender: ["OK", "ERROR", "TIMEOUT"]
    if (os == "linux") and not debug and not fission and not webrender: ["ERROR", "OK"]
    if (os == "linux") and not debug and fission: ["ERROR", "OK", "TIMEOUT"]
    if os == "mac": ["ERROR", "OK", "TIMEOUT"]
    ERROR
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if not fission and (os == "linux") and webrender and not debug: ["FAIL", "PASS", "TIMEOUT"]
      if fission and (os == "linux") and not debug: ["TIMEOUT", "FAIL", "PASS"]
      [TIMEOUT, PASS, FAIL]

