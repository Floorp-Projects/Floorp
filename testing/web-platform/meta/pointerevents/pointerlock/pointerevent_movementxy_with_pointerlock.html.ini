[pointerevent_movementxy_with_pointerlock.html]
  expected:
    if (os == "linux") and not debug: [OK, ERROR]
    if (os == "win") and webrender: [OK, ERROR]
    if os == "mac": [OK, ERROR, TIMEOUT]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if (os == "linux") and not webrender and debug: [FAIL, PASS, TIMEOUT]
      if (os == "linux") and not webrender and not debug: [FAIL, PASS, TIMEOUT]
      if (os == "linux") and webrender and fission: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "win") and webrender and not debug: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "android") and webrender: ["FAIL", "PASS", "TIMEOUT"]
      [PASS, FAIL, TIMEOUT]

