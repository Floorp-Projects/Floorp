[pointerevent_movementxy_with_pointerlock.html]
  disabled:
    if tsan: https://bugzilla.mozilla.org/show_bug.cgi?id=1701693
  expected:
    if (os == "win") and not webrender: OK
    if os == "android": OK
    if os == "mac": ["OK", "ERROR", "TIMEOUT"]
    [OK, ERROR]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if fission and webrender and (os == "win") and debug: ["FAIL", "PASS", "TIMEOUT"]
      if not fission and (processor == "x86") and (os == "win") and not debug: ["FAIL", "PASS", "TIMEOUT"]
      [PASS, FAIL, TIMEOUT]

