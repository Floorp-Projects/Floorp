[pointerevent_movementxy_with_pointerlock.html]
  expected:
    if (os == "win") and webrender and not fission: ["OK", "ERROR"]
    if (os == "linux") and not debug and webrender: ["OK", "ERROR", "TIMEOUT"]
    if (os == "linux") and not debug and not webrender: ["OK", "ERROR"]
    if os == "mac": ["OK", "ERROR", "TIMEOUT"]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if (os == "win") and (processor == "x86_64") and not fission and not debug and not webrender: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "win") and (processor == "x86_64") and not fission and debug and webrender: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "android") and debug and webrender: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "linux") and fission and debug: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "win") and (processor == "x86_64") and fission: ["FAIL", "PASS", "TIMEOUT"]
      [PASS, FAIL, TIMEOUT]

