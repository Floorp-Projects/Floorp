[pointerevent_movementxy_with_pointerlock.html]
  expected:
    if (os == "win") and not webrender: OK
    if os == "mac": ["OK", "ERROR", "TIMEOUT"]
    if os == "android": OK
    [OK, ERROR]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if debug and (os == "linux") and not fission and webrender and not swgl: ["FAIL", "PASS", "TIMEOUT"]
      if not debug and fission and (os == "linux"): ["FAIL", "PASS", "TIMEOUT"]
      [PASS, FAIL, TIMEOUT]

