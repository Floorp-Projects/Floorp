[pointerevent_movementxy_with_pointerlock.html]
  expected:
    if os == "mac": [OK, ERROR, TIMEOUT]
    if os == "android": OK
    [OK, ERROR]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if debug and not fission and (os == "linux") and swgl: [FAIL, PASS, TIMEOUT]
      if not debug and (os == "linux") and (processor == "x86"): [FAIL, PASS, TIMEOUT]
      [PASS, FAIL, TIMEOUT]
