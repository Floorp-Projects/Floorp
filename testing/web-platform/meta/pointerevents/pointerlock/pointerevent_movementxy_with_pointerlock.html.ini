[pointerevent_movementxy_with_pointerlock.html]
  disabled:
    if tsan: https://bugzilla.mozilla.org/show_bug.cgi?id=1701693
  expected:
    if (os == "win") and not webrender: OK
    if os == "mac": ["OK", "ERROR", "TIMEOUT"]
    if os == "android": OK
    [OK, ERROR]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if (os == "linux") and (processor == "x86_64") and not swgl and webrender and not debug and fission: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "win") and not fission and debug and not webrender and (processor == "x86_64"): ["FAIL", "PASS", "TIMEOUT"]
      if (os == "linux") and (processor == "x86_64") and not swgl and not webrender and debug: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "win") and fission and debug: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "android") and debug: ["FAIL", "PASS", "TIMEOUT"]
      [PASS, FAIL, TIMEOUT]

