[pointerevent_movementxy_with_pointerlock.html]
  expected:
    if (os == "win") and not webrender: OK
    if os == "mac": ["OK", "ERROR", "TIMEOUT"]
    if os == "android": OK
    [OK, ERROR]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if debug and (os == "linux") and webrender and not fission and swgl: ["FAIL", "PASS", "TIMEOUT"]
      if not debug and (os == "linux") and fission and not webrender: ["FAIL", "PASS", "TIMEOUT"]
      if debug and (os == "linux") and webrender and fission: ["FAIL", "PASS", "TIMEOUT"]
      if debug and (os == "win") and not webrender and (processor == "x86_64"): ["FAIL", "PASS", "TIMEOUT"]
      if not debug and (os == "win") and (processor == "x86"): ["FAIL", "PASS", "TIMEOUT"]
      [PASS, FAIL, TIMEOUT]

