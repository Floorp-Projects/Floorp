[pointerevent_movementxy_with_pointerlock.html]
  disabled:
    if tsan: https://bugzilla.mozilla.org/show_bug.cgi?id=1701693
  expected:
    if (os == "win") and not webrender: OK
    if os == "mac": ["OK", "ERROR", "TIMEOUT"]
    if os == "android": OK
    [OK, ERROR]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if (os == "linux") and debug and webrender and not fission and not swgl: ["FAIL", "PASS", "TIMEOUT"]
      if (os == "win") and not debug and not webrender and (processor == "x86_64"): ["FAIL", "PASS", "TIMEOUT"]
      if (os == "linux") and debug and not webrender: ["FAIL", "PASS", "TIMEOUT"]
      [PASS, FAIL, TIMEOUT]

