[pointerevent_movementxy_with_pointerlock.html]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if (processor == "x86_64") and (os == "win") and debug and not webrender and fission: [FAIL, PASS]
      if (processor == "x86_64") and (os == "win") and not debug and fission and not webrender: [FAIL, PASS]
      if (processor == "x86_64") and (os == "linux") and webrender and not debug and not fission: [PASS, FAIL]
      if (processor == "x86_64") and (os == "win") and debug and webrender: [FAIL, PASS]
      if (processor == "x86_64") and (os == "linux") and webrender and debug: [FAIL, PASS]
      if (processor == "x86_64") and (os == "mac") and debug: [FAIL, PASS]
      if not webrender and (os == "linux") and (processor == "x86_64") and debug and fission: ["FAIL", "PASS"]
      if not webrender and (os == "win") and (processor == "x86_64") and debug: ["FAIL", "PASS"]
      if webrender and fission and (os == "linux") and not debug: ["FAIL", "PASS"]
      if webrender and not fission and (os == "win"): ["FAIL", "PASS"]
      [PASS, FAIL]

