[pointerevent_movementxy_with_pointerlock.html]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if (os == "linux") and not debug and not fission and not webrender and (processor == "x86_64"): ["PASS", "FAIL"]
      if (os == "linux") and not debug and not fission and not webrender and (processor == "x86"): ["PASS", "FAIL"]
      if (os == "linux") and not debug and not fission and webrender: ["PASS", "FAIL"]
      if (os == "linux") and not debug and fission and webrender: ["PASS", "FAIL"]
      if (os == "linux") and debug and not webrender: ["PASS", "FAIL"]
      if (os == "win") and (processor == "x86"): ["PASS", "FAIL"]
      if os == "android": ["PASS", "FAIL"]
      [FAIL, PASS]

