[pointerevent_movementxy_with_pointerlock.html]
  expected:
    if (os == "win") and not webrender: OK
    if os == "android": OK
    if os == "mac": ["OK", "ERROR", "TIMEOUT"]
    [OK, ERROR]
  [mouse pointerevent movementX/Y with pointerlock test]
    expected:
      if debug and (os == "win") and not swgl and webrender and not fission: ["FAIL", "PASS", "TIMEOUT"]
      if debug and (os == "win") and swgl: ["FAIL", "PASS", "TIMEOUT"]
      if debug and (os == "linux") and not webrender: ["FAIL", "PASS", "TIMEOUT"]
      [PASS, FAIL, TIMEOUT]

