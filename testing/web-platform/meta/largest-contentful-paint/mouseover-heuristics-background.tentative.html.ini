[mouseover-heuristics-background.tentative.html]
  expected:
    if (os == "linux") and not tsan and not asan and debug and not fission: [ERROR, OK, TIMEOUT]
    if (os == "linux") and not tsan and not asan and not debug: [ERROR, OK, TIMEOUT]
    if (os == "win") and not debug and (processor == "x86"): [TIMEOUT, OK, ERROR]
    if (os == "linux") and tsan: [TIMEOUT, OK, ERROR]
    [ERROR, OK]
  [LCP mouseover heuristics ignore background-based zoom widgets]
    expected:
      if os == "linux": [TIMEOUT, PASS]
      TIMEOUT
