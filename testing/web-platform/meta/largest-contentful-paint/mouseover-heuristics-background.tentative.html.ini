[mouseover-heuristics-background.tentative.html]
  expected:
    if not asan and (os == "linux") and not fission and debug and not swgl: [ERROR, OK, TIMEOUT]
    if asan: [TIMEOUT, OK, ERROR]
    [ERROR, OK]
  [LCP mouseover heuristics ignore background-based zoom widgets]
    expected:
      if (os == "linux") and not debug and fission and not tsan: [TIMEOUT, PASS]
      if (os == "linux") and debug: [TIMEOUT, PASS]
      TIMEOUT
