[mouseover-heuristics-background.tentative.html]
  expected:
    if (os == "linux") and not fission and not swgl and not debug and asan: [ERROR, OK, TIMEOUT]
    if (os == "linux") and not fission and not swgl and debug: [ERROR, OK, TIMEOUT]
    [ERROR, OK]
  [LCP mouseover heuristics ignore background-based zoom widgets]
    expected:
      if (os == "linux") and not debug and asan: [TIMEOUT, PASS]
      if (os == "linux") and debug: [TIMEOUT, PASS]
      TIMEOUT
