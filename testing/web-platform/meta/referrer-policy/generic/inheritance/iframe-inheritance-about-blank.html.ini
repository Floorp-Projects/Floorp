[iframe-inheritance-about-blank.html]
  expected:
    if (os == "win") and (processor == "x86_64") and not webrender and not debug: ["TIMEOUT", "OK"]
    if (os == "win") and (processor == "x86_64") and not webrender and debug: ["TIMEOUT", "OK"]
    if (os == "linux") and webrender and debug and not swgl: ["TIMEOUT", "OK"]
    if (os == "linux") and webrender and not debug and not fission: TIMEOUT
    if (os == "win") and (processor == "x86") and debug: ["TIMEOUT", "OK"]
    if (os == "win") and (processor == "x86_64") and webrender: ["TIMEOUT", "OK"]
    if (os == "linux") and not webrender and debug: ["TIMEOUT", "OK"]
    if os == "mac": TIMEOUT
    [OK, TIMEOUT]
  [The value of document.referrer in an about:blank iframe is the outer document's full URL, regardless of referrer policy]
    expected: FAIL

  [The fetch() API in an about:blank iframe with the 'client' referrer is fetched with no 'Referer' header]
    expected: FAIL

  [A subresource fetched from an about:blank iframe is fetched with no 'Referer' header]
    expected:
      if (os == "win") and (processor == "x86_64") and not webrender and debug: ["NOTRUN", "PASS"]
      if (os == "win") and (processor == "x86_64") and not webrender and not debug: ["NOTRUN", "PASS"]
      if (os == "linux") and webrender and debug and not swgl: ["NOTRUN", "PASS"]
      if (os == "linux") and webrender and not debug and not fission: NOTRUN
      if (os == "win") and (processor == "x86_64") and webrender: ["NOTRUN", "PASS"]
      if (os == "win") and (processor == "x86") and debug: ["NOTRUN", "PASS"]
      if (os == "linux") and not webrender and debug: ["NOTRUN", "PASS"]
      if os == "mac": NOTRUN
      [PASS, NOTRUN]

  [The fetch() API in an about:blank iframe with a custom URL referrer is fetched with a 'Referer` header that uses the outer document's URL along with its referrer policy]
    expected: FAIL

