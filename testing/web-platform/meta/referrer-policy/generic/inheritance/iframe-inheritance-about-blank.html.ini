[iframe-inheritance-about-blank.html]
  expected:
    if (os == "linux") and not debug and (processor == "x86_64") and webrender and fission: ["OK", "TIMEOUT"]
    if (os == "win") and debug and not fission and not webrender and (processor == "x86_64"): ["OK", "TIMEOUT"]
    if (os == "linux") and not debug and (processor == "x86"): ["OK", "TIMEOUT"]
    if (os == "win") and debug and fission: ["OK", "TIMEOUT"]
    if (os == "android") and webrender and debug: ["OK", "TIMEOUT"]
    if os == "mac": TIMEOUT
    [TIMEOUT, OK]
  [The value of document.referrer in an about:blank iframe is the outer document's full URL, regardless of referrer policy]
    expected: FAIL

  [The fetch() API in an about:blank iframe with the 'client' referrer is fetched with no 'Referer' header]
    expected: FAIL

  [A subresource fetched from an about:blank iframe is fetched with no 'Referer' header]
    expected:
      if (os == "win") and debug and not fission and not webrender and (processor == "x86_64"): ["PASS", "NOTRUN"]
      if (os == "linux") and not debug and (processor == "x86_64") and webrender and fission: ["PASS", "NOTRUN"]
      if (os == "win") and debug and fission: ["PASS", "NOTRUN"]
      if (os == "android") and webrender and debug: ["PASS", "NOTRUN"]
      if (os == "linux") and not debug and (processor == "x86"): ["PASS", "NOTRUN"]
      if os == "mac": NOTRUN
      [NOTRUN, PASS]

  [The fetch() API in an about:blank iframe with a custom URL referrer is fetched with a 'Referer` header that uses the outer document's URL along with its referrer policy]
    expected: FAIL

