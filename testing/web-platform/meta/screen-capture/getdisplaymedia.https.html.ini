[getdisplaymedia.https.html]
  disabled:
    if (os == "android") and e10s: bug 1550895 (frequently fails on geckoview)
  expected:
    if (os == "win") and debug and webrender and not fission and (processor == "x86_64") and not swgl: [CRASH, OK]
    if (os == "win") and debug and webrender and not fission and (processor == "x86_64") and swgl: [OK, CRASH]
    if (os == "win") and debug and webrender and not fission and (processor == "x86"): [OK, CRASH]
    if (os == "win") and debug and webrender and fission: [OK, CRASH]
  [getDisplayMedia() with getSettings]
    expected: FAIL

  [applyConstraints(width or height) must downscale precisely]
    expected:
      if debug and webrender: [PASS, FAIL]
