[getdisplaymedia.https.html]
  disabled:
    if (os == "android") and e10s: bug 1550895 (frequently fails on geckoview)
  expected:
    if (os == "win") and (processor == "x86_64") and debug and not webrender: ["OK", "CRASH"]
    if (os == "win") and (processor == "x86_64") and debug and webrender: ["OK", "CRASH"]
    if (os == "win") and (processor == "x86") and not debug: ["OK", "ERROR", "CRASH"]
  [getDisplayMedia() with getSettings]
    expected: FAIL

  [applyConstraints(width or height) must downscale precisely]
    expected:
      if debug and (os == "win") and webrender: ["PASS", "FAIL"]
      if debug and (os == "mac"): ["PASS", "FAIL"]

