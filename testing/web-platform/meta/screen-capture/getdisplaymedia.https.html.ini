[getdisplaymedia.https.html]
  disabled:
    if (os == "android") and e10s: bug 1550895 (frequently fails on geckoview)
  expected:
    if (os == "win") and debug and not fission and (processor == "x86_64"): [OK, CRASH]
    if (os == "win") and debug and not fission and (processor == "x86"): [OK, TIMEOUT, CRASH]
    if (os == "win") and debug and fission: [OK, TIMEOUT, CRASH]
  [getDisplayMedia() with getSettings]
    expected: FAIL

  [applyConstraints(width or height) must downscale precisely]
    expected:
      if debug and (os == "win") and not fission and (processor == "x86_64") and not swgl: [PASS, FAIL]
      if debug and (os == "win") and not fission and (processor == "x86_64") and swgl: [PASS, FAIL]
      if debug and (os == "win") and not fission and (processor == "x86"): [PASS, FAIL]
      if debug and (os == "win") and fission: [PASS, FAIL]
      if debug and (os == "linux"): [PASS, FAIL]
      if debug and (os == "mac"): [PASS, FAIL]
