[getdisplaymedia.https.html]
  disabled:
    if (os == "android") and e10s: bug 1550895 (frequently fails on geckoview)
  expected:
    if (os == "win") and not fission and not webrender and not debug and (processor == "x86"): ["OK", "CRASH"]
    if (os == "win") and not fission and webrender and debug: ["OK", "CRASH"]
  [getDisplayMedia() with getSettings]
    expected: FAIL

  [applyConstraints(width or height) must downscale precisely]
    expected:
      if debug and webrender and not fission: ["PASS", "FAIL"]

