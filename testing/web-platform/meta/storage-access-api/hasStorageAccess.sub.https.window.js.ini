[hasStorageAccess.sub.https.window.html]
  expected:
    if (os == "android") and not fission and not swgl and debug: TIMEOUT
    if (os == "linux") and debug and fission and swgl: [TIMEOUT, OK]
    if (os == "android") and not fission and swgl: [TIMEOUT, OK]
    if (os == "linux") and not debug and fission: OK
    if (os == "win") and debug: TIMEOUT
    if (os == "mac") and debug: [TIMEOUT, OK]
    [OK, TIMEOUT]
