[hasStorageAccess.sub.https.window.html]
  expected:
    if debug and fission and (os == "mac"): [OK, TIMEOUT]
    if debug and not fission and swgl: [TIMEOUT, OK]
    if debug and not fission and not swgl: [OK, TIMEOUT]
    if not debug and (os == "android"): [OK, TIMEOUT]
