[hasStorageAccess.sub.https.window.html]
  expected:
    if not fission and not asan and swgl and (os == "android"): [TIMEOUT, OK]
    if fission and (os == "mac") and not debug: OK
    if fission and (os == "win"): OK
    if fission and (os == "linux"): OK
    [OK, TIMEOUT]
