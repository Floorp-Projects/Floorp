[products]
firefox =

[web-platform-tests]
remote_url = https://github.com/w3c/web-platform-tests.git
branch = master
sync_path = sync

[paths]
prefs = ../profiles
run_info = .

[manifest:upstream]
tests = tests
metadata = meta
url_base = /

[manifest:mozilla]
tests = mozilla/tests
metadata = mozilla/meta
url_base = /_mozilla/