[cts.https.html?q=webgpu:api,validation,query_set,create:count:*]
  [:type="occlusion"]
    expected:
      if os == "win": FAIL
      if os == "linux" and not debug: FAIL
      if os == "mac": FAIL

  [:type="timestamp"]
    expected:
      if os == "win": FAIL
      if os == "linux" and not debug: FAIL
      if os == "mac": FAIL
