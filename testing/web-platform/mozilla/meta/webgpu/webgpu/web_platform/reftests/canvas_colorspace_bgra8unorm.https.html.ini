[canvas_colorspace_bgra8unorm.https.html]
  expected:
    # This intermittently crashes on Windows.
    # See <https://bugzilla.mozilla.org/show_bug.cgi?id=1824042>.
    if os == "windows": [FAIL, TIMEOUT, CRASH]
    TIMEOUT
