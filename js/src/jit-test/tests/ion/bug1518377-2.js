g = (function(t, foreign) {
        "use asm";
        var ff = foreign.ff;
        function f() { ff() }
        return f
})(this, {
        ff: -0 || (this) ? inIon : a &= () => test()
})

function m(f) {
        for (var i = 0; i < 100; ++i) {
            f();
        }
}

m(g);
