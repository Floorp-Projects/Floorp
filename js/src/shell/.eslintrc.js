"use strict";

module.exports = {
  "extends": [
    "plugin:mozilla/recommended"
  ],

  "rules": {
    // SpiderMonkey's style doesn't match any of the possible options.
    "brace-style": "off",
  }
};
