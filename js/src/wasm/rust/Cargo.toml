[package]
name = "wasm-rust"
version = "0.1.0"
authors = ["The Spidermonkey developers"]
edition = "2018"

[lib]
crate-type = ["rlib"]
name = "wasm_rust"

[dependencies]
wat = { version = "1.0.14" }
wasmparser = { version = "0.48.2" }
