---
ExcludesLinter:
    description: >-
        Make sure the string foobar never appears in browser js
        files because it is bad
    rule: no-foobar
    exclude: ['**/foobar.js']
    extensions: ['.js', 'jsm']
    type: string
    payload: foobar
