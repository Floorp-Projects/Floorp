---
GlobalLinter:
    description: It's bad to have the string foobar in js files.
    include:
        - files
    type: global
    extensions: ['.js', '.jsm']
    payload: global_payload:global_payload
